---
phase: 05-chat-visibility-controls
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/chat/ChatPanel.tsx
  - src/__tests__/ChatPanel.test.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - UI-01

must_haves:
  truths:
    - "В ChatPanel.tsx добавлены 3 новых useState: sessionModel: string | null = null, sessionId: string | null = null, accumulatedUsage: Usage | null = null. Все три размещены сразу после существующего sessionResult-state (рядом с другими useState, НЕ с useRef)."
    - "Импорт Usage из ../../types/claude добавлен (или Usage уже доступен в существующем импорт-блоке)."
    - "Импорт IconEraser из @tabler/icons-react добавлен; импорт @mantine/core расширен до Stack, Group, Text, Badge, ActionIcon, Tooltip."
    - "Case 'system' в handleClaudeEvent расширен: setSessionModel(claudeEvent.model) при не-null model; setSessionId через функциональную форму (prevId) => ... с сайд-эффектом setAccumulatedUsage(null) при смене session_id (D-05-06). Существующая логика inline system-info message (создание ChatMessage с kind='system-info') СОХРАНЕНА БЕЗ ИЗМЕНЕНИЙ — D-05 'оставить как есть'."
    - "Case 'assistant' расширен: при наличии claudeEvent.message.usage вызывается setAccumulatedUsage функциональной формой prev => null ? {...curr} : { input_tokens: prev.input + curr.input, ... } (D-05-04, D-05-05). Defensive copy через spread {...curr} для init. Существующая логика textBlocks/updateMessages/resetStreamingState НЕ менялась."
    - "Case 'result' НЕ модифицирован в части usage — D-05-04 явно запрещает читать claudeEvent.usage (typed unknown, double-count risk). Существующая setSessionResult-логика сохранена."
    - "Создан useCallback handleClear, который атомарно сбрасывает: setMessages([]), setSessionResult(null), setSessionModel(null), setSessionId(null), setAccumulatedUsage(null), hasSessionRef.current = false, archivedMessagesRef.current = [], resetStreamingState(). Никакого notifications.show или t('chat.cleared') внутри (D-05-14)."
    - "Inline /clear ветка в handleSend (текущие строки 396-403) заменена на вызов handleClear(); — DRY (D-05-11). Логика '/clear' text-команды теперь идёт через тот же handleClear callback, что и UI-кнопка."
    - "В header Group (текущие строки 460-469) после Badge добавлен Tooltip-обёрнутый ActionIcon (Clear-button): ml='auto', size='xs', variant='subtle', onClick={handleClear}, disabled={isRunning} (D-05-07, D-05-10), aria-label={t('chat.clearChat')}, icon IconEraser size={14} stroke={1.5} (D-05-08). Tooltip имеет label={t('chat.clearChat')} withArrow position='bottom'."
    - "В JSX <StatusBar ... /> (текущая строка 520) прокинуты новые props: model={sessionModel ?? projectModel ?? null}, sessionId={sessionId}, usage={accumulatedUsage} (D-05-12 fallback resolves в caller). Существующие props isRunning и sessionResult сохранены."
    - "Никакого ConfirmModal перед Clear (D-05-09). Кнопка зовёт handleClear напрямую."
    - "В ChatPanel.test.tsx добавлены минимум 5 новых тестов: (a) accumulator растёт на assistant.usage, (b) system с новым session_id обнуляет accumulator, (c) Clear button disabled при isRunning, (d) клик Clear обнуляет messages/sessionModel/sessionId/accumulator (наблюдается через StatusBar prop-flow), (e) первый assistant без предыдущего system всё равно инициализирует accumulator. Существующие 6 тестов не сломаны."
    - "Phase 4 invariant сохранён — handleClaudeEvent остаётся в useCallback, useTauriListener в существующей форме, никаких unmount-triggers не добавлено. Аккумулятор продолжает копить даже когда панель неактивна (specifics truth #10)."
  artifacts:
    - path: "src/components/chat/ChatPanel.tsx"
      provides: "Полная интеграция Phase 5: state-источник для StatusBar (model/sessionId/usage), accumulator на assistant.usage, reset accumulator на смену session_id, handleClear callback с расширенным reset + UI-кнопка Clear с disabled={isRunning}. Закрывает все 4 Phase 5 success criteria + criterion #5 (no sticky-state)."
      contains_grep: "handleClear"
    - path: "src/__tests__/ChatPanel.test.tsx"
      provides: "Unit-тесты для state-аккумулятора, reset на смену session_id, disabled/click поведения Clear-button"
      contains_grep: "Clear button is disabled"
  key_links:
    - from: "src/components/chat/ChatPanel.tsx handleClaudeEvent case 'assistant'"
      to: "setAccumulatedUsage функциональный updater"
      via: "claudeEvent.message.usage (тип Usage из src/types/claude.ts:101-106)"
      pattern: "setAccumulatedUsage\\(\\(prev\\)"
    - from: "src/components/chat/ChatPanel.tsx handleClaudeEvent case 'system'"
      to: "setAccumulatedUsage(null) при смене session_id (D-05-06)"
      via: "функциональный setSessionId сравнивает prevId и claudeEvent.session_id"
      pattern: "claudeEvent\\.session_id !== prevId"
    - from: "src/components/chat/ChatPanel.tsx handleClear"
      to: "all reset state setters (setMessages, setSessionResult, setSessionModel, setSessionId, setAccumulatedUsage, hasSessionRef, archivedMessagesRef, resetStreamingState)"
      via: "useCallback с зависимостью [resetStreamingState]"
      pattern: "const handleClear = useCallback"
    - from: "src/components/chat/ChatPanel.tsx header ActionIcon Clear-button"
      to: "handleClear callback"
      via: "onClick={handleClear} + disabled={isRunning}"
      pattern: "onClick=\\{handleClear\\}"
    - from: "src/components/chat/ChatPanel.tsx <StatusBar />"
      to: "src/components/chat/StatusBar.tsx (план 05-02) props model/sessionId/usage"
      via: "JSX prop-flow: model={sessionModel ?? projectModel ?? null} sessionId={sessionId} usage={accumulatedUsage}"
      pattern: "<StatusBar[\\s\\S]*?usage=\\{accumulatedUsage\\}"
---

<objective>
Завершить Phase 5: расширить `src/components/chat/ChatPanel.tsx` тремя новыми state-полями (sessionModel, sessionId, accumulatedUsage), обработчиками событий `system`/`assistant` для их обновления, выделить переиспользуемый `handleClear` callback, добавить UI-кнопку Clear в header. Обновить `src/__tests__/ChatPanel.test.tsx` под новое поведение.

Привязка к D-XX (LOCKED), которые закрываются именно здесь:
- **D-05-04** — source of truth для usage = только `assistant.message.usage`. Никакого `result.usage`, никакого `stream_event.message_delta.usage`.
- **D-05-05** — накопление на уровне «вкладки до Reset». Reset triggers: Clear button / `/clear` text / новый session_id.
- **D-05-06** — reset accumulator только при смене `session_id`, НЕ при каждом `claude_start`.
- **D-05-07** — ActionIcon icon-only, `ml='auto'`, `size='xs'`, `variant='subtle'`.
- **D-05-08** — иконка `IconEraser`.
- **D-05-09** — никакого ConfirmModal — instant clear.
- **D-05-10** — `disabled={isRunning}`.
- **D-05-11** — выделить переиспользуемый `handleClear` callback (DRY).
- **D-05-12** — `model={sessionModel ?? projectModel ?? null}` fallback резолвится в caller.
- **D-05-13** — projectModel меняется реактивно, sessionModel перебивает (логически следует из формулы fallback в (D-05-12)).
- **D-05-14** — никакого notification на Clear; `chat.cleared` остаётся dead-key.

Закрывает Phase 5 Success Criterion #4 (Clear-button работает) и #5 (нет sticky-state — accumulator/model/sessionId сбрасываются на Clear и на смену session_id). Совместно с планом 05-02 закрывает success criteria #1, #2, #3 на уровне end-to-end data flow (state → props → render).

Purpose: соединить event-pipeline `claude-event` со StatusBar-видимостью, обеспечить чистый reset через UI-кнопку.

Output: обновлённые `ChatPanel.tsx` и `ChatPanel.test.tsx`. `npm run typecheck` и `npm run test` оба обязаны проходить.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-chat-visibility-controls/05-CONTEXT.md
@.planning/phases/05-chat-visibility-controls/05-PATTERNS.md
@.planning/phases/04-chat-persistence/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@src/components/chat/ChatPanel.tsx
@src/components/chat/StatusBar.tsx
@src/components/chat/PromptInput.tsx
@src/components/chat/MessageItem.tsx
@src/__tests__/ChatPanel.test.tsx
@src/types/claude.ts
@src/__tests__/setup.ts

<interfaces>
<!-- Существующий ChatPanelProps (lines 20-28) — НЕ менять. projectModel уже доступен. -->

interface ChatPanelProps {
  panelId?: string;
  mode?: string;
  cwd: string;
  projectId?: string;
  projectModel?: string | null;
  projectPermissionMode?: string;
  isActive?: boolean;
}

<!-- StatusBar контракт после плана 05-02 (что мы должны прокидывать). -->

interface StatusBarProps {
  isRunning: boolean;
  sessionResult: SessionResult | null;
  model: string | null;
  sessionId: string | null;
  usage: Usage | null;
}

<!-- Usage shape (для accumulator). -->

export interface Usage {
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens: number;
  cache_read_input_tokens: number;
}

<!-- Существующий handleClaudeEvent — расширяется case 'system' и 'assistant', case 'result' НЕ трогаем. -->
<!-- Ключевые строки для модификации (по текущему src/components/chat/ChatPanel.tsx): -->
<!--   - 44-54: state declarations (добавить 3 useState) -->
<!--   - 137-156: case 'system' (extend) -->
<!--   - 251-271: case 'assistant' (extend) -->
<!--   - 273-295: case 'result' (DO NOT TOUCH usage-related code) -->
<!--   - 396-403: inline /clear (replace with handleClear() call) -->
<!--   - 460-469: header Group (add ActionIcon Clear-button) -->
<!--   - 520: <StatusBar/> (add 3 new props) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить ChatPanel.tsx — state, handleClaudeEvent (cases system/assistant), handleClear callback, Clear-button в header, прокидывание props в StatusBar</name>
  <read_first>
    - src/components/chat/ChatPanel.tsx (полный файл 523 строки — нужен complete map существующих state, refs, callbacks; будем расширять в 7 точках)
    - src/types/claude.ts (строки 78-83 AssistantEvent shape; 101-106 Usage shape; 257-263 SessionResult; новый SessionMetadata из плана 05-01)
    - src/components/chat/StatusBar.tsx (после плана 05-02 — проверить актуальный StatusBarProps contract)
    - src/components/chat/PromptInput.tsx (строки 155-165 — analog для Tooltip→ActionIcon→IconX с disabled={isRunning})
    - src/components/chat/MessageItem.tsx (строки 45-64 — analog для ActionIcon size='xs' variant='subtle')
    - .planning/phases/05-chat-visibility-controls/05-CONTEXT.md (все D-05-04..14; specifics §1 точные строки кода; §3 truths #4, #5, #6, #7)
    - .planning/phases/05-chat-visibility-controls/05-PATTERNS.md (раздел ChatPanel.tsx Pattern A-F — точные code excerpts)
    - .planning/phases/04-chat-persistence/04-CONTEXT.md (D-04-01 keep-mounted, D-04-07 useTauriListener invariant — не сломать)
  </read_first>
  <files>src/components/chat/ChatPanel.tsx</files>
  <action>
В `src/components/chat/ChatPanel.tsx` выполнить следующие изменения единым атомарным редактированием. Все изменения additive; существующая логика по возможности сохраняется в неприкосновенности.

**(1) Импорты (строки 1-18):**
- Расширить импорт из `@mantine/core` (текущая строка 2) до: `Stack, Group, Text, Badge, ActionIcon, Tooltip` — добавлены `ActionIcon`, `Tooltip`.
- Расширить импорт из `@tabler/icons-react` (текущая строка 4) до: `IconFolderOpen, IconMessage, IconEraser` — добавлен `IconEraser` (D-05-08).
- Расширить импорт типов (текущие строки 8-14) — добавить `Usage` в импорт-список: `import type { RunnerEvent, ClaudeEvent, ChatMessage, SessionResult, PanelEvent, Usage } from "../../types/claude";`. (Опционально можно добавить `SessionMetadata` из плана 05-01, но он не обязателен — функционально достаточно трёх раздельных useState; см. PATTERNS.md `src/types/claude.ts` Pattern A — «обе формы acceptable».)

**(2) Новые useState (строки 44-46, рядом с существующими useState):**

Добавить ТРИ новых useState сразу после `const [sessionResult, setSessionResult] = useState<SessionResult | null>(null);` (текущая строка 46):

```
const [sessionModel, setSessionModel] = useState<string | null>(null);
const [sessionId, setSessionId] = useState<string | null>(null);
const [accumulatedUsage, setAccumulatedUsage] = useState<Usage | null>(null);
```

КРИТИЧНО: использовать `useState`, НЕ `useRef`. Эти значения render-driving — изменение должно триггерить ре-рендер StatusBar (PATTERNS.md cross-cutting: «useState vs useRef heuristic»).

НЕ менять существующие state/refs выше (messages, isRunning, sessionResult) и ниже (hasSessionRef, currentBlockIndexRef, toolJsonBuffersRef, archivedMessagesRef, nextMessageIdRef, streamBufferRef, rafIdRef).

**(3) Case 'system' в handleClaudeEvent (текущие строки 137-156) — расширить:**

ДО существующей логики `wasExisting`/`hasSessionRef.current = true`/`info`/`updateMessages` добавить два новых блока setState:

(a) Update sessionId с reset accumulator при смене session_id (D-05-06):

```
setSessionId((prevId) => {
  if (claudeEvent.session_id && claudeEvent.session_id !== prevId) {
    setAccumulatedUsage(null);  // D-05-06 reset на смену session_id
  }
  return claudeEvent.session_id ?? prevId;
});
```

(b) Update sessionModel:

```
if (claudeEvent.model) {
  setSessionModel(claudeEvent.model);
}
```

ВАЖНО: оба эти блока располагаются ВНУТРИ существующего `case "system": {` блока, ДО или ПОСЛЕ `hasSessionRef.current = true;` и существующей `info` логики. Порядок относительно существующего кода case не критичен — важно что existing inline `system-info` message logic (создание `ChatMessage` с `kind: "system-info"`) НЕ изменена.

CRITICAL: НЕ трогать существующий `wasExisting`/`info`/`updateMessages` блок (текущие строки 138-155). Он создаёт inline system-info message и продолжает работать как есть (CONTEXT.md `<code_context>` Integration Points: «оставить как есть, всё ещё полезно для inline-видимости»).

**(4) Case 'assistant' в handleClaudeEvent (текущие строки 251-271) — расширить:**

В НАЧАЛЕ существующего `case "assistant": {` блока (до строки 252 `const textBlocks = ...`) добавить блок accumulator:

```
if (claudeEvent.message.usage) {
  const curr = claudeEvent.message.usage;
  setAccumulatedUsage((prev) => {
    if (prev === null) {
      return { ...curr };  // defensive copy от event payload
    }
    return {
      input_tokens: prev.input_tokens + curr.input_tokens,
      output_tokens: prev.output_tokens + curr.output_tokens,
      cache_creation_input_tokens: prev.cache_creation_input_tokens + curr.cache_creation_input_tokens,
      cache_read_input_tokens: prev.cache_read_input_tokens + curr.cache_read_input_tokens,
    };
  });
}
```

CRITICAL: defensive copy `{ ...curr }` обязателен — `Usage` объект приходит из event payload и теоретически может быть mutated/recycled внутри Tauri bridge.

CRITICAL: НЕ трогать существующий блок textBlocks-filter-map-join (строки 252-256), блок `if (textBlocks) { updateMessages(...) }` (строки 257-267), и финальный `resetStreamingState();` (строка 269) — они работают как есть.

**(5) Case 'result' (текущие строки 273-295) — НЕ ТРОГАТЬ В ЧАСТИ USAGE.**

D-05-04 явно запрещает читать `claudeEvent.usage` (typed `unknown`, double-count risk). Существующий `setSessionResult({...})` остаётся идентичным. Если в этом case есть какой-то код связанный с usage — он должен быть удалён, но в текущем файле его нет (проверено по строкам 273-295).

**(6) Создать useCallback handleClear (D-05-11) — между existing handleStop и handleLoadEarlier (после строки 435):**

```
const handleClear = useCallback(() => {
  setMessages([]);
  setSessionResult(null);
  setSessionModel(null);
  setSessionId(null);
  setAccumulatedUsage(null);
  hasSessionRef.current = false;
  archivedMessagesRef.current = [];
  resetStreamingState();
}, [resetStreamingState]);
```

Зависимости useCallback ровно `[resetStreamingState]` — setState-функции стабильны (React гарантия), hasSessionRef/archivedMessagesRef — refs (стабильные ссылки), resetStreamingState — единственный неstable callback.

CRITICAL: никакого `notifications.show()` или `t('chat.cleared')` внутри (D-05-14 — silent clear).

**(7) Заменить inline /clear в handleSend (текущие строки 396-403):**

Существующий блок:
```
if (text.trim() === "/clear") {
  setMessages([]);
  setSessionResult(null);
  hasSessionRef.current = false;
  archivedMessagesRef.current = [];
  resetStreamingState();
  return;
}
```

Заменить на:
```
if (text.trim() === "/clear") {
  handleClear();
  return;
}
```

CRITICAL: добавить `handleClear` в зависимости `useCallback` handleSend (текущая строка 426). Заменить деpendencies array с `[createMessageId, cwd, mode, panelId, projectModel, projectPermissionMode, resetStreamingState, updateMessages]` на `[createMessageId, cwd, handleClear, mode, panelId, projectModel, projectPermissionMode, updateMessages]` — добавлен `handleClear`, удалён `resetStreamingState` (он больше не используется напрямую в handleSend — только через handleClear). Если ESLint/TS жалуется на missing dep — добавить обратно `resetStreamingState`, но это не должно быть нужно.

**(8) Header Group — добавить Clear-button (текущие строки 460-469):**

После existing `<Badge ...>...</Badge>` (закрывающий тег строка 468) и ДО закрывающего `</Group>` (строка 469) вставить:

```
<Tooltip label={t("chat.clearChat")} withArrow position="bottom">
  <ActionIcon
    ml="auto"
    size="xs"
    variant="subtle"
    onClick={handleClear}
    disabled={isRunning}
    aria-label={t("chat.clearChat")}
  >
    <IconEraser size={14} stroke={1.5} />
  </ActionIcon>
</Tooltip>
```

Конвенции и rationale (D-05-07..10, PATTERNS.md ChatPanel Pattern E):
- `ml="auto"` — прижать к правому краю, отделить от mode-Badge (D-05-07).
- `size="xs" variant="subtle"` — match header-level convention из MessageItem bookmark-button.
- `disabled={isRunning}` — D-05-10 (Clear не работает во время стрима).
- `aria-label={t("chat.clearChat")}` — для accessibility и для unit-теста (getByRole с name).
- `IconEraser size={14} stroke={1.5}` — D-05-08, визуальная плотность совпадает с другими header-иконками.
- `Tooltip position="bottom"` — header StatusBar внизу, tooltip раскрывается вниз чтобы не перекрывать содержимое.

**(9) Прокинуть новые props в <StatusBar/> (текущая строка 520):**

Заменить:
```
<StatusBar isRunning={isRunning} sessionResult={sessionResult} />
```

на:
```
<StatusBar
  isRunning={isRunning}
  sessionResult={sessionResult}
  model={sessionModel ?? projectModel ?? null}
  sessionId={sessionId}
  usage={accumulatedUsage}
/>
```

CRITICAL:
- `model={sessionModel ?? projectModel ?? null}` — fallback резолвится здесь в caller (D-05-12). `projectModel` уже доступен из props ChatPanel (строка 39).
- `sessionId={sessionId}` — передаём state как есть.
- `usage={accumulatedUsage}` — передаём accumulator как есть.

**Чего НЕ делать:**
- НЕ менять useTauriListener registration (строки 336-345) — Phase 4 invariant.
- НЕ менять PromptInput rendering (строки 513-518) — outside scope.
- НЕ менять MessageList rendering (строки 503-511).
- НЕ менять useEffect для claude_status (строки 347-351) и unmount cleanup (353-355).
- НЕ менять обработчики handleSaveMessage (357-390), handleStop (429-435), handleLoadEarlier (437-454).
- НЕ менять renderedMessages slicing (строка 456) — Phase 4 invariant.
- НЕ добавлять никакой ConfirmModal перед Clear (D-05-09).
- НЕ читать `claudeEvent.usage` в case `'result'` (D-05-04).
- НЕ читать `stream_event.message_delta.usage` (D-05-04).
- НЕ менять backend `src-tauri/` или `crates/` — Compatibility v1.0.
- НЕ менять prop-сигнатуру `ChatPanel` (`ChatPanelProps`).
- НЕ менять MAX_MESSAGES_IN_MEMORY / LOAD_EARLIER_BATCH_SIZE / INACTIVE_VISIBLE_MESSAGES константы.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Команда `npm run typecheck` exit 0.
    - Source assertion (state): `grep -cE "const \[sessionModel, setSessionModel\] = useState<string \| null>" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (state): `grep -cE "const \[sessionId, setSessionId\] = useState<string \| null>" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (state): `grep -cE "const \[accumulatedUsage, setAccumulatedUsage\] = useState<Usage \| null>" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (imports): `grep -nE "import type \{[^}]*\bUsage\b[^}]*\}" src/components/chat/ChatPanel.tsx | wc -l` ≥ 1.
    - Source assertion (imports): `grep -cE "IconEraser" src/components/chat/ChatPanel.tsx` ≥ 2 (import + использование).
    - Source assertion (imports): `grep -cE "\bActionIcon\b" src/components/chat/ChatPanel.tsx` ≥ 2 (import + использование).
    - Source assertion (imports): `grep -cE "\bTooltip\b" src/components/chat/ChatPanel.tsx` ≥ 2.
    - Source assertion (system case): `grep -cE "claudeEvent\.session_id !== prevId" src/components/chat/ChatPanel.tsx` ≥ 1 (D-05-06 reset triggered on session_id change).
    - Source assertion (system case): `grep -cE "setAccumulatedUsage\(null\)" src/components/chat/ChatPanel.tsx` ≥ 2 (минимум: в case 'system' для reset на смену session_id + в handleClear).
    - Source assertion (system case): `grep -cE "setSessionModel\(claudeEvent\.model\)" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (assistant case): `grep -cE "claudeEvent\.message\.usage" src/components/chat/ChatPanel.tsx` ≥ 1 (single source of truth per D-05-04).
    - Source assertion (assistant case): `grep -cE "input_tokens:\s*prev\.input_tokens\s*\+\s*curr\.input_tokens" src/components/chat/ChatPanel.tsx` ≥ 1 (формула сложения accumulator).
    - Source assertion (assistant case): `grep -cE "cache_creation_input_tokens:" src/components/chat/ChatPanel.tsx` ≥ 1 (cache_creation отдельно в state per D-05-03).
    - Source assertion (assistant case): `grep -cE "cache_read_input_tokens:" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (handleClear): `grep -cE "const handleClear = useCallback" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (handleClear): для блока handleClear все 5 setState-сбросов представлены — проверка через multiline grep: `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); const m=s.match(/const handleClear = useCallback\(\(\) => \{[\s\S]*?\}, \[/); if(!m)throw new Error('handleClear block not found'); ['setMessages\\(\\[\\]\\)','setSessionResult\\(null\\)','setSessionModel\\(null\\)','setSessionId\\(null\\)','setAccumulatedUsage\\(null\\)','hasSessionRef\\.current = false','archivedMessagesRef\\.current = \\[\\]','resetStreamingState\\(\\)'].forEach(p=>{if(!new RegExp(p).test(m[0]))throw new Error('handleClear missing '+p)}); console.log('handleClear OK')"` выводит `handleClear OK`.
    - Source assertion (handleSend /clear): `grep -cE "if \(text\.trim\(\) === \"/clear\"\) \{[\s]*handleClear\(\);" src/components/chat/ChatPanel.tsx` ≥ 1 ИЛИ через node-script: `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); if(!/text\.trim\(\) === \"\/clear\"[\s\S]{0,80}handleClear\(\)/.test(s))throw new Error('/clear not delegated to handleClear'); console.log('/clear OK')"` выводит `/clear OK`.
    - Source assertion (handleSend /clear): `grep -cE "if \(text\.trim\(\) === \"/clear\"\)" src/components/chat/ChatPanel.tsx` ровно 1 (нет дубликата) И в этом блоке НЕТ inline `setMessages([])` — проверка через node: `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); const m=s.match(/text\.trim\(\) === \"\/clear\"[\s\S]{0,200}\}/); if(m && /setMessages\(\[\]\);[\s\S]*setSessionResult/.test(m[0]))throw new Error('inline /clear still present'); console.log('inline removed')"` выводит `inline removed`.
    - Source assertion (Clear-button): `grep -cE "aria-label=\{t\(\"chat\.clearChat\"\)\}" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (Clear-button): `grep -cE "onClick=\{handleClear\}" src/components/chat/ChatPanel.tsx` ≥ 1.
    - Source assertion (Clear-button): `grep -cE "ml=\"auto\"" src/components/chat/ChatPanel.tsx` ≥ 1 (D-05-07 spatial separation).
    - Source assertion (Clear-button): `grep -cE "disabled=\{isRunning\}" src/components/chat/ChatPanel.tsx` ≥ 1 (D-05-10).
    - Source assertion (StatusBar props): `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); if(!/<StatusBar[\s\S]*?model=\{sessionModel \?\? projectModel \?\? null\}[\s\S]*?sessionId=\{sessionId\}[\s\S]*?usage=\{accumulatedUsage\}[\s\S]*?\/>/.test(s))throw new Error('StatusBar new props not wired'); console.log('StatusBar wired OK')"` выводит `StatusBar wired OK`.
    - Source assertion (regression): `grep -cE "useTauriListener<PanelEvent>" src/components/chat/ChatPanel.tsx` ≥ 1 (Phase 4 invariant — listener registration not broken).
    - Source assertion (regression): `grep -cE "renderedMessages = isActive" src/components/chat/ChatPanel.tsx` ≥ 1 (Phase 4 isActive slicing сохранён).
    - Source assertion (regression result-case): `grep -cE "claudeEvent\.usage" src/components/chat/ChatPanel.tsx | wc -l` возвращает 0 (D-05-04 — result.usage НЕ читается). Альтернативно: проверить отсутствие через `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); const rb=s.match(/case \"result\": \{[\s\S]*?break;/); if(rb && /\.usage/.test(rb[0]))throw new Error('result case reads usage — violates D-05-04'); console.log('result.usage absent')"` выводит `result.usage absent`.
    - Source assertion (regression): `grep -cE "setSessionResult\(\{" src/components/chat/ChatPanel.tsx` ≥ 1 (result-case всё ещё конструирует SessionResult).
    - Source assertion (regression no notification on clear): `node -e "const s=require('fs').readFileSync('src/components/chat/ChatPanel.tsx','utf8'); const hc=s.match(/const handleClear = useCallback\(\(\) => \{[\s\S]*?\}, \[/); if(hc && /notifications\.show/.test(hc[0]))throw new Error('handleClear shows notification — violates D-05-14'); console.log('silent clear OK')"` выводит `silent clear OK`.
    - Source assertion (regression no ConfirmModal): `grep -cE "ConfirmModal" src/components/chat/ChatPanel.tsx` равен 0 (D-05-09).
    - Backend safety (выполнять через Bash tool): `git diff --name-only src-tauri crates packages | wc -l` возвращает 0.
  </acceptance_criteria>
  <done>
    `src/components/chat/ChatPanel.tsx` расширен: 3 новых useState, case 'system' setSessionModel + setSessionId с reset на смену session_id, case 'assistant' accumulator, useCallback handleClear с полным reset, inline /clear делегирован в handleClear, header ActionIcon Clear-button с disabled={isRunning}, прокинуты новые props в <StatusBar/>. Case 'result' не задет в части usage. Backend не задет. Phase 4 invariant сохранён. `npm run typecheck` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Расширить ChatPanel.test.tsx — тесты accumulator (роста и reset), Clear-button (disabled + click), интеграция со StatusBar (новые props)</name>
  <read_first>
    - src/__tests__/ChatPanel.test.tsx (полный файл 272 строки — capturedCallback pattern, существующие 6 тестов)
    - src/components/chat/ChatPanel.tsx (после Task 1 — точная семантика state и handleClear)
    - src/components/chat/StatusBar.tsx (после плана 05-02 — структура DOM для assertions через getByText)
    - src/__tests__/setup.ts (моки react-i18next, @tauri-apps/api/core, listen, notifications)
    - .planning/phases/05-chat-visibility-controls/05-CONTEXT.md (Claude's Discretion раздел Тесты — 4 обязательных кейса; specifics §3 truths #1, #3, #4, #5, #6, #7)
    - .planning/phases/05-chat-visibility-controls/05-PATTERNS.md (раздел ChatPanel.test.tsx Pattern A-D — точные fixtures и assertion patterns)
  </read_first>
  <files>src/__tests__/ChatPanel.test.tsx</files>
  <action>
В `src/__tests__/ChatPanel.test.tsx` добавить новые тесты, не трогая существующие 6 (renders default panelId, renders custom panelId, filters events by panelId, limits messages in memory, cleans streaming buffer, removes listener on unmount).

**(1) Импорты — расширить, если нужно (текущие строки 1-7):**
- Если `Usage` тип нужен в новых тестах для type-annotations — добавить в импорт типов: `import type { PanelEvent, Usage } from "../types/claude";`.
- Никаких новых внешних импортов не нужно — `act`, `render`, `screen`, `MantineProvider`, `invoke`, `listen` уже импортированы.

**(2) Добавить ровно 5 новых `it(...)` блоков в конец существующего `describe("ChatPanel", ...)` (после строки 270, ПЕРЕД закрывающим `});` describe-блока).**

Каждый новый тест использует тот же паттерн capturedCallback что и существующий тест `filters events by panelId` (строки 58-111).

**Тест A: `accumulator grows on assistant event with usage` (CONTEXT.md specifics #5):**

```tsx
it("accumulates usage tokens from assistant events", async () => {
  let capturedCallback: ((event: { payload: PanelEvent }) => void) | null = null;
  mockedListen.mockImplementation((_eventName, handler) => {
    capturedCallback = handler as (event: { payload: PanelEvent }) => void;
    return Promise.resolve(() => {});
  });

  renderWithMantine(<ChatPanel panelId="code" cwd="D:\\test" />);
  await vi.waitFor(() => expect(capturedCallback).not.toBeNull());

  // First assistant event — init accumulator
  act(() => {
    capturedCallback!({
      payload: {
        panel_id: "code",
        event: {
          Claude: {
            type: "assistant",
            session_id: "s1",
            message: {
              role: "assistant",
              content: [{ type: "text", text: "Hi" }],
              usage: {
                input_tokens: 100,
                output_tokens: 50,
                cache_creation_input_tokens: 0,
                cache_read_input_tokens: 200,
              },
            },
          },
        },
      },
    });
  });

  // Second assistant event — accumulate
  act(() => {
    capturedCallback!({
      payload: {
        panel_id: "code",
        event: {
          Claude: {
            type: "assistant",
            session_id: "s1",
            message: {
              role: "assistant",
              content: [{ type: "text", text: "More" }],
              usage: {
                input_tokens: 200,
                output_tokens: 100,
                cache_creation_input_tokens: 0,
                cache_read_input_tokens: 100,
              },
            },
          },
        },
      },
    });
  });

  // Expected sum: 300 + 150 + 0 + 300 = 750. Format via toLocaleString may vary; query as substring of any sum-token rendering.
  await vi.waitFor(() => {
    // StatusBar shows "chat.tokens: 750" (the sum). t mock returns key as-is, so look for "750" near "chat.tokens".
    expect(screen.getByText((content) => /chat\.tokens/.test(content) && /750/.test(content))).toBeInTheDocument();
  });
});
```

**Тест B: `system event with new session_id resets accumulator` (CONTEXT.md specifics #4, D-05-06):**

```tsx
it("resets accumulator when session_id changes via system event", async () => {
  let capturedCallback: ((event: { payload: PanelEvent }) => void) | null = null;
  mockedListen.mockImplementation((_eventName, handler) => {
    capturedCallback = handler as (event: { payload: PanelEvent }) => void;
    return Promise.resolve(() => {});
  });

  renderWithMantine(<ChatPanel panelId="code" cwd="D:\\test" />);
  await vi.waitFor(() => expect(capturedCallback).not.toBeNull());

  // System event with session_id=s1
  act(() => {
    capturedCallback!({
      payload: { panel_id: "code", event: { Claude: { type: "system", subtype: "init", session_id: "s1", tools: [], mcp_servers: [], model: "sonnet" } } },
    });
  });

  // Assistant event — accumulator -> 100
  act(() => {
    capturedCallback!({
      payload: { panel_id: "code", event: { Claude: { type: "assistant", session_id: "s1", message: { role: "assistant", content: [{ type: "text", text: "" }], usage: { input_tokens: 100, output_tokens: 0, cache_creation_input_tokens: 0, cache_read_input_tokens: 0 } } } } },
    });
  });

  await vi.waitFor(() => {
    expect(screen.getByText((c) => /chat\.tokens/.test(c) && /100/.test(c))).toBeInTheDocument();
  });

  // New system event with different session_id=s2 — accumulator должен сброситься
  act(() => {
    capturedCallback!({
      payload: { panel_id: "code", event: { Claude: { type: "system", subtype: "init", session_id: "s2", tools: [], mcp_servers: [], model: "opus" } } },
    });
  });

  // Now accumulator is null → em-dash
  await vi.waitFor(() => {
    expect(screen.getByText((c) => /chat\.tokens/.test(c) && /—/.test(c))).toBeInTheDocument();
  });
});
```

**Тест C: `Clear button is disabled while isRunning` (D-05-10, CONTEXT.md specifics #6):**

Чтобы isRunning стал true, нужно эмулировать invoke('claude_status') возвращающий "running" ИЛИ запустить handleSend (что внутри сетит isRunning). Проще: `mockedInvoke.mockResolvedValueOnce("running");` в начале теста — это заставит первый useEffect `claude_status` вернуть "running" и setIsRunning(true).

```tsx
it("disables Clear button while isRunning", async () => {
  mockedInvoke.mockResolvedValueOnce("running");
  renderWithMantine(<ChatPanel panelId="code" cwd="D:\\test" />);
  await vi.waitFor(() => {
    const btn = screen.getByRole("button", { name: "chat.clearChat" });
    expect(btn).toBeDisabled();
  });
});
```

**Тест D: `clicking Clear resets state` (D-05-09, specifics #7):**

```tsx
it("clicking Clear resets messages, model, sessionId and accumulator", async () => {
  let capturedCallback: ((event: { payload: PanelEvent }) => void) | null = null;
  mockedListen.mockImplementation((_eventName, handler) => {
    capturedCallback = handler as (event: { payload: PanelEvent }) => void;
    return Promise.resolve(() => {});
  });
  mockedInvoke.mockResolvedValue("idle");

  renderWithMantine(<ChatPanel panelId="code" cwd="D:\\test" />);
  await vi.waitFor(() => expect(capturedCallback).not.toBeNull());

  // Set up state: system event + assistant event with usage
  act(() => {
    capturedCallback!({ payload: { panel_id: "code", event: { Claude: { type: "system", subtype: "init", session_id: "s1", tools: [], mcp_servers: [], model: "sonnet" } } } });
    capturedCallback!({ payload: { panel_id: "code", event: { Claude: { type: "assistant", session_id: "s1", message: { role: "assistant", content: [{ type: "text", text: "Hi" }], usage: { input_tokens: 42, output_tokens: 0, cache_creation_input_tokens: 0, cache_read_input_tokens: 0 } } } } } });
  });

  await vi.waitFor(() => {
    expect(screen.getByText((c) => /chat\.model/.test(c) && /sonnet/.test(c))).toBeInTheDocument();
    expect(screen.getByText((c) => /chat\.tokens/.test(c) && /42/.test(c))).toBeInTheDocument();
  });

  // Click Clear
  const clearBtn = screen.getByRole("button", { name: "chat.clearChat" });
  expect(clearBtn).not.toBeDisabled();
  act(() => { clearBtn.click(); });

  // Verify state reset: model em-dash, tokens em-dash, session em-dash
  await vi.waitFor(() => {
    expect(screen.getByText((c) => /chat\.model/.test(c) && /—/.test(c))).toBeInTheDocument();
    expect(screen.getByText((c) => /chat\.tokens/.test(c) && /—/.test(c))).toBeInTheDocument();
    expect(screen.getByText((c) => /chat\.session/.test(c) && /—/.test(c))).toBeInTheDocument();
  });
});
```

**Тест E: `system event populates sessionModel and sessionId` (specifics #1, end-to-end через StatusBar):**

```tsx
it("populates sessionModel and sessionId from system event", async () => {
  let capturedCallback: ((event: { payload: PanelEvent }) => void) | null = null;
  mockedListen.mockImplementation((_eventName, handler) => {
    capturedCallback = handler as (event: { payload: PanelEvent }) => void;
    return Promise.resolve(() => {});
  });

  renderWithMantine(<ChatPanel panelId="code" cwd="D:\\test" />);
  await vi.waitFor(() => expect(capturedCallback).not.toBeNull());

  act(() => {
    capturedCallback!({
      payload: { panel_id: "code", event: { Claude: { type: "system", subtype: "init", session_id: "abc12345-uuid-rest", tools: [], mcp_servers: [], model: "claude-sonnet-4-6" } } },
    });
  });

  await vi.waitFor(() => {
    expect(screen.getByText((c) => /chat\.model/.test(c) && /claude-sonnet-4-6/.test(c))).toBeInTheDocument();
    expect(screen.getByText((c) => /chat\.session/.test(c) && /abc12345/.test(c))).toBeInTheDocument();
  });
});
```

**Что НЕ делать:**
- НЕ переписывать существующие 6 тестов.
- НЕ тестировать через `userEvent` пакет (его в setup нет, используем `act(() => element.click())`).
- НЕ тестировать содержимое Tooltip-breakdown через hover — Mantine Tooltip ленив, для unit-теста достаточно проверить displayed sum.
- НЕ ассертить через `getAllByText` если можно через targeted regex-matcher `(content) => /pattern/.test(content)`.
- НЕ создавать новый describe-блок — добавлять тесты в существующий `describe("ChatPanel", ...)`.
- НЕ менять beforeEach (vi.clearAllMocks, mockedInvoke.mockResolvedValue("idle"), listen mock, requestAnimationFrame stub).
  </action>
  <verify>
    <automated>npm run test -- ChatPanel</automated>
  </verify>
  <acceptance_criteria>
    - Команда `npm run test -- ChatPanel` exit 0 — все тесты (6 существующих + 5 новых) проходят.
    - Source assertion (test count): `grep -cE "^  it\(" src/__tests__/ChatPanel.test.tsx` ≥ 11 (минимум 6 + 5).
    - Source assertion (accumulator test): `grep -cE "accumulates usage tokens" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion (reset test): `grep -cE "resets accumulator when session_id changes" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion (disabled test): `grep -cE "disables Clear button while isRunning" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion (click test): `grep -cE "clicking Clear resets" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion (system test): `grep -cE "populates sessionModel and sessionId" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion: `grep -cE "chat\.clearChat" src/__tests__/ChatPanel.test.tsx` ≥ 2 (минимум: disabled-test getByRole + click-test getByRole).
    - Source assertion: `grep -cE "input_tokens:\s*(100|42|200)" src/__tests__/ChatPanel.test.tsx` ≥ 2 (как минимум 2 fixture с usage).
    - Source assertion: `grep -cE "session_id:\s*\"s[12]\"" src/__tests__/ChatPanel.test.tsx` ≥ 2 (s1 и s2 для reset-теста).
    - Source assertion: `grep -cE "claude-sonnet-4-6" src/__tests__/ChatPanel.test.tsx` ≥ 1 (model fixture в test E).
    - Source assertion: `grep -cE "mockResolvedValueOnce\(\"running\"\)" src/__tests__/ChatPanel.test.tsx` ≥ 1 (running-state setup для disabled-test).
    - Source assertion (regression): `grep -cE "filters events by panelId" src/__tests__/ChatPanel.test.tsx` ≥ 1 (существующий тест сохранён).
    - Source assertion (regression): `grep -cE "limits messages in memory" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Source assertion (regression): `grep -cE "removes listener on unmount" src/__tests__/ChatPanel.test.tsx` ≥ 1.
    - Behavior: `npm run test -- ChatPanel --reporter=verbose 2>&1 | grep -cE "✓"` ≥ 11.
  </acceptance_criteria>
  <done>
    `src/__tests__/ChatPanel.test.tsx` расширен: добавлены 5 новых тестов (accumulator growth, reset on new session_id, Clear disabled при isRunning, Clear click resets state, system event populates model/sessionId). Все 6 существующих тестов не задеты. `npm run test -- ChatPanel` exit 0. Тесты покрывают specifics truths #1, #4, #5, #6, #7 и D-05-04, D-05-06, D-05-09, D-05-10, D-05-11.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` exit 0 — типы корректны после Task 1.
- `npm run test` exit 0 — все тесты ChatPanel + StatusBar + остальные проходят (это уже full test-pass).
- `git diff --name-only` показывает ровно: `src/components/chat/ChatPanel.tsx`, `src/__tests__/ChatPanel.test.tsx`. (Файлы плана 05-02 — отдельный коммит.)
- Backend (`src-tauri/`, `crates/`) НЕ задет — `grep -rn "session_model\|accumulatedUsage\|handleClear" src-tauri crates | wc -l` возвращает 0 (выполнять через Bash tool).
- Вендорированные пакеты (`packages/uni-fw-*`, `crates/uni-*`) НЕ задеты.
- D-05-04: case 'result' не читает usage (отдельный grep-check на claudeEvent.usage = 0).
- D-05-09: ConfirmModal не упоминается в ChatPanel.tsx.
- D-05-14: notifications.show внутри handleClear отсутствует.
- Phase 4 invariant: useTauriListener registration и keep-mounted рендер не сломаны.
- Phase 5 Success Criterion #4 (Clear-button работает) и #5 (no sticky-state) закрыты на уровне поведения + unit-тестов.
</verification>

<success_criteria>
- Все 4 требования Phase 5 (VIS-01, VIS-02, VIS-03, UI-01) функционально закрыты на end-to-end уровне (state → props → render).
- 3 новых useState (sessionModel, sessionId, accumulatedUsage) добавлены и корректно обновляются в case 'system'/'assistant'.
- handleClear выделен как useCallback и используется обоими источниками («/clear» text-команда и UI-кнопка).
- UI-кнопка Clear: ActionIcon icon-only, IconEraser, ml='auto', size='xs', variant='subtle', Tooltip-обёрнутый, disabled={isRunning}, aria-label="chat.clearChat".
- <StatusBar/> получает 3 новых props со значениями из state ChatPanel.
- case 'result' не модифицирован в части usage (D-05-04).
- Никакого ConfirmModal (D-05-09), никакого notification (D-05-14).
- Phase 4 invariant (keep-mounted, useTauriListener) сохранён.
- Тесты: 6 существующих + 5 новых, все проходят.
- `npm run typecheck` и `npm run test` оба exit 0.
</success_criteria>

<output>
Создать `.planning/phases/05-chat-visibility-controls/05-03-SUMMARY.md` со следующими секциями:
- Изменённые блоки в `src/components/chat/ChatPanel.tsx` (imports, state, case 'system', case 'assistant', handleClear, /clear-delegate, header Clear-button, StatusBar props)
- Список новых тестов в `src/__tests__/ChatPanel.test.tsx` с маппингом на specifics truths
- Подтверждение, что case 'result' не модифицирован в части usage (D-05-04)
- Подтверждение, что handleClear silent (D-05-14, без notifications)
- Подтверждение, что ConfirmModal отсутствует (D-05-09)
- Подтверждение, что Phase 4 invariant сохранён (useTauriListener registration не изменена)
- Маппинг покрытых REQ-IDs: VIS-01 (sessionModel state → StatusBar.model), VIS-02 (sessionId state → StatusBar.sessionId), VIS-03 (accumulatedUsage state → StatusBar.usage), UI-01 (Clear-button)
- Маппинг покрытых D-XX: D-05-04..14 (все 11 LOCKED-решений по реализации в этом плане)
- Phase 5 Success Criterion статусы: #1-5 — все закрыты
- Результат `npm run typecheck` и `npm run test`
- Если что-то сломалось при интеграции с планом 05-02 — описать и предложить fix
</output>
