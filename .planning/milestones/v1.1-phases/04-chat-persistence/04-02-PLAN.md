---
phase: 04-chat-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/DualPanelLayout.tsx
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02

must_haves:
  truths:
    - "Интерфейс DualPanelLayoutState и связанные опциональные props initialState / onStateChange полностью удалены из публичного API src/components/DualPanelLayout.tsx."
    - "Внутренний onStateChangeRef и сериализующий state useEffect (текущие строки 66-67 и 113-121) удалены — компонент больше не пытается отдавать наружу свой layoutMode/activePanel/splitPosition/discussTabs/discussActiveTab."
    - "useState инициализаторы (layoutMode, activePanel, splitPosition, discussTabs, discussActiveTab) переключены на статические дефолтные значения — без обращения к initialState."
    - "Никаких других изменений поведения DualPanelLayout (drag-resize, табы, claude-event listener) — только cleanup мёртвых API."
  artifacts:
    - path: "src/components/DualPanelLayout.tsx"
      provides: "Упрощённый DualPanelLayout без устаревшего state-lifting API"
      forbids_grep: "initialState|onStateChange|DualPanelLayoutState"
  key_links:
    - from: "src/App.tsx (после плана 04-01)"
      to: "src/components/DualPanelLayout.tsx"
      via: "props cwd, projectId, projectModel, projectPermissionMode — без initialState / onStateChange"
      pattern: "DualPanelLayoutProps"
---

<objective>
Доделать cleanup, начатый в плане 04-01 (D-04-05): удалить из `src/components/DualPanelLayout.tsx` ставшие мёртвыми внешние props `initialState` / `onStateChange`, экспорт типа `DualPanelLayoutState`, внутренний `onStateChangeRef` и сериализующий `useEffect`, а также упростить `useState` инициализаторы до статических дефолтов.

После плана 04-01 `App.tsx` уже не передаёт эти props и не импортирует тип — `DualPanelLayout` в реальности использует только дефолтные значения. Сейчас мы лишь приводим публичный API компонента в соответствие с этой реальностью, чтобы будущие читатели не пытались дёргать ненужные state-lifting крючки.

Purpose: Закрыть Claude's-discretion решение D-04-05 «убрать projectLayoutState и связанные props полностью» — простая разовая правка одного файла без изменений поведения.

Output: Упрощённый `src/components/DualPanelLayout.tsx`. После плана `npm run typecheck` обязан проходить. Тесты `DualPanelLayout.test.tsx` затрагивают только cwd / projectId props и поведение panels — не должны сломаться этой правкой.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chat-persistence/04-CONTEXT.md
@.planning/phases/04-chat-persistence/04-01-SUMMARY.md
@src/components/DualPanelLayout.tsx
@src/App.tsx
@src/__tests__/DualPanelLayout.test.tsx

<interfaces>
<!-- ДО (src/components/DualPanelLayout.tsx:24-30, 42-49): -->

export interface DualPanelLayoutState {
  layoutMode: LayoutMode;
  activePanel: ActivePanel;
  splitPosition: number;
  discussTabs: TabInfo[];
  discussActiveTab: string;
}

interface DualPanelLayoutProps {
  cwd: string;
  projectId: string;
  projectModel?: string | null;
  projectPermissionMode?: string;
  initialState?: DualPanelLayoutState;
  onStateChange?: (state: DualPanelLayoutState) => void;
}

<!-- ПОСЛЕ (целевое состояние): -->

interface DualPanelLayoutProps {
  cwd: string;
  projectId: string;
  projectModel?: string | null;
  projectPermissionMode?: string;
}

<!-- DualPanelLayoutState полностью удалён -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить устаревший state-lifting API DualPanelLayout (initialState / onStateChange props, DualPanelLayoutState интерфейс, onStateChangeRef, сериализующий useEffect)</name>
  <read_first>
    - src/components/DualPanelLayout.tsx (полный файл — правим интерфейсы, props, useState инициализаторы, удаляем useEffect)
    - src/App.tsx (подтвердить, что 04-01 уже убрал использование initialState/onStateChange — это инвариант перед стартом)
    - src/__tests__/DualPanelLayout.test.tsx (подтвердить, что существующие тесты не передают initialState/onStateChange — они должны продолжать работать без правок)
    - .planning/phases/04-chat-persistence/04-CONTEXT.md (D-04-05, D-04-01)
    - .planning/phases/04-chat-persistence/04-01-SUMMARY.md (для подтверждения, что 04-01 действительно прошёл)
  </read_first>
  <files>src/components/DualPanelLayout.tsx</files>
  <action>
В `src/components/DualPanelLayout.tsx` выполнить cleanup следующих участков:

(1) Удалить полностью `export interface DualPanelLayoutState` (текущие строки 24-30). После этого изменения в файле не должно остаться идентификатора `DualPanelLayoutState`.

(2) Из `interface DualPanelLayoutProps` (текущие строки 42-49) удалить поля `initialState?: DualPanelLayoutState;` и `onStateChange?: (state: DualPanelLayoutState) => void;`. Итоговый интерфейс — ровно 4 поля: `cwd: string; projectId: string; projectModel?: string | null; projectPermissionMode?: string;`.

(3) Из деструктуризации параметров функции `DualPanelLayout` (текущие строки 51-58) удалить `initialState`, `onStateChange`. Итоговая сигнатура: `export function DualPanelLayout({ cwd, projectId, projectModel, projectPermissionMode }: DualPanelLayoutProps) {`.

(4) Заменить инициализаторы useState (текущие строки 60-62) на статические дефолты:
  - `const [layoutMode, setLayoutMode] = useState<LayoutMode>("single");`
  - `const [activePanel, setActivePanel] = useState<ActivePanel>("architect");`
  - `const [splitPosition, setSplitPosition] = useState(50);`

(5) Удалить полностью `const onStateChangeRef = useRef(onStateChange);` и `onStateChangeRef.current = onStateChange;` (текущие строки 66-67).

(6) Заменить инициализатор `useState<TabInfo[]>(() => { ... })` для `discussTabs` (текущие строки 70-77) на статический дефолт без условной ветки `initialState?.discussTabs.length`. Итоговый код:
  ```
  const [discussTabs, setDiscussTabs] = useState<TabInfo[]>(() => {
    const id = nextTabId("discuss");
    return [{ id, label: "Session 1", status: "idle" as TabStatus, mode: "architect" as TabMode }];
  });
  ```

(7) Заменить инициализатор `useState` для `discussActiveTab` (текущие строки 78-80) на `const [discussActiveTab, setDiscussActiveTab] = useState(() => discussTabs[0].id);`. Здесь правомерно, что `discussTabs[0]` всегда определён, так как предыдущий инициализатор гарантирует массив длины 1.

(8) Удалить полностью `useEffect` который сериализует state наружу (текущие строки 113-121). После удаления зависимости `[activePanel, discussActiveTab, discussTabs, layoutMode, splitPosition]` нигде больше не нужны.

ВАЖНО:
- НЕ трогать всю остальную логику: drag-resize (`isDragging`, `handleMouseDown`, useEffect для mousemove/mouseup), claude-event listener (`useTauriListener`), handleAddTab, handleTabModeChange, handleCloseTab, JSX-разметку панелей, SessionTabs.
- НЕ менять импорт `useRef` — он всё ещё нужен для `containerRef`.
- НЕ удалять `useRef` импорт из `react`.
- Type alias `LayoutMode`, `ActivePanel`, `TabMode`, interface `TabInfo`, константа `MAX_TABS_PER_PANEL`, переменная `tabCounter` и функции `nextTabId`, `nextTabLabel` — остаются как есть.
- НЕ трогать `src-tauri/**` и `crates/**`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- DualPanelLayout</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (`tsc --noEmit -p tsconfig.json`) завершается с exit code 0.
    - `npm run test -- DualPanelLayout` (Vitest fileFilter) проходит — все 6 тестов в `DualPanelLayout.test.tsx` зелёные. Если тест падает по причине unrelated (rendering / Mantine), исполнитель восстанавливает падающий ассерт минимально необходимой правкой, оставляя коммент «не относится к Phase 4».
    - Source assertion: `grep -nE "DualPanelLayoutState|initialState|onStateChange|onStateChangeRef" src/components/DualPanelLayout.tsx | grep -v '^#' | wc -l` возвращает 0.
    - Source assertion: `grep -n "useState<LayoutMode>(\"single\")" src/components/DualPanelLayout.tsx | wc -l` ≥ 1 (статический дефолт single).
    - Source assertion: `grep -n "useState<ActivePanel>(\"architect\")" src/components/DualPanelLayout.tsx | wc -l` ≥ 1.
    - Source assertion: `grep -n "useState(50)" src/components/DualPanelLayout.tsx | wc -l` ≥ 1 (статический splitPosition 50).
    - Source assertion: `grep -nE "interface DualPanelLayoutProps" src/components/DualPanelLayout.tsx | wc -l` = 1, при этом тело интерфейса не содержит initialState / onStateChange (повторное проверочное `grep -A 6 "interface DualPanelLayoutProps" src/components/DualPanelLayout.tsx | grep -E "initialState|onStateChange" | wc -l` возвращает 0).
    - Source assertion: `grep -n "useTauriListener" src/components/DualPanelLayout.tsx | wc -l` = 1 (claude-event listener сохранён).
    - Source assertion: `grep -nE "containerRef|isDragging|handleMouseDown|handleAddTab|handleCloseTab" src/components/DualPanelLayout.tsx | wc -l` ≥ 5 (вся остальная логика сохранена).
    - Backend safety: `git diff --name-only` для этого таска показывает только `src/components/DualPanelLayout.tsx`.
  </acceptance_criteria>
  <done>
    `src/components/DualPanelLayout.tsx` упрощён: `DualPanelLayoutState` удалён, props `initialState` / `onStateChange` удалены, `onStateChangeRef` и сериализующий useEffect удалены, useState инициализаторы статические. `npm run typecheck` проходит. `npm run test -- DualPanelLayout` проходит без правок к тестам (существующие тесты не используют удалённые props).
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` exit 0.
- `npm run test -- DualPanelLayout` exit 0 (все 6 тестов).
- `grep -nE "DualPanelLayoutState|initialState|onStateChange|onStateChangeRef" src/components/DualPanelLayout.tsx | wc -l` = 0.
- `git diff --name-only` ограничен `src/components/DualPanelLayout.tsx`.
- Backend (`src-tauri/`, `crates/`) не задет.
</verification>

<success_criteria>
- Публичный API `DualPanelLayout` упрощён до 4 props (cwd, projectId, projectModel, projectPermissionMode).
- Внутренний state-lifting (`onStateChangeRef`, сериализующий useEffect) полностью удалён.
- useState инициализаторы — статические дефолты, без обращения к удалённому `initialState`.
- Compatibility-инвариант с milestone v1.0 (вендорированные `@uni-fw/*` / `uni-*` не модифицируются) соблюдён — мы трогаем только `src/components/DualPanelLayout.tsx`.
- Все существующие тесты `DualPanelLayout.test.tsx` проходят без модификаций.
</success_criteria>

<output>
Создать `.planning/phases/04-chat-persistence/04-02-SUMMARY.md` со следующими секциями:
- Удалённые блоки (DualPanelLayoutState, props, onStateChangeRef, useEffect, условные инициализаторы)
- Подтверждение, что остальная логика (drag-resize, claude-event listener, табы) не задета
- Результат `npm run typecheck`
- Результат `npm run test -- DualPanelLayout` (6/6 зелёных)
- Список файлов в `git diff` (должен содержать только src/components/DualPanelLayout.tsx)
</output>
</content>
</invoke>