---
phase: 02-npm-vendoring
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - .npmrc
  - packages/uni-fw-ui/package.json
  - packages/uni-fw-ssh-ui/package.json
  - packages/uni-fw-terminal-ui/package.json
autonomous: true
requirements: [NPM-04, NPM-05, NPM-06]

must_haves:
  truths:
    - "Корневой `package.json` содержит поле `\"workspaces\": [\"packages/*\"]` (D-10)"
    - "В корневом `package.json` каждая `@uni-fw/*` запись внутри `dependencies` заменена на `\"workspace:*\"` (D-04)"
    - "Файл `.npmrc` удалён из репозитория полностью — не оставлен пустым / закомментированным (D-11)"
    - "Каждый из трёх вендорированных `package.json` объявляет точку входа на TS-исходник через `main`/`types`/`exports` указывающие на `./src/index.ts` (D-01 source-direct, без `dist/`)"
    - "CSS-импорт `@uni-fw/ui/src/styles/markdown.css` из `src/main.tsx:7` продолжает резолвиться через поле `exports."./src/styles/*"` пакета (D-03)"
    - "Транзитивные peerDependencies на @xterm/* пакеты (`@xterm/xterm`, `@xterm/addon-fit`, `@xterm/addon-web-links`) подняты в `dependencies` корневого `package.json` либо подтверждены уже присутствующими"
    - "Никаких сложных npm-конструкций (`overrides`, `peerDependenciesMeta`, `nohoist`) не введено — простая `workspaces` декларация + `workspace:*` ссылки (Established Patterns в 02-PATTERNS.md)"
    - "Импорты `@uni-fw/*` в `src/` НЕ изменены (compatibility-constraint D-12, унаследовано из Phase 1)"
  artifacts:
    - path: "package.json"
      provides: "Обновлённый корневой манифест с workspaces + workspace:* ссылками + явными @xterm peer-deps"
      contains: "\"workspaces\""
    - path: "packages/uni-fw-ui/package.json"
      provides: "Манифест @uni-fw/ui с source-direct entry-point на ./src/index.ts (D-01)"
      contains: "./src/index.ts"
    - path: "packages/uni-fw-ssh-ui/package.json"
      provides: "Манифест @uni-fw/ssh-ui с source-direct entry-point"
      contains: "./src/index.ts"
    - path: "packages/uni-fw-terminal-ui/package.json"
      provides: "Манифест @uni-fw/terminal-ui с source-direct entry-point"
      contains: "./src/index.ts"
  key_links:
    - from: "package.json [workspaces]"
      to: "packages/uni-fw-ui, packages/uni-fw-ssh-ui, packages/uni-fw-terminal-ui"
      via: "Glob `packages/*` подхватывает все три каталога"
      pattern: "packages/\\*"
    - from: "package.json [dependencies]"
      to: "packages/uni-fw-ui (workspace symlink)"
      via: "\"@uni-fw/ui\": \"workspace:*\""
      pattern: "\"@uni-fw/ui\": \"workspace:\\*\""
    - from: "package.json [dependencies]"
      to: "packages/uni-fw-ssh-ui (workspace symlink)"
      via: "\"@uni-fw/ssh-ui\": \"workspace:*\""
      pattern: "\"@uni-fw/ssh-ui\": \"workspace:\\*\""
    - from: "package.json [dependencies]"
      to: "packages/uni-fw-terminal-ui (workspace symlink)"
      via: "\"@uni-fw/terminal-ui\": \"workspace:*\""
      pattern: "\"@uni-fw/terminal-ui\": \"workspace:\\*\""
    - from: "src/main.tsx:7 импорт"
      to: "packages/uni-fw-ui/src/styles/markdown.css"
      via: "exports.\"./src/styles/*\" в манифесте @uni-fw/ui (сохранено из источника)"
      pattern: "./src/styles/\\*"
---

<objective>
Заменить в корневом `package.json` три ссылки `"@uni-fw/*": "^0.1.x"` (=> приватный реестр `npm.ts-vit.com`) на `"workspace:*"` (=> локальный source через npm workspaces), объявить `"workspaces": ["packages/*"]`, явно поднять транзитивные peer-deps на `@xterm/*`, перевести три вендорированных `package.json` на source-direct entry (`./src/index.ts`) и удалить файл `.npmrc` целиком.

После этого ни одна часть конфигурации сборки не должна указывать на `npm.ts-vit.com`. Lockfile регенерируется в Plan 02-03.

Покрывает требования NPM-04 (workspaces декларация), NPM-05 (`@uni-fw/*` → `workspace:*`), NPM-06 (`.npmrc` удалён). Завершает источниковую часть NPM-09 (типы резолвятся через source-direct).

Purpose: Это финальное звено в графе «копия → workspace member → workspace:* зависимость» для устранения SPOF приватного npm-реестра, прямой аналог Plan 01-02 (там путь `git → path` для Cargo, тут `^semver → workspace:*` для npm). Source-direct entry (D-01) необходим, потому что `dist/` в snapshot НЕТ — без переписывания `main`/`exports` ни Vite, ни tsc не найдут модули.

Output: Один отредактированный корневой `package.json` (3 dep-замены + workspaces поле + явные xterm-deps), три отредактированных пакетных `package.json` (entry-point поля), один удалённый `.npmrc`. `package-lock.json` пока НЕ трогаем — его регенерация в Plan 02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-npm-vendoring/02-CONTEXT.md
@.planning/phases/02-npm-vendoring/02-PATTERNS.md

# Phase 1 прецедент — паттерн «replace remote-source с local-source в манифестах»
@.planning/phases/01-rust-vendoring/01-02-rewrite-cargo-manifests-PLAN.md

# Текущее состояние корневых конфигов (читать ПЕРЕД редактированием)
@package.json
@.npmrc

# Manifests, которые этот план переводит на source-direct entry (созданы в Plan 02-01)
@packages/uni-fw-ui/package.json
@packages/uni-fw-ssh-ui/package.json
@packages/uni-fw-terminal-ui/package.json

# Файл, чьи импорты НЕ должны сломаться после source-direct rewrite
@src/main.tsx

<interfaces>
<!-- Текущие записи в корневом package.json, которые этот план меняет.
     Источник: чтение D:\work-ai\uni-claude-code\package.json. -->

Текущие @uni-fw/* dependencies (lines 26-28 в package.json):
  "@uni-fw/ssh-ui": "^0.1.2",
  "@uni-fw/terminal-ui": "^0.1.5",
  "@uni-fw/ui": "^0.1.0",

Целевая форма (D-04):
  "@uni-fw/ssh-ui": "workspace:*",
  "@uni-fw/terminal-ui": "workspace:*",
  "@uni-fw/ui": "workspace:*",

Поле workspaces — новое поле верхнего уровня (D-10):
  "workspaces": ["packages/*"]

Текущие peer-deps вендорированных пакетов (peerDependencies в каждом package.json):

@uni-fw/ui peer-deps (уже все в корневом package.json — НЕ требуют добавления):
  - @mantine/core, @mantine/hooks, @mantine/notifications (есть)
  - @tabler/icons-react (есть)
  - i18next, react-i18next (есть)
  - react, react-dom (есть)

@uni-fw/ui dependencies (НЕ peer — это hard deps пакета):
  - react-markdown, rehype-highlight, remark-gfm — npm workspaces поставит их в packages/uni-fw-ui/node_modules автоматически, в корневой package.json поднимать НЕ нужно

@uni-fw/ssh-ui peer-deps:
  - @mantine/* (есть в корневом), @tabler/icons-react (есть), i18next (есть), react/react-dom (есть), react-i18next (есть)
  - @tauri-apps/api (есть как ^2 в корневом), @tauri-apps/plugin-dialog (есть)
  - @uni-fw/ui — резолвится через sibling workspace symlink, НЕ требует записи в корневом

@uni-fw/terminal-ui peer-deps:
  - @mantine/* (есть), @tabler/icons-react (есть), i18next (есть), react/react-dom (есть), react-i18next (есть)
  - @tauri-apps/api (есть)
  - @xterm/xterm ^6.0.0, @xterm/addon-fit ^0.11.0, @xterm/addon-web-links ^0.12.0 — НЕТ в корневом package.json, но src/main.tsx:8 уже импортирует "@xterm/xterm/css/xterm.css"
    => текущая работа этих импортов в `npm run dev` свидетельствует, что @xterm/* подтягивается транзитивно через старую registry-version @uni-fw/terminal-ui
    => после vendoring и удаления `.npmrc` транзитив сохранится только если @xterm/* останется в `dependencies` пакета `packages/uni-fw-terminal-ui` (так оно и есть в snapshot — peerDependencies секция)
    => npm НЕ устанавливает peerDependencies автоматически. Поэтому эти три пакета НЕОБХОДИМО добавить в `dependencies` корневого package.json явно

Целевые добавления в корневой package.json [dependencies]:
  "@xterm/addon-fit": "^0.11.0",
  "@xterm/addon-web-links": "^0.12.0",
  "@xterm/xterm": "^6.0.0",

Source-direct entry-point rewrite (D-01) для каждого вендорированного package.json:

@uni-fw/ui (текущие поля):
  "main": "dist/index.js"
  "types": "dist/index.d.ts"
  "exports": {
    ".": { "import": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
    "./src/styles/*": "./src/styles/*"
  }

@uni-fw/ui (целевые поля — source-direct):
  "main": "./src/index.ts"
  "types": "./src/index.ts"
  "exports": {
    ".": { "import": { "types": "./src/index.ts", "default": "./src/index.ts" } },
    "./src/styles/*": "./src/styles/*"
  }
  (поле "./src/styles/*" сохраняется без изменений — поддерживает D-03 CSS-импорт)

@uni-fw/ssh-ui (текущие поля):
  "main": "dist/index.js", "types": "dist/index.d.ts"
  "exports": { ".": { "import": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } } }

@uni-fw/ssh-ui (целевые поля — source-direct):
  "main": "./src/index.ts", "types": "./src/index.ts"
  "exports": { ".": { "import": { "types": "./src/index.ts", "default": "./src/index.ts" } } }

@uni-fw/terminal-ui (целевые поля — аналогично @uni-fw/ssh-ui, индекс `./src/index.ts`):
  "main": "./src/index.ts", "types": "./src/index.ts"
  "exports": { ".": { "import": { "types": "./src/index.ts", "default": "./src/index.ts" } } }

ОСТАЛЬНЫЕ поля каждого пакетного package.json НЕ трогать: name, version, private, description, license, author, repository, homepage, keywords, type, files, scripts, peerDependencies, dependencies, devDependencies — всё остаётся как в snapshot (D-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать три пакетных package.json на source-direct entry (./src/index.ts)</name>
  <files>packages/uni-fw-ui/package.json, packages/uni-fw-ssh-ui/package.json, packages/uni-fw-terminal-ui/package.json</files>
  <read_first>
    - D:\work-ai\uni-claude-code\packages\uni-fw-ui\package.json (после Plan 02-01 — текущие поля main=dist/index.js, types=dist/index.d.ts, exports.".".import.{types,default})
    - D:\work-ai\uni-claude-code\packages\uni-fw-ssh-ui\package.json
    - D:\work-ai\uni-claude-code\packages\uni-fw-terminal-ui\package.json
    - D:\work-ai\uni-claude-code\src\main.tsx (строка 7: `import "@uni-fw/ui/src/styles/markdown.css"` — должна продолжать работать после rewrite)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-CONTEXT.md (D-01 source-direct, D-03 CSS-import preservation, Claude's Discretion §1 о форме поля exports)
  </read_first>
  <action>
    Цель — перевести каждый из трёх вендорированных `package.json` на source-direct entry (D-01): Vite/tsc будут резолвить TypeScript-исходники напрямую без build step.

    Для **packages/uni-fw-ui/package.json** заменить ровно три поля (значения слева → справа):
    - `"main": "dist/index.js"` → `"main": "./src/index.ts"`
    - `"types": "dist/index.d.ts"` → `"types": "./src/index.ts"`
    - В блоке `"exports"."."."import"`: заменить `"types": "./dist/index.d.ts"` → `"types": "./src/index.ts"` и `"default": "./dist/index.js"` → `"default": "./src/index.ts"`

    КРИТИЧНО — НЕ трогать поле `"exports"."./src/styles/*": "./src/styles/*"` — оно поддерживает CSS-импорт из `src/main.tsx:7` (D-03).

    Для **packages/uni-fw-ssh-ui/package.json** заменить три поля (как выше):
    - `"main": "dist/index.js"` → `"main": "./src/index.ts"`
    - `"types": "dist/index.d.ts"` → `"types": "./src/index.ts"`
    - В блоке `"exports"."."."import"`: оба поля `"types"` и `"default"` указывают на `./src/index.ts`.

    Для **packages/uni-fw-terminal-ui/package.json** — аналогично.

    Все остальные поля каждого пакетного `package.json` НЕ трогать: `name`, `version`, `private`, `description`, `license`, `author`, `repository` (D-07: метаданные «как есть»), `homepage` (D-07), `keywords`, `type`, `files`, `scripts` (включая «спящий» `prepublishOnly` — D-02), `peerDependencies` (включая `"@uni-fw/ui": "*"` в `uni-fw-ssh-ui` — D-09: НЕ переписывать на `workspace:*` внутри пакета, npm 7+ резолвит peer на sibling в workspace), `dependencies`, `devDependencies` (включая `tsup` — D-02 «спящий» артефакт).

    Использовать Edit-инструмент с точным `old_string`/`new_string` для каждого поля; никаких регулярок/sed-замен.

    Создать атомарный git commit:
    `refactor(02-02): switch vendored @uni-fw/* packages to source-direct entry (./src/index.ts)`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path packages/uni-fw-ui/package.json,packages/uni-fw-ssh-ui/package.json,packages/uni-fw-terminal-ui/package.json -Pattern 'dist/index' | Measure-Object).Count" — должно вернуть 0; powershell -NoProfile -Command "(Select-String -Path packages/uni-fw-ui/package.json,packages/uni-fw-ssh-ui/package.json,packages/uni-fw-terminal-ui/package.json -Pattern '\"\\./src/index\\.ts\"' | Measure-Object).Count" — должно вернуть как минимум 9 (3 пакета × 3 поля: main/types + types/default внутри exports; реально 3 × 4 = 12); powershell -NoProfile -Command "(Select-String -Path packages/uni-fw-ui/package.json -Pattern '\"\\./src/styles/\\*\"' | Measure-Object).Count" — должно вернуть 1 (поле sub-export сохранено)</automated>
  </verify>
  <acceptance_criteria>
    - В `packages/uni-fw-ui/package.json` поле `main` имеет значение `"./src/index.ts"`
    - В `packages/uni-fw-ui/package.json` поле `types` имеет значение `"./src/index.ts"`
    - В `packages/uni-fw-ui/package.json` блок `exports."."` содержит `"types": "./src/index.ts"` и `"default": "./src/index.ts"`
    - В `packages/uni-fw-ui/package.json` блок `exports` сохраняет ключ `"./src/styles/*"` со значением `"./src/styles/*"` (D-03)
    - `packages/uni-fw-ui/package.json` НЕ содержит подстроку `"dist/"` (ни в `main`, ни в `types`, ни в `exports`)
    - `packages/uni-fw-ui/package.json` сохраняет неизменным поле `"name": "@uni-fw/ui"`
    - `packages/uni-fw-ui/package.json` сохраняет неизменными поля `"repository"`, `"homepage"` (D-07: метаданные не правим)
    - `packages/uni-fw-ui/package.json` сохраняет неизменным блок `"peerDependencies"` (включая все @mantine/*, react, i18next записи)
    - В `packages/uni-fw-ssh-ui/package.json` все три поля переписаны (main, types, exports."."): значения `"./src/index.ts"`; подстрока `"dist/"` отсутствует
    - `packages/uni-fw-ssh-ui/package.json` сохраняет в `peerDependencies` строку `"@uni-fw/ui": "*"` (D-09: внутренние peer не переписываются на `workspace:*`, workspace symlink резолвит автоматически)
    - В `packages/uni-fw-terminal-ui/package.json` все три поля переписаны: значения `"./src/index.ts"`; подстрока `"dist/"` отсутствует
    - `packages/uni-fw-terminal-ui/package.json` сохраняет в `peerDependencies` строки на @xterm/* пакеты неизменными
    - Каждый из трёх `package.json` остаётся валидным JSON: `node -e "JSON.parse(require('fs').readFileSync('<path>','utf8'))"` exits 0 для каждого
    - Корневой `package.json` НЕ изменён в этом таске: `git diff --quiet HEAD -- package.json` exits 0
    - `.npmrc` НЕ удалён в этом таске: `Test-Path .npmrc` возвращает True
    - Создан git-коммит с заголовком `refactor(02-02): switch vendored @uni-fw/* packages to source-direct entry (./src/index.ts)`
  </acceptance_criteria>
  <done>Каждый из трёх вендорированных `package.json` указывает entry-point на `./src/index.ts` (main/types/exports), CSS-sub-export `./src/styles/*` в `@uni-fw/ui` сохранён, никаких упоминаний `dist/` в этих манифестах не осталось, остальные поля не тронуты.</done>
</task>

<task type="auto">
  <name>Task 2: Объявить workspaces, переписать @uni-fw/* на workspace:*, поднять xterm peer-deps в корневом package.json</name>
  <files>package.json</files>
  <read_first>
    - D:\work-ai\uni-claude-code\package.json (текущее состояние — `@uni-fw/ssh-ui ^0.1.2`, `@uni-fw/terminal-ui ^0.1.5`, `@uni-fw/ui ^0.1.0` в dependencies; НЕТ поля `workspaces`; НЕТ @xterm/* в dependencies)
    - D:\work-ai\uni-claude-code\packages\uni-fw-terminal-ui\package.json (peerDependencies для @xterm/* — точные версии, которые надо поднять)
    - D:\work-ai\uni-claude-code\src\main.tsx (строка 8: `import "@xterm/xterm/css/xterm.css"` — подтверждение, что @xterm уже потребляется напрямую)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-CONTEXT.md (D-04 workspace:*, D-10 workspaces без overrides, Claude's Discretion §2 о peer-deps списке)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-PATTERNS.md (раздел «Паттерн (a): объявление workspace» и «Паттерн (b): переписать deps на локальный источник»)
  </read_first>
  <action>
    Цель — перевести корневой `package.json` на npm workspaces со ссылками `workspace:*` для всех `@uni-fw/*` записей. Прямой аналог Plan 01-02 (replace `git=...` → `path=...` для Cargo); здесь `^semver` → `workspace:*` для npm.

    Внести в корневой `D:\work-ai\uni-claude-code\package.json` ровно три группы изменений (использовать Edit-инструмент):

    1. **Добавить новое поле верхнего уровня `"workspaces"`** (D-10) сразу после `"type": "module"` и перед `"scripts"`:
       `"workspaces": ["packages/*"]`
       Никаких `nohoist`, `hoist`-overrides — поведение npm по умолчанию (D-10).

    2. **Заменить три записи в `"dependencies"`** (D-04):
       - `"@uni-fw/ssh-ui": "^0.1.2"` → `"@uni-fw/ssh-ui": "workspace:*"`
       - `"@uni-fw/terminal-ui": "^0.1.5"` → `"@uni-fw/terminal-ui": "workspace:*"`
       - `"@uni-fw/ui": "^0.1.0"` → `"@uni-fw/ui": "workspace:*"`

    3. **Добавить три новые записи в `"dependencies"`** (Claude's Discretion §2 — явное поднятие peer-deps пакета `@uni-fw/terminal-ui` на корневой уровень, поскольку npm не устанавливает peerDependencies автоматически, а `src/main.tsx:8` уже импортирует `@xterm/xterm/css/xterm.css` напрямую):
       - `"@xterm/addon-fit": "^0.11.0"`
       - `"@xterm/addon-web-links": "^0.12.0"`
       - `"@xterm/xterm": "^6.0.0"`
       Версии взяты из `packages/uni-fw-terminal-ui/package.json` peerDependencies блока (D-09: версии источника).

    Соблюсти JSON-конвенцию текущего файла:
    - Алфавитный порядок ключей внутри `dependencies` сохранить (`@xterm/*` встанут между `@tauri-apps/plugin-shell` и `i18next`, `@uni-fw/*` остаются на своих местах между `@xterm/*` и `i18next`).
    - Двухпробельный отступ.
    - Никаких других изменений: сохранить блоки `name`, `version`, `private`, `type`, `scripts`, `devDependencies` как есть (включая существующие записи `@mantine/*`, `@tabler/icons-react`, `@tanstack/react-virtual`, `@tauri-apps/*`, `react*`, `i18next`).

    НЕ вводить (D-10, Established Patterns):
    - блок `"overrides"` — не нужен
    - блок `"peerDependenciesMeta"` — не нужен
    - блок `"resolutions"` — не нужен
    - изменения в `scripts` (workspace-aware команды — задача Phase 3, не этой)

    Не запускать `npm install` / `npm ci` в этом таске — это Plan 02-03. На этом таске — только редактирование файла.

    После сохранения убедиться, что JSON валидный: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0.

    Запустить промежуточный gate без install (аналог `cargo metadata --no-deps` из Phase 1, см. 02-PATTERNS.md «Verification gate: metadata-check без полного build»):
    `npm pkg get workspaces` — должно вернуть `["packages/*"]` (валидирует, что поле workspaces читается npm-ом).

    Создать атомарный git commit:
    `refactor(02-02): declare npm workspaces and switch @uni-fw/* to workspace:* in root package.json`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path package.json -Pattern '\"workspaces\"' | Measure-Object).Count" — должно вернуть 1; powershell -NoProfile -Command "(Select-String -Path package.json -Pattern '\"@uni-fw/' | Measure-Object).Count" — должно вернуть 3; powershell -NoProfile -Command "(Select-String -Path package.json -Pattern '\"workspace:\\*\"' | Measure-Object).Count" — должно вернуть 3; powershell -NoProfile -Command "(Select-String -Path package.json -Pattern '\"@xterm/' | Measure-Object).Count" — должно вернуть 3; node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(JSON.stringify(p.workspaces)!=='[\"packages/*\"]')process.exit(1); for(const k of ['@uni-fw/ui','@uni-fw/ssh-ui','@uni-fw/terminal-ui'])if(p.dependencies[k]!=='workspace:*')process.exit(2); for(const k of ['@xterm/xterm','@xterm/addon-fit','@xterm/addon-web-links'])if(!p.dependencies[k])process.exit(3); console.log('ok')" — должно вывести `ok` и exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Корневой `package.json` содержит поле верхнего уровня `"workspaces": ["packages/*"]` (точно один элемент в массиве)
    - В `dependencies` корневого `package.json` поле `"@uni-fw/ui"` имеет значение `"workspace:*"`
    - В `dependencies` корневого `package.json` поле `"@uni-fw/ssh-ui"` имеет значение `"workspace:*"`
    - В `dependencies` корневого `package.json` поле `"@uni-fw/terminal-ui"` имеет значение `"workspace:*"`
    - В `dependencies` корневого `package.json` присутствует поле `"@xterm/xterm"` со значением `"^6.0.0"`
    - В `dependencies` корневого `package.json` присутствует поле `"@xterm/addon-fit"` со значением `"^0.11.0"`
    - В `dependencies` корневого `package.json` присутствует поле `"@xterm/addon-web-links"` со значением `"^0.12.0"`
    - `package.json` НЕ содержит подстроку `"^0.1.0"` рядом с `@uni-fw/ui` (старая версия снята)
    - `package.json` НЕ содержит подстроку `"^0.1.2"` рядом с `@uni-fw/ssh-ui`
    - `package.json` НЕ содержит подстроку `"^0.1.5"` рядом с `@uni-fw/terminal-ui`
    - `package.json` НЕ содержит блок `"overrides"`, `"peerDependenciesMeta"`, `"resolutions"`, `"nohoist"` (D-10 minimal magic)
    - Существующие записи `@mantine/core`, `@mantine/hooks`, `@mantine/notifications`, `@tabler/icons-react`, `@tanstack/react-virtual`, `@tauri-apps/api`, `@tauri-apps/plugin-dialog`, `@tauri-apps/plugin-shell`, `i18next`, `react`, `react-dom`, `react-i18next` присутствуют в `dependencies` без изменений
    - Блок `devDependencies` (10 записей включая `vite`, `vitest`, `typescript`) сохранён без изменений
    - Блок `scripts` (10 записей включая `dev`, `build`, `test:all`) сохранён без изменений
    - JSON валидный: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
    - `npm pkg get workspaces` возвращает строку `["packages/*"]` (одна строка с массивом)
    - `package-lock.json` НЕ изменён в этом таске: `git diff --quiet HEAD -- package-lock.json` exits 0
    - `.npmrc` всё ещё существует и не изменён в этом таске
    - Создан git-коммит с заголовком `refactor(02-02): declare npm workspaces and switch @uni-fw/* to workspace:* in root package.json`
  </acceptance_criteria>
  <done>Корневой `package.json` объявляет workspaces, ссылается на `@uni-fw/*` через `workspace:*`, явно перечисляет три @xterm peer-deps; нет ни одной registry-version ссылки на `@uni-fw/*`; никаких сложных npm-конструкций не введено.</done>
</task>

<task type="auto">
  <name>Task 3: Удалить .npmrc с приватным реестром npm.ts-vit.com</name>
  <files>.npmrc</files>
  <read_first>
    - D:\work-ai\uni-claude-code\.npmrc (текущее содержимое — одна строка `@uni-fw:registry=https://npm.ts-vit.com`, должна быть подтверждена перед удалением)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-CONTEXT.md (D-11 — удалить полностью, не оставлять пустым/закомментированным)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-PATTERNS.md (раздел «`.npmrc` (config-delete)» — zero-tolerance принцип)
  </read_first>
  <action>
    Удалить файл `D:\work-ai\uni-claude-code\.npmrc` целиком (D-11). Не оставлять пустым / закомментированным — приватный реестр `npm.ts-vit.com` после vendoring не нужен, оставлять любую запись = оставлять SPOF.

    Перед удалением убедиться, что текущее содержимое файла — ровно одна строка `@uni-fw:registry=https://npm.ts-vit.com` (никаких других директив `auth`/`_authToken`/`always-auth` там быть не должно). Если в файле обнаружатся иные строки (например, токен), остановиться и сообщить — сначала их надо проверить вручную, прежде чем удалять.

    Использовать `Remove-Item .npmrc -Force` (PowerShell) или `rm .npmrc` (Bash) — но не через Edit/Write пустого файла.

    Также убедиться, что НЕТ других `.npmrc` файлов в репозитории (например, `packages/uni-fw-ui/.npmrc`, `src-tauri/.npmrc`): `git ls-files '**/.npmrc'` возвращает пусто после удаления корневого.

    Создать атомарный git commit:
    `chore(02-02): remove .npmrc with private registry npm.ts-vit.com`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path .npmrc" — должно вернуть False; powershell -NoProfile -Command "(git ls-files '**/.npmrc' | Measure-Object).Count" — должно вернуть 0</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.npmrc` в корне репозитория НЕ существует: `Test-Path .npmrc` возвращает False
    - В индексе git нет ни одного `.npmrc` файла (проверка для случая `packages/uni-fw-*/.npmrc`): `git ls-files '**/.npmrc'` возвращает пусто
    - В файле `package.json` НЕТ поля `"publishConfig"` с приватным registry (defensive — на случай если кто-то пытался дублировать настройку): `(Select-String -Path package.json -Pattern 'npm\\.ts-vit\\.com').Count` возвращает 0
    - `git status` показывает `.npmrc` как deleted (или коммит уже создан и его нет в working tree)
    - Создан git-коммит с заголовком `chore(02-02): remove .npmrc with private registry npm.ts-vit.com`
    - Коммит-payload: ровно одно изменение — удаление `.npmrc`; НЕ затрагивает другие файлы
  </acceptance_criteria>
  <done>Файл `.npmrc` удалён из репозитория полностью; никаких других следов `npm.ts-vit.com` в конфигах нет; изменение закоммичено атомарно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Конфиг репо → npm CLI | `package.json` + `.npmrc` определяют, откуда npm тянет пакеты. После этого плана единственный «удалённый» источник — публичный реестр `registry.npmjs.org` (default), приватный `npm.ts-vit.com` устранён |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | `.npmrc` может содержать `_authToken` | mitigate | Action явно требует verify-step: проверить, что в `.npmrc` ровно одна строка `@uni-fw:registry=https://npm.ts-vit.com` (никаких токенов/auth директив). Если обнаружатся иные директивы — остановиться и проверить вручную перед удалением. После удаления `.npmrc` не остаётся ни на диске, ни в индексе. |
| T-02-04 | Tampering | Подмена `@uni-fw/*` пакетов через приватный реестр | mitigate | После удаления `.npmrc` и переключения на `workspace:*` все `@uni-fw/*` резолвятся ИСКЛЮЧИТЕЛЬНО локально через workspace symlink. npm физически не может скачать другую версию пакета — `workspace:*` это явная привязка к локальному каталогу. Защищает от компрометации приватного реестра. |
| T-02-05 | Tampering | `@xterm/*` теперь явная dependency (не транзитивная) | accept | Добавляем три пакета `@xterm/*` из ПУБЛИЧНОГО `registry.npmjs.org` — supply-chain risk такой же, как у любого публичного npm-пакета. Версии (`^6.0.0`, `^0.11.0`, `^0.12.0`) точно соответствуют peerDependencies из snapshot — то есть тот же код, который уже работал транзитивно. Дополнительной mitigation не требуется на этом этапе; install-gate активируется в Plan 02-03 (ниже T-02-SC). |
| T-02-SC | Tampering | `npm install`/`npm ci` (любые package-manager installs) | mitigate | На этом плане НЕ выполняется ни `npm install`, ни `npm ci` — только редактирование манифестов и удаление файла. Установка пакетов начинается в Plan 02-03. Package Legitimacy Gate активируется в Plan 02-03 ПЕРЕД install-командой. На этом плане gate не требуется. |
</threat_model>

<verification>
- В корневом `package.json` нет ни одной registry-version ссылки `^0.1.x` для `@uni-fw/*` — все три заменены на `workspace:*`
- В корневом `package.json` присутствует поле `"workspaces": ["packages/*"]`
- В корневом `package.json` присутствуют три новых записи `@xterm/*` в `dependencies`
- Каждый из трёх пакетных `package.json` имеет `main`/`types`/`exports` указывающие на `./src/index.ts` (D-01)
- `@uni-fw/ui` сохраняет `exports."./src/styles/*"` (D-03 — поддержка CSS-импорта)
- Файл `.npmrc` физически удалён из репозитория
- Команда `npm pkg get workspaces` (без install) возвращает `["packages/*"]` — workspace декларация валидна
- `package-lock.json` пока НЕ обновлён — это задача Plan 02-03
- Никаких изменений в `src/`, `src-tauri/src/`, `crates/` (compatibility-constraint D-12)
- Три атомарных коммита созданы с конвенциями из 02-PATTERNS.md «Атомарные коммиты per-task»
</verification>

<success_criteria>
Покрытие требований:
- NPM-04: `"workspaces": ["packages/*"]` присутствует в корневом package.json ✓
- NPM-05: Все три `@uni-fw/*` записи в dependencies заменены на `workspace:*` ✓
- NPM-06: Файл `.npmrc` удалён ✓
- (Подготовка к NPM-09): три пакетных `package.json` переведены на source-direct entry — типы будут резолвиться через workspace symlink на TS-источники в Plan 02-03

Конфигурация готова к `npm install` / `npm ci` в Plan 02-03 — никаких остатков приватного реестра, валидная workspaces декларация, корректные entry-point поля.
</success_criteria>

<output>
Создать `.planning/phases/02-npm-vendoring/02-02-SUMMARY.md` по завершении.
</output>
