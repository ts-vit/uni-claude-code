---
phase: 02-npm-vendoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/uni-fw-ui/package.json
  - packages/uni-fw-ui/README.md
  - packages/uni-fw-ui/.npmignore
  - packages/uni-fw-ui/tsconfig.json
  - packages/uni-fw-ui/tsconfig.build.json
  - packages/uni-fw-ui/tsup.config.ts
  - packages/uni-fw-ui/vitest.config.ts
  - packages/uni-fw-ui/src/index.ts
  - packages/uni-fw-ui/src/components/ConfirmModal.tsx
  - packages/uni-fw-ui/src/components/EmptyState.tsx
  - packages/uni-fw-ui/src/components/index.ts
  - packages/uni-fw-ui/src/components/KeyValueEditor.tsx
  - packages/uni-fw-ui/src/components/MarkdownRenderer.tsx
  - packages/uni-fw-ui/src/components/ResizablePanel.tsx
  - packages/uni-fw-ui/src/components/SessionTabs.tsx
  - packages/uni-fw-ui/src/components/StatusBadge.tsx
  - packages/uni-fw-ui/src/components/UniProvider.tsx
  - packages/uni-fw-ui/src/modules/index.ts
  - packages/uni-fw-ui/src/modules/budget/BudgetSettings.tsx
  - packages/uni-fw-ui/src/modules/budget/index.ts
  - packages/uni-fw-ui/src/modules/generation/GenerationSettings.tsx
  - packages/uni-fw-ui/src/modules/generation/index.ts
  - packages/uni-fw-ui/src/modules/interface/InterfaceSettings.tsx
  - packages/uni-fw-ui/src/modules/interface/index.ts
  - packages/uni-fw-ui/src/modules/ollama/OllamaModels.tsx
  - packages/uni-fw-ui/src/modules/ollama/OllamaSettings.tsx
  - packages/uni-fw-ui/src/modules/ollama/index.ts
  - packages/uni-fw-ui/src/modules/ollama/types.ts
  - packages/uni-fw-ui/src/modules/ollama/useOllamaApi.ts
  - packages/uni-fw-ui/src/modules/openrouter/ModelCatalog.tsx
  - packages/uni-fw-ui/src/modules/openrouter/OpenRouterSettings.tsx
  - packages/uni-fw-ui/src/modules/openrouter/index.ts
  - packages/uni-fw-ui/src/modules/openrouter/types.ts
  - packages/uni-fw-ui/src/modules/openrouter/useOpenRouterModels.ts
  - packages/uni-fw-ui/src/modules/terminal/TerminalSettings.tsx
  - packages/uni-fw-ui/src/modules/terminal/index.ts
  - packages/uni-fw-ui/src/modules/web-search/WebSearchSettings.tsx
  - packages/uni-fw-ui/src/modules/web-search/index.ts
  - packages/uni-fw-ui/src/settings/SettingsContext.tsx
  - packages/uni-fw-ui/src/settings/TauriSettingsAdapter.ts
  - packages/uni-fw-ui/src/settings/index.ts
  - packages/uni-fw-ui/src/settings/types.ts
  - packages/uni-fw-ui/src/settings/useSettings.ts
  - packages/uni-fw-ui/src/styles/markdown.css
  - packages/uni-fw-ui/src/theme/brandPalette.ts
  - packages/uni-fw-ui/src/theme/cssResolver.ts
  - packages/uni-fw-ui/src/__tests__/**
  - packages/uni-fw-ssh-ui/package.json
  - packages/uni-fw-ssh-ui/README.md
  - packages/uni-fw-ssh-ui/.npmignore
  - packages/uni-fw-ssh-ui/tsconfig.json
  - packages/uni-fw-ssh-ui/tsconfig.build.json
  - packages/uni-fw-ssh-ui/tsup.config.ts
  - packages/uni-fw-ssh-ui/vitest.config.ts
  - packages/uni-fw-ssh-ui/src/index.ts
  - packages/uni-fw-ssh-ui/src/SshTunnelSettings.tsx
  - packages/uni-fw-ssh-ui/src/types.ts
  - packages/uni-fw-ssh-ui/src/useSshTunnel.ts
  - packages/uni-fw-ssh-ui/src/__tests__/**
  - packages/uni-fw-terminal-ui/package.json
  - packages/uni-fw-terminal-ui/README.md
  - packages/uni-fw-terminal-ui/.npmignore
  - packages/uni-fw-terminal-ui/tsconfig.json
  - packages/uni-fw-terminal-ui/tsconfig.build.json
  - packages/uni-fw-terminal-ui/tsup.config.ts
  - packages/uni-fw-terminal-ui/vitest.config.ts
  - packages/uni-fw-terminal-ui/src/index.ts
  - packages/uni-fw-terminal-ui/src/TerminalPanel.tsx
  - packages/uni-fw-terminal-ui/src/themes.ts
  - packages/uni-fw-terminal-ui/src/types.ts
  - packages/uni-fw-terminal-ui/src/__tests__/**
autonomous: true
requirements: [NPM-01, NPM-02, NPM-03]

must_haves:
  truths:
    - "Каталог `packages/uni-fw-ui/` существует и содержит snapshot-копию `D:\\work-ai\\ai-chat\\packages\\uni-ui\\` побайтно (D-09)"
    - "Каталог `packages/uni-fw-ssh-ui/` существует и содержит snapshot-копию `D:\\work-ai\\ai-chat\\packages\\uni-ssh-ui\\` побайтно (D-09)"
    - "Каталог `packages/uni-fw-terminal-ui/` существует и содержит snapshot-копию `D:\\work-ai\\ai-chat\\packages\\uni-terminal-ui\\` побайтно (D-09)"
    - "Имена пакетов внутри сохранены: `\"name\": \"@uni-fw/ui\"`, `\"name\": \"@uni-fw/ssh-ui\"`, `\"name\": \"@uni-fw/terminal-ui\"` (D-08)"
    - "Метаданные `repository`/`homepage` каждого пакета НЕ изменены — остались с исходными `https://github.com/ts-vit/ai-chat` URL (D-07)"
    - "Структура каталогов зеркалит `crates/uni-*` — по одному явному каталогу на пакет, без вложенных scope-каталогов вида `packages/@uni-fw/ui` (D-08)"
    - "Файлы вне `node_modules/`/`dist/` скопированы целиком: `package.json`, `README.md`, `.npmignore`, `tsconfig.json`, `tsconfig.build.json`, `tsup.config.ts`, `vitest.config.ts`, весь `src/` включая `__tests__/`, `styles/`, `components/`, `modules/` (D-09)"
    - "D-02: `tsup.config.ts`, `tsconfig.build.json` и `prepublishOnly`-скрипт каждого пакета физически присутствуют в `packages/uni-fw-*/` как «спящие» артефакты — корневой сборкой `uni-claude-code` не активируются, оставлены на случай возврата к публикации"
  artifacts:
    - path: "packages/uni-fw-ui/package.json"
      provides: "Манифест вендорированного @uni-fw/ui (snapshot from ai-chat)"
      contains: "\"name\": \"@uni-fw/ui\""
    - path: "packages/uni-fw-ui/src/index.ts"
      provides: "Точка входа пакета @uni-fw/ui (source-direct entry для Plan 02-02)"
    - path: "packages/uni-fw-ui/src/styles/markdown.css"
      provides: "CSS-файл, импортируемый из src/main.tsx:7 через @uni-fw/ui/src/styles/markdown.css (D-03)"
    - path: "packages/uni-fw-ssh-ui/package.json"
      provides: "Манифест вендорированного @uni-fw/ssh-ui"
      contains: "\"name\": \"@uni-fw/ssh-ui\""
    - path: "packages/uni-fw-ssh-ui/src/index.ts"
      provides: "Точка входа пакета @uni-fw/ssh-ui"
    - path: "packages/uni-fw-terminal-ui/package.json"
      provides: "Манифест вендорированного @uni-fw/terminal-ui"
      contains: "\"name\": \"@uni-fw/terminal-ui\""
    - path: "packages/uni-fw-terminal-ui/src/index.ts"
      provides: "Точка входа пакета @uni-fw/terminal-ui"
  key_links:
    - from: "packages/uni-fw-ssh-ui/package.json peerDependencies"
      to: "packages/uni-fw-ui (sibling-пакет в workspace)"
      via: "\"@uni-fw/ui\": \"*\" — workspace npm 7+ резолвит на sibling автоматически"
      pattern: "\"@uni-fw/ui\""
    - from: "src/main.tsx:7"
      to: "packages/uni-fw-ui/src/styles/markdown.css"
      via: "Импорт `@uni-fw/ui/src/styles/markdown.css` — резолвится через workspace symlink"
      pattern: "@uni-fw/ui/src/styles/markdown.css"
---

<objective>
Перенести (snapshot-копией из `D:\work-ai\ai-chat\packages\<pkg>\`) три npm-пакета `@uni-fw/*` в каталоги `packages/uni-fw-*/` текущего репо. Этот план **только копирует** — никаких правок корневого `package.json`, `.npmrc` или внутренних манифестов пакетов на этом шаге. Регистрация workspaces и переключение `dependencies` на `workspace:*` — задача Plan 02-02. Регенерация `package-lock.json` и финальная верификация — задача Plan 02-03.

Покрывает требования NPM-01, NPM-02, NPM-03 (по одному на каждый пакет).

Purpose: Создать локальные источники для будущих `workspace:*` зависимостей, которые в Plan 02-02 заменят `^0.1.x` (=> приватный реестр) ссылки в корневом `package.json`. Без этого шага npm workspaces ссылались бы в пустоту.

Output: 3 каталога `packages/uni-fw-*/` со скопированными исходниками (включая `src/__tests__/`, `tsup.config.ts`, `vitest.config.ts` — даже несмотря на то что они «спящие» под D-02/D-05/D-06). Корневой `package.json` и `.npmrc` пока НЕ модифицированы — это Plan 02-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-npm-vendoring/02-CONTEXT.md
@.planning/phases/02-npm-vendoring/02-PATTERNS.md

# Phase 1 прецедент — паттерн «вкопировать snapshot и зарегистрировать в workspace»
@.planning/phases/01-rust-vendoring/01-01-vendor-uni-crates-PLAN.md

# Source-of-truth манифесты вендорируемых пакетов (читать ПЕРЕД копированием —
# чтобы видеть точную структуру `peerDependencies`, `dependencies`, `exports`)
@D:\work-ai\ai-chat\packages\uni-ui\package.json
@D:\work-ai\ai-chat\packages\uni-ssh-ui\package.json
@D:\work-ai\ai-chat\packages\uni-terminal-ui\package.json

<interfaces>
<!-- Public API of each package (consumer side, src/ uses only these symbols).
     Источник: grep "@uni-fw/" в src/ (21 хит) и анализ src/index.ts каждого пакета.
     Этот список заморожен compatibility-constraint (D-12) — менять API нельзя. -->

@uni-fw/ui (потребляется в 9 файлах src/):
  - UniProvider, TauriSettingsAdapter (src/main.tsx)
  - useSettings (src/App.tsx, src/components/{McpServersPage,SettingsPage,SshStatusIndicator}.tsx, src/__tests__/{App,McpServersPage,SshStatusIndicator}.test.tsx)
  - MarkdownRenderer (src/components/{ClaudeMdEditor,chat/MessageItem,HistoryPage}.tsx)
  - ConfirmModal (src/components/{McpServersPage,HistoryPage,ProjectSidebar}.tsx)
  - "@uni-fw/ui/src/styles/markdown.css" (CSS-импорт через source-путь, src/main.tsx:7)

@uni-fw/ssh-ui (потребляется в 1 файле src/):
  - SshTunnelSettings, useSshTunnel (src/components/SettingsPage.tsx)
  - peerDependencies."@uni-fw/ui": "*" — cross-package peer внутри workspace (резолвится через sibling)

@uni-fw/terminal-ui (потребляется в 1 файле src/):
  - TerminalPanel (src/components/DualPanelLayout.tsx)
  - peerDependencies: @xterm/xterm, @xterm/addon-fit, @xterm/addon-web-links — это будет адресовано в Plan 02-02

Cross-package map внутри вендорируемых пакетов:
  uni-fw-ui          → нет внутренних @uni-fw/* зависимостей (лист графа)
  uni-fw-terminal-ui → нет внутренних @uni-fw/* зависимостей (лист графа)
  uni-fw-ssh-ui      → @uni-fw/ui (peerDependencies, не dependencies — workspace symlink на sibling)
</interfaces>

<source_layout>
<!-- Verified via Get-ChildItem 2026-05-16. Файлы, которые ДОЛЖНЫ скопироваться. -->

D:\work-ai\ai-chat\packages\uni-ui\
  package.json, README.md, .npmignore
  tsconfig.json, tsconfig.build.json, tsup.config.ts, vitest.config.ts
  src/index.ts (1409 bytes)
  src/components/ — 9 файлов: ConfirmModal, EmptyState, KeyValueEditor, MarkdownRenderer, ResizablePanel, SessionTabs, StatusBadge, UniProvider, index.ts
  src/modules/ — 6 модулей: budget, generation, interface, ollama, openrouter, terminal, web-search + index.ts
  src/settings/ — SettingsContext, TauriSettingsAdapter, useSettings, types, index.ts
  src/styles/markdown.css
  src/theme/ — brandPalette.ts, cssResolver.ts
  src/__tests__/ — тестовая папка целиком (D-05: копируется, не запускается)

D:\work-ai\ai-chat\packages\uni-ssh-ui\
  package.json, README.md, .npmignore
  tsconfig.json, tsconfig.build.json, tsup.config.ts, vitest.config.ts
  src/index.ts (152 bytes), SshTunnelSettings.tsx, types.ts, useSshTunnel.ts
  src/__tests__/setup.ts, SshTunnelSettings.test.tsx

D:\work-ai\ai-chat\packages\uni-terminal-ui\
  package.json, README.md, .npmignore
  tsconfig.json, tsconfig.build.json, tsup.config.ts, vitest.config.ts
  src/index.ts (245 bytes), TerminalPanel.tsx, themes.ts, types.ts
  src/__tests__/setup.ts, TerminalPanel.test.tsx

ИСКЛЮЧЕНИЯ: `node_modules/`, `dist/` — этих каталогов в источнике НЕТ согласно проверке;
defensive-флаг для копирования всё равно их исключает.
</source_layout>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Скопировать 3 пакета @uni-fw/* snapshot-ом из ai-chat в packages/uni-fw-*</name>
  <files>packages/uni-fw-ui/, packages/uni-fw-ssh-ui/, packages/uni-fw-terminal-ui/</files>
  <read_first>
    - D:\work-ai\ai-chat\packages\uni-ui\package.json (исходный манифест с `repository`/`homepage`/`peerDependencies`/`exports` — НЕ редактировать)
    - D:\work-ai\ai-chat\packages\uni-ssh-ui\package.json (исходный манифест, peerDependencies."@uni-fw/ui" — оставить как `"*"`)
    - D:\work-ai\ai-chat\packages\uni-terminal-ui\package.json (исходный манифест, peerDependencies на @xterm/* — оставить как есть)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-CONTEXT.md (D-07, D-08, D-09 — что копировать и что НЕ править)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-PATTERNS.md (раздел «PowerShell от Bash-tool — workaround» и образцы verify-команд)
    - D:\work-ai\uni-claude-code\.planning\phases\01-rust-vendoring\01-01-SUMMARY.md (Issues Encountered — workaround для PowerShell `$`-переменных в Bash-обёртке)
  </read_first>
  <action>
    Рекурсивно скопировать (snapshot) содержимое каждого исходного каталога в целевой:

    1. `D:\work-ai\ai-chat\packages\uni-ui\`          → `D:\work-ai\uni-claude-code\packages\uni-fw-ui\`
    2. `D:\work-ai\ai-chat\packages\uni-ssh-ui\`      → `D:\work-ai\uni-claude-code\packages\uni-fw-ssh-ui\`
    3. `D:\work-ai\ai-chat\packages\uni-terminal-ui\` → `D:\work-ai\uni-claude-code\packages\uni-fw-terminal-ui\`

    Имена целевых каталогов отличаются от исходных префиксом `uni-fw-` (D-08): зеркалит `crates/uni-*` (по одному явному каталогу на пакет, без вложенных scope-каталогов вида `packages/@uni-fw/ui`). Имена пакетов внутри (`"name": "@uni-fw/ui"` и т.д.) НЕ меняются — отличие только в названии каталога.

    Копировать ВСЁ содержимое каждого исходного каталога: `package.json`, `README.md`, `.npmignore`, `tsconfig.json`, `tsconfig.build.json`, `tsup.config.ts`, `vitest.config.ts`, весь `src/` (включая `__tests__/`, `styles/`, `components/`, `modules/`, `settings/`, `theme/`, любые index-файлы). Исключить (если присутствует в источнике): `node_modules/`, `dist/`, `.git/`, `coverage/`, `.turbo/` — defensive-флаг, эти каталоги в источнике отсутствуют согласно проверке `Get-ChildItem`, но защита остаётся.

    Использовать на Windows PowerShell `Copy-Item -Recurse -Force` либо аналог в Bash (`cp -R`). Для PowerShell-from-Bash-tool применить workaround из 01-01-SUMMARY.md (Issues Encountered): записать команду во временный файл `.tmp-copy-packages.ps1` и выполнить через `powershell -ExecutionPolicy Bypass -File .tmp-copy-packages.ps1`, затем удалить временный файл (он НЕ должен попасть в коммит).

    НЕ модифицировать содержимое файлов на этом шаге — копирование строго побайтное (D-09). В частности:
    - НЕ править `repository`/`homepage` в `package.json` каждого пакета (D-07) — остаются с исходными `https://github.com/ts-vit/ai-chat` URL.
    - НЕ править `peerDependencies."@uni-fw/ui": "*"` в `packages/uni-fw-ssh-ui/package.json` — workspace npm 7+ резолвит на sibling-пакет автоматически (аналогия с Plan 01-01: `path = "../uni-common"` внутренние ссылки не правились, потому что относительная раскладка совпадает).
    - НЕ менять `main`, `types`, `exports` (поля entry-point) в манифестах пакетов — это будет адресовано в Plan 02-02 (D-01 source-direct rewrite).
    - НЕ удалять `tsup.config.ts`, `tsconfig.build.json`, `prepublishOnly`-скрипты — они оставлены как «спящие» артефакты (D-02), полная копия по принципу snapshot побайтная.

    После копирования (внутри того же таска) создать атомарный git commit:
    `feat(02-01): vendor 3 @uni-fw/* packages from ai-chat as snapshot` — добавить в индекс ровно содержимое `packages/uni-fw-ui/`, `packages/uni-fw-ssh-ui/`, `packages/uni-fw-terminal-ui/`. НЕ коммитить временные `.tmp-copy-*.ps1` файлы и НЕ менять корневой `package.json`/`.npmrc`/`package-lock.json` в этом коммите.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Get-ChildItem packages/uni-fw-ui,packages/uni-fw-ssh-ui,packages/uni-fw-terminal-ui -Filter package.json | Measure-Object | Select-Object -ExpandProperty Count" — должно вернуть 3; powershell -NoProfile -Command "(Test-Path packages/uni-fw-ui/src/index.ts) -and (Test-Path packages/uni-fw-ssh-ui/src/index.ts) -and (Test-Path packages/uni-fw-terminal-ui/src/index.ts)" — должно вернуть True; powershell -NoProfile -Command "(Select-String -Path packages/uni-fw-ui/package.json -Pattern '\"name\": \"@uni-fw/ui\"' | Measure-Object).Count" — должно вернуть 1</automated>
  </verify>
  <acceptance_criteria>
    - Каталог `packages/uni-fw-ui/` существует
    - Файл `packages/uni-fw-ui/package.json` существует и содержит подстроку `"name": "@uni-fw/ui"`
    - Файл `packages/uni-fw-ui/src/index.ts` существует
    - Файл `packages/uni-fw-ui/src/styles/markdown.css` существует (нужен для импорта `src/main.tsx:7`)
    - Каталог `packages/uni-fw-ui/src/components/` содержит файлы `ConfirmModal.tsx`, `MarkdownRenderer.tsx`, `UniProvider.tsx`, `index.ts` (минимум — публичный API из interfaces-блока)
    - Каталог `packages/uni-fw-ui/src/settings/` содержит файлы `useSettings.ts`, `TauriSettingsAdapter.ts`, `SettingsContext.tsx`, `index.ts`
    - Каталог `packages/uni-fw-ssh-ui/` существует
    - Файл `packages/uni-fw-ssh-ui/package.json` существует и содержит подстроку `"name": "@uni-fw/ssh-ui"`
    - Файл `packages/uni-fw-ssh-ui/src/index.ts` существует
    - Файл `packages/uni-fw-ssh-ui/src/SshTunnelSettings.tsx` существует
    - Файл `packages/uni-fw-ssh-ui/src/useSshTunnel.ts` существует
    - В `packages/uni-fw-ssh-ui/package.json` сохранена строка `"@uni-fw/ui": "*"` внутри блока `peerDependencies` (НЕ переписана на `"workspace:*"` на этом шаге — это будет в Plan 02-02 для корневого `package.json`, но peerDeps пакета остаются как есть согласно D-07/snapshot побайтная)
    - Каталог `packages/uni-fw-terminal-ui/` существует
    - Файл `packages/uni-fw-terminal-ui/package.json` существует и содержит подстроку `"name": "@uni-fw/terminal-ui"`
    - Файл `packages/uni-fw-terminal-ui/src/TerminalPanel.tsx` существует
    - Каждый из трёх `package.json` содержит подстроку `"repository"` с URL `https://github.com/ts-vit/ai-chat` (метаданные «как есть», НЕ почищены — D-07)
    - Каждый из трёх каталогов `packages/uni-fw-*/` содержит файлы `tsup.config.ts`, `tsconfig.build.json`, `vitest.config.ts` (D-02, D-06: «спящие» артефакты скопированы)
    - Каждый из трёх каталогов `packages/uni-fw-*/src/__tests__/` существует (D-05: тесты скопированы, но не будут запускаться)
    - Корневой `package.json` НЕ изменён (это задача Plan 02-02): `git diff --quiet -- package.json` exits 0
    - Файл `.npmrc` существует БЕЗ изменений (это задача Plan 02-02): `git diff --quiet -- .npmrc` exits 0
    - Каталог `packages/` НЕ содержит файлов `node_modules/`, `dist/`, `coverage/` ни в одном из трёх подкаталогов: `powershell -NoProfile -Command "(Get-ChildItem packages -Recurse -Force -Directory -Include node_modules,dist,coverage -ErrorAction SilentlyContinue | Measure-Object).Count"` возвращает 0
    - Никаких временных `.tmp-copy-*.ps1` файлов не закоммичено: `git ls-files .tmp-copy-*.ps1` возвращает пусто
    - Создан git-коммит с заголовком `feat(02-01): vendor 3 @uni-fw/* packages from ai-chat as snapshot`, в котором изменены только пути под `packages/`
  </acceptance_criteria>
  <done>3 каталога `packages/uni-fw-*/` существуют, в каждом валидный `package.json` с правильным `"name"`, исходники в `src/` присутствуют, метаданные «как есть», корневые конфиги не тронуты, изменения закоммичены атомарно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Локальная файловая система → репозиторий | Snapshot копирует код из локального пути `D:\work-ai\ai-chat` (внешний по отношению к репо). Доверие к источнику = доверие к локальному клону ai-chat (фиксированная ревизия на момент копирования) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | snapshot из `D:\work-ai\ai-chat\packages\*` | accept | Source — локальный клон под контролем разработчика; snapshot фиксирует known-good состояние на момент копирования. Live-fetch отсутствует (D-09: не клонируем приватный репо во время фазы). После копирования код становится частью репо и проходит обычные git-ревью. |
| T-02-02 | Information Disclosure | `node_modules/`/`coverage/` могут случайно попасть в snapshot | mitigate | Defensive-исключение `node_modules`, `dist`, `coverage`, `.turbo`, `.git` при копировании (хотя в источнике их нет согласно `Get-ChildItem`). Acceptance criterion проверяет отсутствие этих каталогов после копирования. |
| T-02-SC | Tampering | npm package install (любые `npm install` команды) | mitigate | На этом плане НЕ выполняется ни `npm install`, ни `npm ci` — копирование чистого исходного кода. Package Legitimacy Gate не активируется. Установка пакетов начинается только в Plan 02-03. |
</threat_model>

<verification>
- 3 каталога `packages/uni-fw-*/` существуют и содержат `package.json` + `src/index.ts`
- Имена пакетов внутри сохранены: `@uni-fw/ui`, `@uni-fw/ssh-ui`, `@uni-fw/terminal-ui`
- Метаданные `repository`/`homepage` не тронуты (D-07)
- Корневой `package.json` и `.npmrc` НЕ изменены — это задача Plan 02-02
- `package-lock.json` НЕ изменён — это задача Plan 02-03
- В `src/` НЕТ изменений (compatibility-constraint D-12)
- Коммит создан с конвенцией из 02-PATTERNS.md «Атомарные коммиты per-task»
</verification>

<success_criteria>
Покрытие требований:
- NPM-01: `packages/uni-fw-ui/` существует со snapshot-копией и собственным `package.json` ✓
- NPM-02: `packages/uni-fw-ssh-ui/` существует со snapshot-копией и собственным `package.json` ✓
- NPM-03: `packages/uni-fw-terminal-ui/` существует со snapshot-копией и собственным `package.json` ✓

Workspaces ещё НЕ объявлены в корневом `package.json` (это Plan 02-02). `npm ci` пока не запускается. Цель этого плана — только snapshot.
</success_criteria>

<output>
Создать `.planning/phases/02-npm-vendoring/02-01-SUMMARY.md` по завершении.
</output>
