---
phase: 03-build-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [BUILD-06]

must_haves:
  truths:
    - "`CLAUDE.md` строка 53 (свободная часть, ВНЕ GSD-маркеров) переписана — фраза «External Dependencies … come from a private registry» заменена на параграф «Vendored Dependencies», который описывает `uni-*` как path-зависимости workspace в `crates/uni-*` и `@uni-fw/*` как npm workspaces в `packages/uni-fw-*` (D-C1)"
    - "Содержимое CLAUDE.md между парами GSD-маркеров (`<!-- GSD:project-start ... -->` / `<!-- GSD:project-end -->`, `<!-- GSD:stack-start ... -->` / `<!-- GSD:stack-end -->`, и все остальные) **байт-в-байт идентично** состоянию до плана — D-C4 строго запрещает ручные правки между маркерами"
    - "`.planning/codebase/STACK.md` строки в районе 58, 73, 97 переписаны: заголовок UNI Framework (npm) больше не содержит «private registry npm.ts-vit.com», заголовок UNI Framework (Rust) больше не содержит «git github.com/ts-vit/ai-chat branch dev», и `Configuration → Build` пункт про `.npmrc` либо удалён, либо переписан на «`.npmrc` удалён (vendored as npm workspaces)» (D-C1)"
    - "`.planning/codebase/INTEGRATIONS.md` строки в районе 66, 133-134 переписаны: «`uni-ssh` crate (git: …)» → «`uni-ssh` crate (vendored as `crates/uni-ssh` path-dep)»; пункты Environment Configuration про `@uni-fw/* npm packages from private registry` и `UNI Framework Rust crates from github.com/ts-vit/ai-chat` либо удалены, либо переписаны на vendored (D-C1)"
    - "`.planning/codebase/ARCHITECTURE.md` строки в районе 92-96 (Layer 4 «UNI Framework Layer (external)») переписаны: «Location: github.com/ts-vit/ai-chat branch dev (git dependency)» → «Location: `crates/uni-*` (workspace path-зависимости) и `packages/uni-fw-*` (npm workspaces)»; слово `(external)` в заголовке снято (D-C1)"
    - "`.planning/codebase/CONCERNS.md` секция «Supply Chain Risk» (изначально строки 51-69 включая разделитель `---`) **полностью удалена** — НЕ помечена как resolved, не закомментирована, не оставлена с suffix. Заголовок `## Supply Chain Risk` и все три блока («Private npm registry», «Private git repo», «No integrity enforcement») исчезают из файла (D-C2)"
    - "В семи целевых файлах (CLAUDE.md, STACK.md, INTEGRATIONS.md, ARCHITECTURE.md, CONCERNS.md, CONVENTIONS.md, TESTING.md) ровно 0 совпадений по regex `npm\\.ts-vit\\.com`, `ts-vit/ai-chat`, `private registry`, `git dependency` (D-C1, D-C3)"
    - "CONVENTIONS.md и TESTING.md проверены grep'ом — если совпадений по D-C3-паттернам не было, файлы остаются неизменными; если были — отредактированы по тому же принципу что STACK/INTEGRATIONS/ARCHITECTURE (D-C3)"
    - "Между GSD-маркерами в CLAUDE.md НЕ внесено правок — байт-сравнение содержимого внутри пар `<!-- GSD:*-start -->`…`<!-- GSD:*-end -->` до/после плана идентично (D-C4)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Описание зависимостей в свободной части файла (строка ~53) отражает вендорированное состояние"
      contains: "Vendored Dependencies"
    - path: ".planning/codebase/STACK.md"
      provides: "Зачищенный stack-документ — UNI Framework секции описаны как vendored"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Зачищенный integrations-документ — `uni-ssh` и Environment Configuration больше не ссылаются на приватный git/npm"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Layer 4 описан как vendored (`crates/uni-*` + `packages/uni-fw-*`), не как external git dependency"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Supply Chain Risk секция удалена; CONCERNS описывает только актуальные проблемы кодовой базы"
  key_links:
    - from: "CLAUDE.md (свободная часть, ~line 53)"
      to: "`crates/uni-*` + `packages/uni-fw-*`"
      via: "Параграф «Vendored Dependencies»"
      pattern: "Vendored Dependencies|vendored"
    - from: ".planning/codebase/STACK.md UNI Framework секции"
      to: "`crates/uni-*` path-deps + `packages/uni-fw-*` npm workspaces"
      via: "Заголовки и описания пакетов больше не упоминают npm.ts-vit.com / ts-vit/ai-chat"
      pattern: "vendored|workspace path|npm workspace"
    - from: ".planning/codebase/ARCHITECTURE.md Layer 4"
      to: "Внутренние workspace-каталоги"
      via: "Поле `Location:` указывает на `crates/uni-*` и `packages/uni-fw-*`"
      pattern: "crates/uni-|packages/uni-fw-"
---

<objective>
Расширенный BUILD-06: пользователь явно расширил scope от «обновить CLAUDE.md» до **полной ревизии** `.planning/codebase/*`. Цель плана — привести семь документов в соответствие с фактическим (после Phase 1/2) состоянием: `uni-*` крейты живут в `crates/uni-*` как workspace path-зависимости, `@uni-fw/*` пакеты живут в `packages/uni-fw-*` как npm workspaces. Все упоминания приватного реестра `npm.ts-vit.com` и приватного git-источника `github.com/ts-vit/ai-chat` устраняются. Секция «Supply Chain Risk» в CONCERNS.md удаляется полностью (D-C2: не помечается resolved — пользователь явно выбрал удаление). Содержимое CLAUDE.md между парами GSD-маркеров **не трогается** (D-C4).

Покрывает требование **BUILD-06** (REQUIREMENTS.md): CLAUDE.md обновлён — упоминание `@uni-fw/*` и `uni-*` как внешних зависимостей удалено или переписано на «вендорированные внутри репо». В рамках плана scope расширен до семи документов (CLAUDE.md + 6 файлов `.planning/codebase/`), как зафиксировано в `03-CONTEXT.md` D-C1.

Purpose: Документация — единственное место, где после Phase 1/2 остались следы старой архитектуры. После плана любой будущий map-codebase прогон или чтение `CONCERNS.md` будет идти от уже-очищенного состояния, без дубля работы и без ложных upstream-рисков.

Output: Семь файлов, отредактированных строго в соответствии с D-C1, D-C2, D-C3, D-C4. В шести из семи (CLAUDE.md, STACK.md, INTEGRATIONS.md, ARCHITECTURE.md, CONCERNS.md, и в одном из CONVENTIONS/TESTING если grep что-то найдёт) — реальные правки. CONVENTIONS.md и TESTING.md остаются неизменными, если grep по D-C3-паттернам вернул 0 совпадений.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-build-docs/03-CONTEXT.md

# Целевые файлы — каждый редактируется или проверяется этим планом
@CLAUDE.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

# Прецедент по формату «vendored» формулировок — Phase 1/2 VERIFICATION
@.planning/phases/01-rust-vendoring/01-VERIFICATION.md
@.planning/phases/02-npm-vendoring/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать упоминания приватных источников в CLAUDE.md и `.planning/codebase/STACK.md` / `INTEGRATIONS.md` / `ARCHITECTURE.md`</name>
  <files>CLAUDE.md, .planning/codebase/STACK.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - `CLAUDE.md` целиком — для подтверждения, что свободный (вне-маркерный) параграф «External Dependencies» начинается на строке 51 (`### External Dependencies`) и заканчивается строкой 53 (текст про private registry); первый GSD-маркер `<!-- GSD:project-start source:PROJECT.md -->` находится на строке 59 (любая правка между 59 и `<!-- GSD:project-end -->` запрещена D-C4)
    - `.planning/codebase/STACK.md` строки 58 (заголовок «**UNI Framework (Frontend — private registry `npm.ts-vit.com`)**»), 73 («**UNI Framework (Rust — git ... ai-chat ... branch dev)**»), 97 («Private npm registry for `@uni-fw/*`: configured in `.npmrc`...»)
    - `.planning/codebase/INTEGRATIONS.md` строки 66 («**Provider:** `uni-ssh` crate (git: ...)»), 133 («`@uni-fw/*` npm packages from private registry ...»), 134 («UNI Framework Rust crates from `https://github.com/ts-vit/ai-chat` ...»)
    - `.planning/codebase/ARCHITECTURE.md` строки 92-96 («**UNI Framework Layer (external):**» — поля Purpose / Location / Contains / Used by)
    - `.planning/phases/03-build-docs/03-CONTEXT.md` D-C1 (строки 41-45), D-C4 (строка 48), `canonical_refs` (строки 105-114) — задают точный список целевых строк
    - `.planning/phases/01-rust-vendoring/01-VERIFICATION.md` строки 81-91 (Requirements Coverage) — формат описания «vendored as `crates/uni-*` path-dep» уже использовался в Phase 1 verification, переиспользовать формулировку для консистентности
    - `.planning/phases/02-npm-vendoring/02-VERIFICATION.md` строки 92-103 — формат «vendored as `packages/uni-fw-*` npm workspace» из Phase 2
  </read_first>
  <action>
    Отредактировать 4 файла. Для каждого ниже — точные изменения. **НЕЛЬЗЯ** менять содержимое между парами GSD-маркеров в CLAUDE.md (D-C4).

    **(1) `CLAUDE.md`** — строки 51-53 (свободная часть, вне маркеров):
    - Существующий заголовок `### External Dependencies` (строка 51) переименовать в `### Vendored Dependencies`
    - Существующий параграф (строка 53): «UNI Framework packages (`@uni-fw/*` on frontend, `uni-*` Rust crates) come from a private registry (npm: npm.ts-vit.com, git: github.com/ts-vit/ai-chat branch dev).» → переписать на 2-3 строки в стиле:
      - Первая строка: «UNI Framework packages live inside this repository as vendored snapshots.»
      - Вторая строка: «`uni-*` Rust crates: `crates/uni-{common,process,settings,db,ssh,terminal}/` — workspace path-dependencies declared in `Cargo.toml`.»
      - Третья строка: «`@uni-fw/*` npm packages: `packages/uni-fw-{ui,ssh-ui,terminal-ui}/` — npm workspaces declared in root `package.json` `"workspaces"` field.»
      - (опционально) Четвёртая строка про non-resync: «Upstream sync is not maintained — vendored copy is a one-shot snapshot from `D:\\work-ai\\ai-chat`.»

    После правки строка ровно с identifier'ом `private registry` или `github.com/ts-vit/ai-chat` или `npm.ts-vit.com` в файле отсутствует (вне GSD-маркеров; внутри них — не трогаем).

    **(2) `.planning/codebase/STACK.md`** — три точечные правки:
    - Строка ~58 (заголовок UNI Framework для frontend): `**UNI Framework (Frontend — private registry \`npm.ts-vit.com\`):**` → `**UNI Framework (Frontend — vendored npm workspaces in \`packages/uni-fw-*\`):**`
    - Строка ~73 (заголовок UNI Framework для Rust): `**UNI Framework (Rust — git \`https://github.com/ts-vit/ai-chat\` branch \`dev\`):**` → `**UNI Framework (Rust — vendored workspace path-dependencies in \`crates/uni-*\`):**`
    - Строка ~97 (Configuration → Build, пункт про .npmrc): `- Private npm registry for \`@uni-fw/*\`: configured in \`.npmrc\` as \`@uni-fw:registry=https://npm.ts-vit.com\`` → удалить пункт целиком (он описывает удалённый `.npmrc` — больше не актуален) ИЛИ переписать на `- \`@uni-fw/*\` packages resolved as npm workspaces (no \`.npmrc\` required — package source is \`packages/uni-fw-*\` inside repo)`. Любой из двух вариантов приемлем; предпочтительно удаление, чтобы не плодить «historical noise».

    **(3) `.planning/codebase/INTEGRATIONS.md`** — три точечные правки:
    - Строка ~66 («**Provider:** `uni-ssh` crate (git: `https://github.com/ts-vit/ai-chat`, branch `dev`)»): убрать `(git: ..., branch dev)` → `**Provider:** \`uni-ssh\` crate (vendored as \`crates/uni-ssh\` path-dependency)`
    - Строка ~133 («- `@uni-fw/*` npm packages from private registry `https://npm.ts-vit.com` (registry configured in `.npmrc`)»): переписать на `- \`@uni-fw/*\` npm packages vendored as workspace packages in \`packages/uni-fw-*\` (no private registry required)` ИЛИ удалить пункт, если он повторяет уже сказанное на строке 132 (claude CLI остаётся требованием runtime).
    - Строка ~134 («- UNI Framework Rust crates from `https://github.com/ts-vit/ai-chat` (branch `dev`) — requires git access at build time»): переписать на `- UNI Framework Rust crates vendored as workspace path-dependencies in \`crates/uni-*\` (no git access required at build time)` ИЛИ удалить пункт (предпочтительно — он был отдельным «требованием», которое больше не существует).

    **(4) `.planning/codebase/ARCHITECTURE.md`** — правка одного блока (строки ~92-96):
    - Заголовок `**UNI Framework Layer (external):**` → `**UNI Framework Layer (vendored):**` (убрать слово `external`)
    - Поле `Location: \`github.com/ts-vit/ai-chat\` branch \`dev\` (git dependency)` → `Location: \`crates/uni-*\` (workspace path-dependencies) and \`packages/uni-fw-*\` (npm workspaces)`
    - Поля `Purpose:`, `Contains:`, `Used by:` оставить без изменений — они корректны.

    После всех правок выполнить grep-инвариант:
    ```
    grep -lE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md
    ```
    Ожидаемый результат: пустой вывод (0 файлов). Если хотя бы один файл выводится — план провален и требует доработки.

    **Защита GSD-маркеров (D-C4):** перед сохранением CLAUDE.md скопировать содержимое между каждой парой `<!-- GSD:*-start -->`…`<!-- GSD:*-end -->` в memory, выполнить правку, затем убедиться, что содержимое внутри маркеров идентично сохранённому до правки. Если содержимое внутри хотя бы одного блока отличается — это нарушение D-C4, откатить правку и переделать с правильным таргетингом строки.
  </action>
  <verify>
    <automated>grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md | awk -F: '{s+=$2} END{exit (s==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - В четырёх отредактированных файлах 0 совпадений по приватным regex: `grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md` суммарно по всем файлам == 0
    - В CLAUDE.md появилось слово/фраза «Vendored»: `grep -q 'Vendored' CLAUDE.md` exit 0
    - В CLAUDE.md между маркерами `<!-- GSD:project-start source:PROJECT.md -->` и `<!-- GSD:project-end -->` содержимое байт-в-байт идентично состоянию до правки — проверка через `git diff CLAUDE.md` показывает изменения только за пределами строк маркеров (визуальная проверка diff'а; формально: `git diff -U0 CLAUDE.md | grep -E '^[+-]' | grep -vE '^(\+\+\+|---|@@)' | awk 'NR==1{first=$0} END{last=$0}'` — все изменения должны быть в строках вне диапазонов GSD-блоков)
    - Все три пары GSD-маркеров (`project`, `stack`, `conventions`, `architecture`, `skills`, `workflow`, `profile`) присутствуют в CLAUDE.md ровно по одному разу: `grep -c '<!-- GSD:' CLAUDE.md` == 14 (7 start + 7 end)
    - ARCHITECTURE.md Layer 4 заголовок больше не содержит `(external)`: `grep -E 'UNI Framework Layer \(external\)' .planning/codebase/ARCHITECTURE.md` exit 1 (нет матчей)
    - STACK.md больше не содержит подстроку `.npmrc as `: `grep -F '.npmrc as ' .planning/codebase/STACK.md` exit 1 (нет матчей)
    - INTEGRATIONS.md upstream `(git: ...)` parenthetical в Provider больше не присутствует: `grep -E 'Provider:.*\(git:' .planning/codebase/INTEGRATIONS.md` exit 1 (нет матчей)
  </acceptance_criteria>
  <done>
    Четыре файла (CLAUDE.md, STACK.md, INTEGRATIONS.md, ARCHITECTURE.md) отредактированы, все acceptance_criteria выполнены, содержимое между GSD-маркерами в CLAUDE.md сохранено байт-в-байт.
  </done>
</task>

<task type="auto">
  <name>Task 2: Удалить секцию «Supply Chain Risk» из `.planning/codebase/CONCERNS.md` и выполнить grep-аудит CONVENTIONS.md + TESTING.md</name>
  <files>.planning/codebase/CONCERNS.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` строки 49-72 — заголовок `## Supply Chain Risk` на строке 51, три блока внутри (lines 53-57, 59-63, 65-69), разделители `---` на строках 49 и 71. Точный диапазон удаления: от строки 50 (пустая) или 51 (заголовок) включительно по строку 71 (закрывающий `---`) включительно — нужно убрать и разделитель `---` после секции, чтобы не оставить «двойной разделитель» между Security Considerations и Tech Debt.
    - `.planning/codebase/CONVENTIONS.md` целиком — проверить grep'ом наличие D-C3 паттернов
    - `.planning/codebase/TESTING.md` целиком — проверить grep'ом наличие D-C3 паттернов
    - `.planning/phases/03-build-docs/03-CONTEXT.md` D-C2 (строка 46) — фиксирует «удалить полностью», D-C3 (строка 47) — фиксирует «grep'ом и править только если есть совпадения»
    - `.planning/phases/03-build-docs/03-CONTEXT.md` блок `specifics` (строки 153-159) — особенно «удаление вместо mark as resolved» и логика «CONCERNS.md описывает текущее, история живёт в `phases/`»
  </read_first>
  <action>
    **(1) `.planning/codebase/CONCERNS.md` — удалить секцию «Supply Chain Risk» полностью.**

    Точно: убрать строки от заголовка `## Supply Chain Risk` (строка ~51) до закрывающего разделителя `---` (строка ~71) включительно. После удаления файл должен выглядеть так:
    - Строка 49 заканчивает блок «Security Considerations» (последняя рекомендация «Until then, treat SSH MITM resistance as unverified.»)
    - Сразу после идёт разделитель `---` (это разделитель ПЕРЕД Supply Chain Risk; он удаляется вместе с секцией) → должен остаться только ОДИН разделитель `---` между Security Considerations и Tech Debt.
    - Следующая секция — `## Tech Debt` (была на строке 73).

    Конкретная директива: удалить **либо** строки от `## Supply Chain Risk` до `---` перед `## Tech Debt` ВКЛЮЧИТЕЛЬНО, **либо** строки от `---` перед `## Supply Chain Risk` до строки `---` после неё ВКЛЮЧИТЕЛЬНО — главное, чтобы между секциями «Security Considerations» и «Tech Debt» остался ровно один `---` разделитель.

    После удаления выполнить sanity-check: `grep -n '^---$' .planning/codebase/CONCERNS.md` — не должно быть двух подряд идущих строк `---` (без content между ними).

    Запрет: НЕ помечать секцию как resolved, НЕ оставлять её закомментированной, НЕ добавлять заметку «(удалено в Phase 3, см. VERIFICATION.md)» — D-C2 явно требует **полное удаление** (история фиксируется в `phases/`).

    **(2) `.planning/codebase/CONVENTIONS.md` — grep-аудит без правок (default).**

    Выполнить:
    ```
    grep -nE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' .planning/codebase/CONVENTIONS.md
    ```
    Если 0 матчей — файл НЕ трогаем, фиксируем в SUMMARY «CONVENTIONS.md: 0 матчей D-C3-паттернов, без правок». Если матчи есть — отредактировать аналогично Task 1: переписать на «vendored» формулировку или удалить пункт, если он избыточен.

    **(3) `.planning/codebase/TESTING.md` — grep-аудит без правок (default).**

    Выполнить:
    ```
    grep -nE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' .planning/codebase/TESTING.md
    ```
    Если 0 матчей — файл НЕ трогаем, фиксируем в SUMMARY «TESTING.md: 0 матчей D-C3-паттернов, без правок». Если матчи есть — отредактировать аналогично.

    Финальный grep-инвариант для всех семи файлов:
    ```
    grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/TESTING.md
    ```
    Ожидаемый результат: сумма счётчиков = 0 (все файлы возвращают `:0`).
  </action>
  <verify>
    <automated>grep -c 'Supply Chain Risk' .planning/codebase/CONCERNS.md | awk -F: '{exit ($NF==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - В CONCERNS.md 0 совпадений `Supply Chain Risk`: `grep -c 'Supply Chain Risk' .planning/codebase/CONCERNS.md` == 0
    - В CONCERNS.md 0 совпадений с тремя подзаголовками удалённой секции: `grep -cE 'Private npm registry|Private git repo|No integrity enforcement' .planning/codebase/CONCERNS.md` == 0
    - Между секциями `## Security Considerations` и `## Tech Debt` ровно один разделитель `---`: `awk '/^## Security Considerations/,/^## Tech Debt/' .planning/codebase/CONCERNS.md | grep -c '^---$'` == 1
    - В семи целевых файлах суммарно 0 совпадений D-C3-паттернов: `grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/TESTING.md | awk -F: '{s+=$2} END{exit (s==0)?0:1}'` exit 0
    - Если CONVENTIONS.md и TESTING.md изначально не содержали D-C3-паттернов: `git diff --stat HEAD .planning/codebase/CONVENTIONS.md .planning/codebase/TESTING.md` показывает 0 изменённых строк (файлы не тронуты). Если содержали — diff показывает только правки на «vendored» формулировки.
    - CONCERNS.md остаётся валидным markdown (заголовки `##` присутствуют для Security Considerations, Tech Debt, Known Bugs, Performance Hotspots, Fragile Areas, Scaling Limits, Test Coverage Gaps): `grep -cE '^## ' .planning/codebase/CONCERNS.md` == 7
  </acceptance_criteria>
  <done>
    CONCERNS.md больше не содержит секции Supply Chain Risk; CONVENTIONS.md и TESTING.md проверены grep'ом и либо неизменны, либо отредактированы по тому же принципу; финальный grep-инвариант по всем семи файлам возвращает 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/codebase/*` ↔ будущие GSD-агенты (map-codebase, planner, researcher) | Эти документы — каноничный source-of-truth для будущей работы агентов; устаревшие упоминания приватных URL приведут к ложным архитектурным выводам |
| CLAUDE.md (свободная часть) ↔ Claude Code CLI | Claude читает CLAUDE.md как project instructions; устаревший параграф «private registry» провоцирует неправильные подсказки при обсуждении сборки |
| CLAUDE.md (между GSD-маркерами) ↔ `generate-claude-profile` | Содержимое между маркерами генерируется отдельным процессом; ручная правка десинхронизирует файл с source-документом и будет затёрта при следующей регенерации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure / Repudiation | CLAUDE.md (свободная часть) | mitigate | Точный grep-инвариант после правки (0 матчей `npm.ts-vit.com` / `ts-vit/ai-chat` / `private registry` / `git dependency`); ручная проверка содержимого вне GSD-маркеров |
| T-03-04 | Tampering | CLAUDE.md между GSD-маркерами | mitigate | Acceptance включает проверку количества маркеров (==14) и проверку, что diff содержит правки только в строках вне маркер-диапазонов; D-C4 запрещает любые правки внутри маркеров |
| T-03-05 | Information Disclosure (устаревшая информация в CONCERNS.md) | `.planning/codebase/CONCERNS.md` | mitigate | Полное удаление секции Supply Chain Risk (D-C2); CONCERNS.md описывает только актуальные риски, история живёт в `phases/01-rust-vendoring/` и `phases/02-npm-vendoring/` |
</threat_model>

<verification>
- Все acceptance_criteria из Task 1 и Task 2 выполнены.
- Финальный sweep grep-инвариант по семи файлам возвращает 0:
  - `grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|git dependency' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/TESTING.md`
- CLAUDE.md содержит все 14 GSD-маркеров: `grep -c '<!-- GSD:' CLAUDE.md` == 14
- CONCERNS.md больше не содержит «Supply Chain Risk» подзаголовка
- В SUMMARY указано, изменялись ли CONVENTIONS.md / TESTING.md или они остались нетронутыми (для трассировки D-C3-решения)
</verification>

<success_criteria>
Семь документов (CLAUDE.md + 6 файлов `.planning/codebase/`) после плана точно отражают вендорированное состояние репозитория. Любой будущий map-codebase прогон или чтение CONCERNS.md идёт от уже-очищенного baseline, без ложных upstream-рисков. Содержимое CLAUDE.md между GSD-маркерами не тронуто — generate-claude-profile продолжит работать без расхождения с source-документами.
</success_criteria>

<output>
Создать `.planning/phases/03-build-docs/03-02-SUMMARY.md` по шаблону из `$HOME/.claude/get-shit-done/templates/summary.md`. В SUMMARY обязательно указать, были ли реальные правки в CONVENTIONS.md и TESTING.md (D-C3-решение: если изначально 0 матчей, файлы остаются нетронутыми).
</output>
