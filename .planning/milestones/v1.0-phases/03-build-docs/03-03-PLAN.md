---
phase: 03-build-docs
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - .planning/phases/03-build-docs/03-03-TEST-NOTES.md
  - .planning/phases/03-build-docs/03-VERIFICATION.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-05]

must_haves:
  truths:
    - "Создан временный каталог вне `D:\\work-ai\\` (например, `$env:TEMP\\uni-claude-code-verify-<timestamp>` / `%TEMP%\\uni-claude-code-verify-<timestamp>`) и в нём выполнен `git clone --no-local D:\\work-ai\\uni-claude-code <temp>` — флаг `--no-local` гарантирует, что временный клон не разделяет inode/hardlinks с источником (D-V3)"
    - "Все 5 grep-инвариантов из D-V3 вернули 0 совпадений во временном клоне: (i) `package-lock.json` без `npm.ts-vit.com`; (ii) корневой `package.json` без значений `@uni-fw/...` ≠ `workspace:*`; (iii) `Cargo.lock` без `github.com/ts-vit/ai-chat`; (iv) репозиторные `Cargo.toml` без `git = \"https://github.com/ts-vit/ai-chat\"`; (v) файл `.npmrc` отсутствует в корне"
    - "`npm ci` во временном клоне завершился с exit code 0 (BUILD-01 предусловие — без приватной сети к `npm.ts-vit.com`)"
    - "`cargo build --workspace` во временном клоне завершился с exit code 0 (минимум обязателен по Claude's Discretion в `03-CONTEXT.md`)"
    - "`npm run test:all` (typecheck + vitest + cargo test) во временном клоне завершился с exit code 0 (BUILD-03)"
    - "Опционально: `npm run build` во временном клоне завершился с exit code 0 — выполняется, если не уводит план в overrun; пропуск фиксируется явно в TEST-NOTES как «опциональный шаг, ROADMAP success criterion 4 не требует» (BUILD-02 — Claude's Discretion)"
    - "Артефакт `03-03-TEST-NOTES.md` создан внутри плана и фиксирует: путь к временному каталогу, исход 5 grep-инвариантов в виде таблицы, исход 3-4 команд (npm ci / cargo build / npm run test:all / [опционально npm run build]) с exit codes и durations (форма аналогична `02-03-TEST-NOTES.md`)"
    - "Артефакт `03-VERIFICATION.md` создан по формату Phase 2 (YAML frontmatter с `phase`, `phase_name`, `verified`, `status`, `score`) и содержит truths-таблицу с маппингом BUILD-01..BUILD-06 → evidence (D-V4)"
    - "В `03-VERIFICATION.md` явно зафиксировано deviation D-V2: текст ROADMAP success criterion 4 «сеть к npm.ts-vit.com и github.com/ts-vit/ai-chat заблокирована» интерпретирован как «фактическая независимость от приватной сети доказана через 5 grep-инвариантов + полный прогон» — без физической блокировки firewall'ом (D-V1, D-V2)"
    - "Временный каталог удалён или явно помечен в TEST-NOTES как «оставлен для возможного re-run» — никакого мусора в `D:\\work-ai\\`"
  artifacts:
    - path: ".planning/phases/03-build-docs/03-03-TEST-NOTES.md"
      provides: "Live-протокол end-to-end проверки: temp path, 5 grep-инвариантов, exit codes + durations команд"
    - path: ".planning/phases/03-build-docs/03-VERIFICATION.md"
      provides: "Финальный отчёт фазы 3 в формате Phase 2: YAML frontmatter, truths/evidence таблица для BUILD-01..06, секция Deviations с D-V2"
      contains: "BUILD-01"
  key_links:
    - from: "03-03-TEST-NOTES.md (таблица grep-инвариантов)"
      to: "Файлы временного клона `<temp>/package-lock.json`, `<temp>/package.json`, `<temp>/Cargo.lock`, `<temp>/**/Cargo.toml`, `<temp>/.npmrc`"
      via: "Каждая строка таблицы — regex + target file + hit count"
      pattern: "0 hits|0 matches"
    - from: "03-VERIFICATION.md (Requirements Coverage)"
      to: "REQUIREMENTS.md BUILD-01..BUILD-06"
      via: "По строке на каждое требование — какой артефакт / какой exit code / какой grep подтверждает SATISFIED"
      pattern: "BUILD-0[1-6]"
    - from: "03-VERIFICATION.md (Deviations)"
      to: "ROADMAP success criterion 4 + 03-CONTEXT.md D-V1, D-V2"
      via: "Явный параграф «Deviation D-V2: интерпретация буквы success criterion 4 — фактическая независимость через grep-инварианты, без physical firewall block»"
      pattern: "D-V2|deviation|grep-инварианты"
---

<objective>
Финальный план фазы Build & Docs: подтвердить через end-to-end прогон, что **чистый клон репозитория** (без приватного `.npmrc`, без сетевого доступа к `npm.ts-vit.com` и `github.com/ts-vit/ai-chat`) полностью собирается и проходит все тесты. Артефакты — TEST-NOTES внутри плана и финальный `03-VERIFICATION.md` по формату Phase 2.

**Важная оговорка (D-V1, D-V2):** сеть к приватным сервисам **физически не блокируется** (никаких Windows Firewall rules, никаких hosts-заглушек). Доказательная модель — **фактическая независимость**: 5 grep-инвариантов подтверждают, что репозиторий не содержит ссылок на приватные сервисы, после чего полный прогон `npm ci` + `cargo build` + `npm run test:all` подтверждает, что результат фактически работает. Это решение **должно быть явно зафиксировано** в обоих артефактах (`03-03-TEST-NOTES.md` и `03-VERIFICATION.md`) как осознанное отклонение от буквы ROADMAP success criterion 4 (D-V2).

Покрывает требования:
- **BUILD-01** — `npm run dev` запускается из чистого клона без приватной сети. (Замечание: `npm run dev` сам по себе — long-running Tauri процесс, который ждёт input; в плане НЕ запускаем его полностью, но фиксируем как SATISFIED на основании (a) `npm ci` exit 0 — все зависимости установились, (b) `cargo build --workspace` exit 0 — backend компилируется, (c) Phase 2 verification живьём подтвердил `npm run typecheck`/`test`. Этого достаточно: `npm run dev` это просто композитная команда `vite + tauri dev`, которая запускается тем же тулчейном.)
- **BUILD-02** — `npm run build` — опционально (Claude's Discretion). См. ниже.
- **BUILD-03** — `npm run test:all` зелёный во временном клоне.
- **BUILD-05** — end-to-end проверка в `<temp>` каталоге.

Дополнительно: BUILD-04 и BUILD-06 закрываются Wave 1 (планы 03-01 и 03-02); этот план **ссылается** на их артефакты в `03-VERIFICATION.md`, но самих правок не делает.

Purpose: Зафиксировать формальное PASS milestone-цели «чистый клон без сети полностью собирается». Без этого плана фазы Phase 1 и Phase 2 верифицированы по отдельности, но milestone в целом — нет. После плана любой человек может выполнить процедуру из `03-03-TEST-NOTES.md` и воспроизвести результат.

Output: Два файла внутри `.planning/phases/03-build-docs/`:
- `03-03-TEST-NOTES.md` — live-протокол прогона (форма аналогична `02-03-TEST-NOTES.md`)
- `03-VERIFICATION.md` — финальный отчёт фазы (форма аналогична `02-VERIFICATION.md` с YAML frontmatter)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-build-docs/03-CONTEXT.md

# Wave 1 артефакты — должны быть на диске к моменту выполнения этого плана
@README.md
@CLAUDE.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

# Источники истины для grep-инвариантов
@package.json
@package-lock.json
@Cargo.lock
@src-tauri/Cargo.toml
@crates/claude-code-core/Cargo.toml
@src-tauri/tauri.conf.json

# Шаблоны артефактов
@.planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md
@.planning/phases/02-npm-vendoring/02-VERIFICATION.md
@.planning/phases/01-rust-vendoring/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Клонировать репозиторий в temp-каталог и выполнить 5 grep-инвариантов</name>
  <files>.planning/phases/03-build-docs/03-03-TEST-NOTES.md</files>
  <read_first>
    - `.planning/phases/03-build-docs/03-CONTEXT.md` D-V3 (строки 59-72) — точная процедура: temp-каталог вне `D:\\work-ai\\`, `git clone --no-local`, 5 grep-инвариантов, прогон команд
    - `.planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md` — шаблон формата: заголовок, таблица команд с колонками (Group / Test files / Tests passed / failed / skipped) — для TEST-NOTES Phase 3 структура чуть иная (см. action), но формат «жирный заголовок + таблица + комментарий» переиспользуем
    - `package.json` — для grep-инварианта (ii) нужно понимать, какие ключи `@uni-fw/*` ожидаемы и какие значения легитимны (`workspace:*` — единственное легитимное значение)
    - `src-tauri/tauri.conf.json` — для опционального шага `npm run build`: проверить, что `bundle.signing` либо отсутствует, либо null, иначе prod build потребует ключей и упадёт (см. `03-CONTEXT.md` `code_context` строки 146-148)
  </read_first>
  <action>
    Выполнить шаги в указанном порядке. Все команды запускаются в PowerShell (Windows-only, см. CLAUDE.md «Shell: PowerShell»). Команды pipe'аются в файл вывода для последующей записи в TEST-NOTES.

    **Шаг 1 — Создать временный каталог вне `D:\\work-ai\\`:**

    ```
    $ts = Get-Date -UFormat "%Y%m%d-%H%M%S"
    $temp = Join-Path $env:TEMP "uni-claude-code-verify-$ts"
    New-Item -ItemType Directory -Path $temp -Force | Out-Null
    Write-Output $temp
    ```
    Записать полученный путь — он войдёт в заголовок TEST-NOTES.

    **Шаг 2 — Склонировать репозиторий с `--no-local`:**

    ```
    git clone --no-local D:\work-ai\uni-claude-code $temp
    ```
    Флаг `--no-local` критичен (D-V3): без него `git` использует hardlinks между source и target, и временный клон фактически разделяет дисковое состояние с исходным репо. С `--no-local` идёт полная пересылка объектов через transport — клон становится самостоятельным.

    Проверить exit code; если ≠ 0 — план провален, остановиться.

    **Шаг 3 — Прогнать 5 grep-инвариантов (D-V3):**

    Все инварианты выполняются ОТНОСИТЕЛЬНО `$temp` (не относительно `D:\\work-ai\\uni-claude-code\\`). Использовать PowerShell `Select-String` или Bash `grep -c` через WSL — выбор инструмента на усмотрение executor'a; обе формы дают численный hit count.

    Каждый инвариант возвращает hit count, ожидаемый результат — **0 hits**:

    | # | Инвариант | Target | regex |
    |---|-----------|--------|-------|
    | 1 | `package-lock.json` без `npm.ts-vit.com` | `<temp>/package-lock.json` | `npm\.ts-vit\.com` |
    | 2 | Корневой `package.json` не содержит `@uni-fw/...` со значением ≠ `workspace:*` | `<temp>/package.json` | `"@uni-fw/[^"]+"\s*:\s*"(?!workspace:\*")` |
    | 3 | `Cargo.lock` без `github.com/ts-vit/ai-chat` | `<temp>/Cargo.lock` | `github\.com/ts-vit/ai-chat` |
    | 4 | Все репозиторные `Cargo.toml` без `git = "https://github.com/ts-vit/ai-chat"` | `<temp>/Cargo.toml`, `<temp>/src-tauri/Cargo.toml`, `<temp>/crates/*/Cargo.toml` | `git\s*=\s*"https://github\.com/ts-vit/ai-chat"` |
    | 5 | Файл `.npmrc` отсутствует в корне | `<temp>/.npmrc` | (не grep, а `Test-Path` / `test -f` — ожидаемый результат: файл НЕ существует) |

    Замечание для инварианта (4): grep'ить надо именно `Cargo.toml` репозитория. Вендорированные `crates/uni-*/Cargo.toml` сохраняют `repository = "https://github.com/ts-vit/ai-chat"` (это metadata-поле, не зависимость — см. 01-VERIFICATION.md «Anti-Patterns Found» Info-level), поэтому regex для инварианта (4) использует префикс `git\s*=\s*` чтобы отличить **зависимость** от **metadata**. После grep'a — для каждого hit-а (если есть) ручная проверка, что это именно `git = "..."` в `[dependencies]` секции, а не `repository = "..."` в `[package]`. Если все hits — `repository`, инвариант (4) считается PASS (0 hits зависимостей).

    Замечание для инварианта (2): альтернативная форма проверки — выполнить `node -e 'const p=require("./package.json").dependencies; for(const k in p){if(k.startsWith("@uni-fw/")&&p[k]!=="workspace:*"){console.log(k,p[k])}}'` во временном клоне. Если вывод пуст — PASS.

    Записать результат каждого инварианта (hit count или ENOENT-эквивалент для (5)).

    **Шаг 4 — Зафиксировать результаты в `03-03-TEST-NOTES.md`:**

    Создать файл `.planning/phases/03-build-docs/03-03-TEST-NOTES.md` (по аналогии с `02-03-TEST-NOTES.md`) со следующей структурой:

    1) H1 заголовок: `# Phase 03 Plan 03 — Test Notes`
    2) Параграф: `**Дата прогона:**` ISO timestamp, `**Временный каталог:**` `<temp>`, `**Источник:**` `D:\\work-ai\\uni-claude-code` (git HEAD SHA — получить через `git rev-parse HEAD`).
    3) H2 секция `## Deviation Note (D-V1, D-V2)` — короткий параграф: «Сеть к `npm.ts-vit.com` / `github.com/ts-vit/ai-chat` не блокируется физически (D-V1). Фактическая независимость подтверждается через 5 grep-инвариантов ниже и полный прогон сборки. Это интерпретация ROADMAP success criterion 4 (D-V2).»
    4) H2 секция `## Grep-инварианты` — таблица:

       | # | Инвариант | Target | Regex / Check | Hit count | Status |
       |---|-----------|--------|---------------|-----------|--------|
       | 1 | … | `<temp>/package-lock.json` | `npm\\.ts-vit\\.com` | 0 | PASS |
       | … | … | … | … | … | … |

       Все 5 строк, все Status = PASS, все Hit count = 0 (для инварианта 5 «PASS» = файл отсутствует).

       Если хотя бы один инвариант FAIL (hit count > 0) — план провален, идти в гэп-режим (`/gsd:plan-phase --gaps`).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-build-docs/03-03-TEST-NOTES.md &amp;&amp; grep -cE 'PASS' .planning/phases/03-build-docs/03-03-TEST-NOTES.md | awk '{exit ($1>=5)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Временный каталог создан вне `D:\\work-ai\\`: путь в TEST-NOTES начинается на `C:\\Users\\` или `$env:TEMP\\` или `/tmp/` и не содержит `D:\\work-ai\\`
    - В временном клоне HEAD SHA совпадает с HEAD SHA исходного репо: `git -C <temp> rev-parse HEAD` == `git rev-parse HEAD`
    - `03-03-TEST-NOTES.md` существует и содержит таблицу с 5 строками grep-инвариантов
    - Все 5 hit counts == 0 (для инварианта 5 — «file does not exist»): `grep -cE '\| 0 \| PASS \|' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` ≥ 4 (для инвариантов 1-4) + 1 строка для инварианта 5 с PASS-эквивалентом
    - В TEST-NOTES присутствует секция Deviation Note, упоминающая D-V1 и D-V2: `grep -cE 'D-V1|D-V2' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` ≥ 2
    - 0 инвариантов с FAIL: `grep -c 'FAIL' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` == 0
  </acceptance_criteria>
  <done>
    Временный каталог создан, репозиторий склонирован с `--no-local`, 5 grep-инвариантов прогнаны, все 5 вернули 0 hits, `03-03-TEST-NOTES.md` создан с таблицей grep-инвариантов и секцией Deviation Note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Выполнить `npm ci` + `cargo build --workspace` + `npm run test:all` во временном клоне; дописать таблицу команд в TEST-NOTES</name>
  <files>.planning/phases/03-build-docs/03-03-TEST-NOTES.md</files>
  <read_first>
    - `.planning/phases/03-build-docs/03-03-TEST-NOTES.md` — результат Task 1: путь к временному каталогу + grep-инварианты
    - `.planning/phases/03-build-docs/03-CONTEXT.md` D-V3 (строки 67-72) — список команд: `npm ci`, `cargo build --workspace`, `npm run test:all`, опционально `npm run build`
    - `.planning/phases/03-build-docs/03-CONTEXT.md` Claude's Discretion (строки 78-82) — обоснование «BUILD-02 vs ROADMAP criterion 4»: `npm run build` опционален, минимум обязателен `cargo build --workspace`
    - `package.json` `scripts` — посмотреть точный вид `test:all` (последовательность `typecheck && test && test:rust`)
    - `src-tauri/tauri.conf.json` — убедиться, что `bundle.signing` не настроен (если есть `signing` ключ с непустым `certificate`, `npm run build` попросит подпись и упадёт; в этом случае пропустить опциональный шаг и зафиксировать причину в TEST-NOTES)
  </read_first>
  <action>
    Все команды запускаются с `$temp` в качестве working directory (cd `<temp>` или `Push-Location $temp` / `Pop-Location`).

    **Шаг 1 — `npm ci` во временном клоне:**

    ```
    Push-Location $temp
    $sw = [System.Diagnostics.Stopwatch]::StartNew()
    npm ci
    $exit = $LASTEXITCODE
    $sw.Stop()
    Write-Output "exit=$exit duration=$($sw.Elapsed.TotalSeconds)s"
    ```
    Ожидаемый exit code — 0. Записать exit + duration. Ожидаемый результат — около 490 пакетов установлено, `@uni-fw/*` появляются как symlinks (см. 02-VERIFICATION.md строки 30-31).

    **Шаг 2 — `cargo build --workspace` во временном клоне:**

    ```
    $sw = [System.Diagnostics.Stopwatch]::StartNew()
    cargo build --workspace
    $exit = $LASTEXITCODE
    $sw.Stop()
    Write-Output "exit=$exit duration=$($sw.Elapsed.TotalSeconds)s"
    ```
    Ожидаемый exit code — 0. Длительность — несколько минут на первом запуске (компиляция всех `uni-*` крейтов + Tauri backend + `claude-code-core`), приемлемо.

    **Шаг 3 — `npm run test:all` во временном клоне:**

    ```
    $sw = [System.Diagnostics.Stopwatch]::StartNew()
    npm run test:all
    $exit = $LASTEXITCODE
    $sw.Stop()
    Write-Output "exit=$exit duration=$($sw.Elapsed.TotalSeconds)s"
    ```
    Ожидаемый exit code — 0. Это композитная команда: `tsc --noEmit` + `vitest run` + `cargo test --workspace`. Длительность зависит от cargo-кэша (если cargo build только что прошёл, `cargo test --workspace` переиспользует артефакты и завершится быстрее).

    **Шаг 4 (опциональный) — `npm run build` во временном клоне:**

    Решение: запускать ли — на усмотрение executor'a (см. 03-CONTEXT.md Claude's Discretion). Условия пропуска:
    - В `<temp>/src-tauri/tauri.conf.json` есть ненулевой `bundle.signing` — пропустить (build потребует ключи).
    - Время выполнения Task 1+2 уже превышает ~10 минут — пропустить (overrun-риск).
    - Если ни одно из условий не выполнено — запустить `npm run build`, зафиксировать exit + duration.

    Если пропустили — в TEST-NOTES явно зафиксировать: «Шаг `npm run build` пропущен: <причина>. ROADMAP success criterion 4 не требует `npm run build`; BUILD-02 покрывается косвенно через `cargo build --workspace` (Rust backend компилируется) + `npm run test:all` (frontend typecheck зелёный) — full Tauri bundle deferred до отдельной валидации.»

    **Шаг 5 — Дописать таблицу команд в `03-03-TEST-NOTES.md`:**

    Добавить H2 секцию `## Команды end-to-end прогона` с таблицей:

    | # | Команда | cwd | Exit code | Duration | Резюме |
    |---|---------|-----|-----------|----------|--------|
    | 1 | `npm ci` | `<temp>` | 0 | <N>s | <X> packages installed, `@uni-fw/*` symlinks |
    | 2 | `cargo build --workspace` | `<temp>` | 0 | <N>s | `Finished dev profile`, all 8 workspace members compiled |
    | 3 | `npm run test:all` | `<temp>` | 0 | <N>s | typecheck OK + vitest <X>/<Y> tests passed + cargo test <X>/<Y> tests passed |
    | 4 (опц.) | `npm run build` | `<temp>` | 0 / skipped | <N>s | <причина пропуска или путь к bundle> |

    Также добавить параграф с «head of stdout/stderr» — для `npm run test:all` зафиксировать суммарные числа vitest (`<N> files / <M> tests passed`) и cargo test (`<X> passed; <Y> ignored`).

    Финальная секция TEST-NOTES — `## Резюме`: одна строка, например «Чистый клон без приватной сети полностью собирается; все DoD-команды exit 0; deviation D-V2 зафиксирован выше».

    **Шаг 6 — Очистка:**

    После всех команд (если executor желает) выполнить `Pop-Location` и `Remove-Item -Recurse -Force $temp`. Либо оставить временный каталог для возможного re-run и зафиксировать его путь в TEST-NOTES финальным параграфом «Временный каталог сохранён по адресу `<temp>` для возможного re-run; удалить вручную при необходимости».
  </action>
  <verify>
    <automated>grep -cE '^\| [0-9] \| `?(npm ci|cargo build|npm run test:all)' .planning/phases/03-build-docs/03-03-TEST-NOTES.md | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - В `03-03-TEST-NOTES.md` присутствует таблица команд с ≥ 3 обязательными строками (`npm ci`, `cargo build --workspace`, `npm run test:all`): `grep -cE '^\| [0-9] \| `?(npm ci|cargo build|npm run test:all)' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` ≥ 3
    - Каждая обязательная команда имеет exit code 0 в таблице: ручная проверка ячеек «Exit code» — все три «0»
    - Если `npm run build` запускался — exit code 0; если пропущен — в TEST-NOTES есть параграф с причиной пропуска: `grep -cE 'npm run build.*(skipped|пропущ)' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` ≥ 1 (если пропущен)
    - Резюме `npm run test:all` фиксирует числа vitest (например `19 files / 106 tests`) и cargo test (например `97 passed / 1 ignored`): `grep -cE 'vitest|tests passed|cargo' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` ≥ 2
    - Финальная секция «Резюме» присутствует: `grep -q '^## Резюме' .planning/phases/03-build-docs/03-03-TEST-NOTES.md` или эквивалент (`## Итог`, `## Summary`)
  </acceptance_criteria>
  <done>
    Все DoD-команды (npm ci + cargo build --workspace + npm run test:all) выполнены во временном клоне с exit code 0; TEST-NOTES дополнен таблицей команд + резюме; временный каталог либо удалён, либо его путь зафиксирован для возможного re-run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать `03-VERIFICATION.md` — финальный отчёт фазы 3 по формату Phase 2</name>
  <files>.planning/phases/03-build-docs/03-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/02-npm-vendoring/02-VERIFICATION.md` целиком — образец формата: YAML frontmatter (строки 1-7), H1 + контекст, секция «Goal Achievement» с Observable Truths / Required Artifacts / Key Link Verification / Requirements Coverage / Anti-Patterns Found / Compatibility Verification / Human Verification Required / Gaps Summary, финальный «## Итог»
    - `.planning/phases/01-rust-vendoring/01-VERIFICATION.md` — альтернативный образец (без YAML frontmatter) — НЕ использовать, ориентир — Phase 2 (D-V4 явно требует Phase 2 формат)
    - `.planning/phases/03-build-docs/03-03-TEST-NOTES.md` — артефакт Task 1+2 этого плана, основной evidence-источник
    - `.planning/ROADMAP.md` строки 51-59 — Phase 3 Goal + 4 Success Criteria — каждый Success Criterion должен быть отражён в truths-таблице
    - `.planning/REQUIREMENTS.md` строки 38-43 — BUILD-01..BUILD-06, все 6 должны быть в Requirements Coverage таблице со status SATISFIED
    - `README.md`, `CLAUDE.md`, `.planning/codebase/*` (отредактированные планами 03-01 и 03-02) — evidence для BUILD-04 и BUILD-06
    - `.planning/phases/03-build-docs/03-CONTEXT.md` D-V2 (строки 57-58) — точный текст deviation, который должен попасть в `03-VERIFICATION.md`
  </read_first>
  <action>
    Создать файл `.planning/phases/03-build-docs/03-VERIFICATION.md` со следующей структурой (по образцу Phase 2 `02-VERIFICATION.md`):

    **(1) YAML frontmatter (строки 1-7):**
    ```yaml
    ---
    phase: 03-build-docs
    verified: <ISO timestamp прогона>
    status: passed
    score: <N>/<N> must-haves verified
    overrides_applied: 0
    ---
    ```
    Поле `score` заполнить по факту: число VERIFIED truths / общее число truths.

    **(2) H1 + контекст (после frontmatter):**
    - `# Phase 03: Build & Docs — Отчёт верификации`
    - Параграф: `**Цель фазы (из ROADMAP.md):**` + полный текст phase goal
    - `**Verified:**` ISO timestamp, `**Verdict:** **PASS**`, `**Re-verification:** No — initial verification`

    **(3) Секция `## Goal Achievement`:**

    Подсекция `### Observable Truths` — таблица:

    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | `npm ci` во временном клоне без `.npmrc` завершается exit 0 (ROADMAP SC#1, BUILD-01 предусловие) | ✓ VERIFIED | `03-03-TEST-NOTES.md` секция «Команды end-to-end прогона» строка 1: `npm ci` exit 0, `@uni-fw/*` symlinks |
    | 2 | `cargo build --workspace` во временном клоне завершается exit 0 (ROADMAP SC#1, BUILD-02 минимум) | ✓ VERIFIED | `03-03-TEST-NOTES.md` строка 2 |
    | 3 | `npm run test:all` (typecheck + vitest + cargo test) завершается exit 0 (ROADMAP SC#2, BUILD-03) | ✓ VERIFIED | `03-03-TEST-NOTES.md` строка 3; vitest <N>/<M> passed, cargo test <X>/<Y> passed |
    | 4 | `README.md` существует, на русском, без упоминания приватного реестра / git-источника (ROADMAP SC#3, BUILD-04) | ✓ VERIFIED | `grep -E 'npm\\.ts-vit\\.com\|ts-vit/ai-chat\|private registry' README.md` → 0 матчей; H1 содержит `UNI Claude Code`; длина 35-80 строк |
    | 5 | `CLAUDE.md` обновлён — параграф «External Dependencies» переписан на «Vendored Dependencies» (ROADMAP SC#3, BUILD-06) | ✓ VERIFIED | `grep -c 'Vendored' CLAUDE.md` ≥ 1; `grep -E 'npm\\.ts-vit\\.com\|ts-vit/ai-chat' CLAUDE.md` → 0 матчей вне GSD-маркеров |
    | 6 | `.planning/codebase/*` зачищены: STACK, INTEGRATIONS, ARCHITECTURE переписаны на vendored; CONCERNS Supply Chain Risk секция удалена (BUILD-06 расширенный scope, D-C1/D-C2) | ✓ VERIFIED | `grep -cE 'npm\\.ts-vit\\.com\|ts-vit/ai-chat\|private registry\|git dependency' .planning/codebase/{STACK,INTEGRATIONS,ARCHITECTURE,CONCERNS,CONVENTIONS,TESTING}.md` суммарно 0; `grep -c 'Supply Chain Risk' .planning/codebase/CONCERNS.md` == 0 |
    | 7 | End-to-end в `<temp>` каталоге выполнен с `git clone --no-local` (ROADMAP SC#4 modulo D-V2, BUILD-05) | ✓ VERIFIED | `03-03-TEST-NOTES.md` заголовок: `<temp>` путь, HEAD SHA совпадает с источником |
    | 8 | Все 5 grep-инвариантов в `<temp>` вернули 0 hits (BUILD-05 — D-V3) | ✓ VERIFIED | `03-03-TEST-NOTES.md` таблица grep-инвариантов: 5/5 PASS |
    | 9 | Содержимое CLAUDE.md между GSD-маркерами не модифицировано (D-C4) | ✓ VERIFIED | `grep -c '<!-- GSD:' CLAUDE.md` == 14 (7 start + 7 end); diff CLAUDE.md показывает правки только в строках вне маркер-диапазонов |
    | 10 (опц.) | `npm run build` во временном клоне завершается exit 0 (BUILD-02 полный) | ✓ VERIFIED / ⊘ DEFERRED | Зависит от того, запускался ли (см. Claude's Discretion). Если запускался — exit 0; если пропущен — DEFERRED с причиной в TEST-NOTES |

    Под таблицей: `**Score:** <N>/<N> truths verified.` (если truth 10 DEFERRED — формула `9/9` + явная заметка про опциональный шаг).

    Подсекция `### Required Artifacts` — таблица с одной строкой на каждый ожидаемый артефакт фазы:

    | Artifact | Expected | Status | Details |
    |----------|----------|--------|---------|
    | `README.md` | Top-level README на русском, 35-80 строк, без приватных URL, 7 npm-команд + Tauri prerequisites link | ✓ VERIFIED | … |
    | `CLAUDE.md` (свободная часть) | Параграф «Vendored Dependencies» вместо «External Dependencies» | ✓ VERIFIED | … |
    | `.planning/codebase/STACK.md` | UNI Framework секции описаны как vendored | ✓ VERIFIED | … |
    | `.planning/codebase/INTEGRATIONS.md` | `uni-ssh` provider и Environment Configuration без приватных URL | ✓ VERIFIED | … |
    | `.planning/codebase/ARCHITECTURE.md` | Layer 4 без `(external)` и без `github.com/ts-vit/ai-chat` | ✓ VERIFIED | … |
    | `.planning/codebase/CONCERNS.md` | Секция «Supply Chain Risk» удалена | ✓ VERIFIED | … |
    | `.planning/phases/03-build-docs/03-03-TEST-NOTES.md` | Live-протокол с temp path, grep-инвариантами, exit codes | ✓ VERIFIED | … |

    Подсекция `### Requirements Coverage` — таблица BUILD-01..BUILD-06:

    | Requirement | Source Plan | Description | Status | Evidence |
    |-------------|-------------|-------------|--------|----------|
    | BUILD-01 | 03-03-PLAN | `npm run dev` запускается из чистого клона без приватной сети | ✓ SATISFIED | `npm ci` exit 0 + `cargo build --workspace` exit 0 + Phase 2 verification (`npm run typecheck` + `npm run test` зелёные); `npm run dev` это композитная `vite + tauri dev` команда, которая использует тот же тулчейн |
    | BUILD-02 | 03-03-PLAN | `npm run build` (Tauri prod) проходит из чистого клона | ✓ SATISFIED / ⊘ PARTIAL | Если `npm run build` запускался — SATISFIED; если пропущен — PARTIAL: `cargo build --workspace` подтверждает компиляцию backend, `npm run test:all` подтверждает frontend; полный Tauri bundle deferred (см. Deviations) |
    | BUILD-03 | 03-03-PLAN | `npm run test:all` зелёный | ✓ SATISFIED | `npm run test:all` exit 0 во временном клоне; `03-03-TEST-NOTES.md` фиксирует числа |
    | BUILD-04 | 03-01-PLAN | README создан, без приватных URL | ✓ SATISFIED | `README.md` существует; 0 матчей по приватным regex |
    | BUILD-05 | 03-03-PLAN | End-to-end проверка в чистом клоне | ✓ SATISFIED | `<temp>` каталог + `git clone --no-local` + 5 grep-инвариантов 0 hits + 3 команды exit 0; deviation D-V2 зафиксирован |
    | BUILD-06 | 03-02-PLAN | CLAUDE.md обновлён, `@uni-fw/*` и `uni-*` описаны как вендорированные (+ расширенный scope `.planning/codebase/*` per D-C1/D-C2) | ✓ SATISFIED | CLAUDE.md «Vendored Dependencies»; 0 матчей в 6 файлах `.planning/codebase/`; CONCERNS «Supply Chain Risk» удалён |

    Подсекция `### Anti-Patterns Found`:
    - Сканированы файлы, изменённые/созданные в фазе 3: `README.md`, `CLAUDE.md`, `.planning/codebase/{STACK,INTEGRATIONS,ARCHITECTURE,CONCERNS,CONVENTIONS,TESTING}.md`, `.planning/phases/03-build-docs/03-03-TEST-NOTES.md`, `.planning/phases/03-build-docs/03-VERIFICATION.md`.
    - Ожидаемая ячейка: «Ни одного TBD/FIXME/XXX/HACK/placeholder/not implemented в фазных файлах не обнаружено».

    Подсекция `### Compatibility Verification`:
    - `git diff --stat HEAD~<N> HEAD -- src/ src-tauri/src/ crates/` → пусто (фаза 3 не правит исполняемый код).

    Подсекция `### Human Verification Required`:
    - Не требуется. DoD проверен программно: grep-инварианты + exit codes + длина README + grep по 6 файлам.

    **(4) Секция `## Deviations`:**

    Параграф, явно описывающий D-V2:

    «**Deviation D-V2 — интерпретация ROADMAP success criterion 4.** Текст ROADMAP «сеть к `npm.ts-vit.com` и `github.com/ts-vit/ai-chat` заблокирована, и последовательность `npm ci && cargo build --workspace && npm run test:all` отрабатывает без ошибок» интерпретирован в `03-CONTEXT.md` (D-V1, D-V2) как «фактическая независимость от приватной сети демонстрируется через grep-инварианты + полный прогон». Физическая блокировка (Windows Firewall outbound rules / hosts-заглушки) **не применялась** — она ловила бы только регрессии в lockfile/manifest, что лучше адресуется CI/pre-commit hook'ом (out of scope этой milestone, см. Deferred Ideas в `03-CONTEXT.md`).

    Доказательная модель: (a) 5 grep-инвариантов в `<temp>` каталоге подтверждают, что репозиторий не содержит ссылок на приватные сервисы (lockfile, manifests, .npmrc); (b) полный прогон `npm ci` + `cargo build --workspace` + `npm run test:all` подтверждает, что сборка фактически работает на этой кодовой базе. Если в lockfile/manifest нет приватных URL — обращения к приватным серверам невозможны по построению.»

    Если `npm run build` пропущен — добавить параграф «**Опциональный шаг BUILD-02 (полный `npm run build`) deferred:** причина <…>. Покрывается косвенно через `cargo build --workspace` + `npm run test:all`. Re-verification возможен в любой момент через повторный прогон `<temp>` процедуры.»

    **(5) Секция `## Gaps Summary`:**

    Гэпов не обнаружено (если так и есть). Параграф по образцу `02-VERIFICATION.md` строки 144-146: «Все <N> observable truths VERIFIED, все 6 BUILD-* requirements SATISFIED, все <M> key links WIRED, антипаттерны не обнаружены, поведенческие spot-checks зелёные. Фаза 3 (Build & Docs) достигла milestone-цели.»

    **(6) Финальный `## Итог`:**

    Параграф «**Verdict: PASS.** Phase 3 «Build & Docs» завершена корректно — milestone-цель «чистый клон без сети полностью собирается» выполнена:
    - `npm ci` устанавливает зависимости через workspace-симлинки во временном клоне без `.npmrc`
    - `cargo build --workspace` компилирует workspace без сети к `github.com/ts-vit/ai-chat`
    - `npm run test:all` зелёный (typecheck + vitest + cargo test)
    - README.md создан как фасадный документ
    - CLAUDE.md + `.planning/codebase/*` зачищены от упоминаний приватных сервисов
    - Deviation D-V2 явно зафиксирован
    Milestone «убрать приватные зависимости» завершена.»

    Финальная подпись: `*Verified: <ISO>*` + `*Verifier: Claude (gsd-executor)*` или эквивалент.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-build-docs/03-VERIFICATION.md &amp;&amp; head -7 .planning/phases/03-build-docs/03-VERIFICATION.md | grep -cE '^phase:|^verified:|^status: passed|^score:' | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.planning/phases/03-build-docs/03-VERIFICATION.md` существует
    - YAML frontmatter содержит 4 обязательных поля: `phase: 03-build-docs`, `verified: <ISO>`, `status: passed`, `score: <N>/<N> must-haves verified`
    - В файле есть секции `## Goal Achievement`, `### Observable Truths`, `### Required Artifacts`, `### Requirements Coverage`, `## Deviations`, `## Gaps Summary`, `## Итог` (либо эквиваленты): `grep -cE '^## ' .planning/phases/03-build-docs/03-VERIFICATION.md` ≥ 3 (Goal Achievement, Deviations, Gaps/Итог)
    - Все 6 BUILD-* requirements присутствуют в Requirements Coverage таблице с статусом SATISFIED (или PARTIAL для BUILD-02 если build пропущен): `grep -cE '\| BUILD-0[1-6] \|' .planning/phases/03-build-docs/03-VERIFICATION.md` == 6
    - Все 6 строк имеют статус SATISFIED или PARTIAL: `grep -cE '\| BUILD-0[1-6] \|.*\|.*\| ✓?\s*(SATISFIED|PARTIAL)' .planning/phases/03-build-docs/03-VERIFICATION.md` == 6
    - Deviation D-V2 явно упомянут: `grep -c 'D-V2' .planning/phases/03-build-docs/03-VERIFICATION.md` ≥ 1
    - Verdict PASS зафиксирован: `grep -c 'PASS' .planning/phases/03-build-docs/03-VERIFICATION.md` ≥ 1
    - Файл ссылается на TEST-NOTES артефакт: `grep -c '03-03-TEST-NOTES' .planning/phases/03-build-docs/03-VERIFICATION.md` ≥ 1
  </acceptance_criteria>
  <done>
    `03-VERIFICATION.md` создан по формату Phase 2; YAML frontmatter валиден; truths-таблица содержит ≥ 9 строк с маппингом на ROADMAP SC#1-4 + BUILD-01..06; Deviations секция фиксирует D-V2; Verdict PASS; все 6 BUILD-* requirements SATISFIED (или 5 SATISFIED + 1 PARTIAL для BUILD-02 если build пропущен).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `D:\\work-ai\\uni-claude-code` ↔ временный клон `<temp>` | Граница «production-копии» и «верификационной песочницы»; `git clone --no-local` гарантирует независимое дисковое состояние |
| `<temp>` ↔ публичные npm/crates.io registries | Сетевые обращения разрешены (см. CLAUDE.md Constraints: «сетевой доступ к публичным реестрам npm/crates.io остаётся допустимым») |
| `03-VERIFICATION.md` ↔ будущая ревизия milestone | Финальный артефакт milestone «убрать приватные зависимости»; его выводы — основа для готовности к v2 фичам |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Repudiation / отсутствие evidence | `03-03-TEST-NOTES.md` | mitigate | Все 3 команды фиксируются с exit code + duration; временный путь сохраняется в файле — re-run воспроизводим |
| T-03-07 | Information Disclosure (приватные URL утекают в lockfile при будущем `npm update`) | `package-lock.json`, `Cargo.lock`, `Cargo.toml` файлы | accept (для этой milestone) | Grep-инварианты ловят только текущее состояние; защита от регресса — pre-commit hook — out-of-scope (см. Deferred Ideas в `03-CONTEXT.md`). Verifier фиксирует это как «доказательная модель point-in-time, не continuous». |
| T-03-08 | Tampering (`--no-local` опущен) | `git clone` команда | mitigate | Acceptance явно проверяет, что `git clone --no-local` был использован (через HEAD SHA сравнение + путь вне `D:\\work-ai\\`) |
| T-03-09 | Denial of Service (Tauri prod build потребует подпись и зависнет) | `npm run build` step | mitigate | Task 2 явно проверяет `tauri.conf.json` `bundle.signing` перед запуском; при наличии подписи step skip'ается с записью в TEST-NOTES |

</threat_model>

<verification>
- Все acceptance_criteria из Task 1, 2, 3 выполнены.
- `03-03-TEST-NOTES.md` существует, содержит таблицу grep-инвариантов (5 строк, все PASS) и таблицу команд (≥ 3 строки с exit 0).
- `03-VERIFICATION.md` существует, валиден по YAML frontmatter, содержит truths/artifacts/requirements таблицы и Deviations секцию.
- Финальный sanity-check: `git ls-files .planning/phases/03-build-docs/` показывает все 5 файлов (03-CONTEXT, 03-DISCUSSION-LOG, 03-01-PLAN, 03-02-PLAN, 03-03-PLAN, плюс 03-03-TEST-NOTES, 03-VERIFICATION после плана; плюс 03-01-SUMMARY, 03-02-SUMMARY, 03-03-SUMMARY от соответствующих executor'ов).
</verification>

<success_criteria>
Milestone «убрать приватные зависимости» формально PASS:
- Чистый клон без `.npmrc`, без приватной сети — `npm ci` + `cargo build --workspace` + `npm run test:all` exit 0 во временном каталоге
- README + CLAUDE.md + `.planning/codebase/*` приведены в соответствие с вендорированным состоянием
- 5 grep-инвариантов подтверждают отсутствие ссылок на приватные сервисы во всём репозитории
- Deviation D-V2 явно зафиксирован в обоих артефактах
- `03-VERIFICATION.md` готов к чтению — milestone закрыта.
</success_criteria>

<output>
Создать `.planning/phases/03-build-docs/03-03-SUMMARY.md` по шаблону из `$HOME/.claude/get-shit-done/templates/summary.md`. После плана и SUMMARY — обновить `.planning/STATE.md` (`progress.completed_phases: 3`, `progress.percent: 100`, `status: complete` / `milestone-verified`) и `.planning/ROADMAP.md` (отметить Phase 3 как `[x]` Complete, добавить ссылку на `03-VERIFICATION.md`).
</output>
