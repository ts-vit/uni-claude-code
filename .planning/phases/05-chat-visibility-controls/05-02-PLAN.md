---
phase: 05-chat-visibility-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/chat/StatusBar.tsx
  - src/__tests__/StatusBar.test.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "StatusBarProps расширен тремя новыми обязательными полями: model: string | null, sessionId: string | null, usage: Usage | null. Существующие props isRunning и sessionResult сохранены без изменений."
    - "StatusBar импортирует тип Usage из ../../types/claude (помимо существующего SessionResult)."
    - "В рендере StatusBar после Badge и перед существующим sessionResult-блоком добавлены 3 новых поля в порядке: model → session_id+CopyButton → ΣTokens+Tooltip. Layout остаётся flat single-row Group (D-05-01) — никакого нового Stack или Box."
    - "Поле model рендерится как Text size='xs' c='dimmed' с содержимым `chat.model: {model ?? '—'}` — em-dash placeholder при null (D-05-12)."
    - "Поле session_id рендерится как Text size='xs' c='dimmed' с содержимым `chat.session: {sessionId.slice(0, 8)}...` при non-null sessionId, иначе `chat.session: —`. При non-null sessionId рядом рендерится Mantine CopyButton (render-prop) оборачивающий ActionIcon variant='subtle' size='xs' с IconCopy/IconCheck-toggle и Tooltip переключающимся между chat.copySessionId и chat.copied (D-05-02)."
    - "CopyButton.value = полный sessionId (не префикс) — копирование полного UUID в clipboard."
    - "Поле ΣTokens рендерится как Tooltip-обёрнутый Text size='xs' c='dimmed' (D-05-01). При non-null usage Text показывает сумму всех 4 полей Usage через toLocaleString(); Tooltip.label содержит Stack с 4 Text-строками: chat.tokensTooltip.input/output/cacheCreation/cacheRead и соответствующими значениями через toLocaleString(). При null usage — em-dash без Tooltip-content (D-05-03, D-05-12)."
    - "ActionIcon CopyButton получает aria-label={t('chat.copySessionId')} — это позволяет тесту найти кнопку через getByRole('button', { name: 'chat.copySessionId' })."
    - "Существующие поля cost / duration / turns из sessionResult-блока рендерятся ПОСЛЕ новых polей (порядок: badge → model → session → tokens → cost → duration → turns) — D-05-01."
    - "Расширен StatusBar.test.tsx: renderBar helper переписан на options-object форму, добавлены тесты для каждого нового поля (model render + em-dash, session prefix + em-dash, CopyButton presence/absence, tokens sum + em-dash). Существующие 8 тестов не сломаны."
  artifacts:
    - path: "src/components/chat/StatusBar.tsx"
      provides: "Расширенный StatusBar — постоянно видны model, session_id (с CopyButton), накопленный token usage (с Tooltip-breakdown). Закрывает VIS-01, VIS-02, VIS-03 на уровне рендера."
      contains_grep: "CopyButton"
    - path: "src/__tests__/StatusBar.test.tsx"
      provides: "Unit-тесты новых props и поведения StatusBar"
      contains_grep: "model:"
  key_links:
    - from: "src/components/chat/StatusBar.tsx StatusBarProps"
      to: "src/types/claude.ts Usage interface"
      via: "import type { Usage } from '../../types/claude' (план 05-01 SessionMetadata reuse Usage)"
      pattern: "import type \\{[^}]*Usage[^}]*\\} from \"../../types/claude\""
    - from: "src/components/chat/StatusBar.tsx render"
      to: "src/i18n/locales/{en,ru}.json chat.* keys (план 05-01)"
      via: "useTranslation().t('chat.model' | 'chat.session' | 'chat.tokens' | 'chat.copySessionId' | 'chat.copied' | 'chat.tokensTooltip.*')"
      pattern: "t\\(\"chat\\.(model|session|tokens|copySessionId|copied|tokensTooltip)"
    - from: "src/components/chat/StatusBar.tsx CopyButton"
      to: "@mantine/core CopyButton + @tabler/icons-react IconCopy/IconCheck"
      via: "render-prop API (children-функция получает { copied, copy })"
      pattern: "CopyButton[^>]*value=\\{sessionId\\}"
---

<objective>
Расширить `src/components/chat/StatusBar.tsx` тремя новыми полями (model, session_id+copy, ΣTokens+tooltip) — закрывает VIS-01, VIS-02, VIS-03 на уровне рендера. Обновить `src/__tests__/StatusBar.test.tsx` под новые props и покрыть новое поведение.

Привязка к D-XX (LOCKED):
- **D-05-01** flat single-row layout — никакого Stack, расширяем существующий `Group`.
- **D-05-02** session_id префикс 8 символов + CopyButton (полный UUID копируется).
- **D-05-03** cache_creation + cache_read объединены в Σ display, раздельно в Tooltip-breakdown.
- **D-05-12** em-dash placeholders для null; асимметрия model fallback (резолвится в caller — план 05-03 — а StatusBar просто рендерит `model ?? '—'`).

PATTERNS.md → разделы StatusBar Pattern A-E дают точные analog-excerpts. PATTERNS.md → `src/__tests__/StatusBar.test.tsx` даёт точный список test cases.

Purpose: дать пользователю постоянную видимость метаданных активной сессии Claude в StatusBar (Phase 5 success criteria 1, 2, 3) без необходимости прокручивать переписку до начального `system-info` сообщения.

Output: обновлённые `StatusBar.tsx` и `StatusBar.test.tsx`. `npm run typecheck` и `npm run test` оба обязаны проходить. Полная интеграция с ChatPanel (передача новых props и source-of-truth для них) — план 05-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-chat-visibility-controls/05-CONTEXT.md
@.planning/phases/05-chat-visibility-controls/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/components/chat/StatusBar.tsx
@src/__tests__/StatusBar.test.tsx
@src/types/claude.ts
@src/components/chat/MessageItem.tsx
@src/__tests__/setup.ts

<interfaces>
<!-- Существующий контракт StatusBarProps (lines 5-8) — ДО расширения. -->

interface StatusBarProps {
  isRunning: boolean;
  sessionResult: SessionResult | null;
}

<!-- Целевой контракт после плана 05-02 (требуется планом 05-03 для прокидывания props). -->
<!-- Все 3 новых поля — required (не optional через ?), потому что caller всегда явно их передаёт. -->
<!-- null = «нет данных» (D-05-12 em-dash convention). -->

interface StatusBarProps {
  isRunning: boolean;
  sessionResult: SessionResult | null;
  model: string | null;
  sessionId: string | null;
  usage: Usage | null;
}

<!-- Usage shape (для построения accumulator и computed sum в StatusBar). -->
<!-- Источник: src/types/claude.ts:101-106, импортируется в StatusBar.tsx. -->

export interface Usage {
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens: number;
  cache_read_input_tokens: number;
}

<!-- Mantine CopyButton API (render-prop, передаёт { copied, copy } в children). -->
<!-- Новый pattern для проекта (Grep("CopyButton") = 0 hits) — см. PATTERNS.md Pattern D. -->

<CopyButton value={sessionId} timeout={1500}>
  {({ copied, copy }) => (
    <Tooltip label={copied ? t("chat.copied") : t("chat.copySessionId")} withArrow position="top">
      <ActionIcon variant="subtle" size="xs" color={copied ? "teal" : "gray"} onClick={copy} aria-label={t("chat.copySessionId")}>
        {copied ? <IconCheck size={12} stroke={1.5} /> : <IconCopy size={12} stroke={1.5} />}
      </ActionIcon>
    </Tooltip>
  )}
</CopyButton>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить StatusBar.tsx — новые props model/sessionId/usage и рендер 3 новых полей (model, session+CopyButton, ΣTokens+Tooltip)</name>
  <read_first>
    - src/components/chat/StatusBar.tsx (полный файл, 57 строк — будем дописывать props и JSX)
    - src/types/claude.ts (строки 101-106 Usage shape; новый SessionMetadata из плана 05-01)
    - src/components/chat/MessageItem.tsx (строки 45-64 — analog для Tooltip→ActionIcon size='xs' variant='subtle' stylistic convention)
    - .planning/phases/05-chat-visibility-controls/05-CONTEXT.md (раздел Раскладка StatusBar — D-05-01, D-05-02, D-05-03, D-05-12)
    - .planning/phases/05-chat-visibility-controls/05-PATTERNS.md (раздел StatusBar.tsx Pattern A-E — точные excerpts и identifiers)
  </read_first>
  <files>src/components/chat/StatusBar.tsx</files>
  <action>
В `src/components/chat/StatusBar.tsx` выполнить следующие изменения единым атомарным редактированием:

**(1) Импорты (строки 1-3):**
- Расширить импорт из `@mantine/core` до `Group, Text, Badge, ActionIcon, Tooltip, CopyButton, Stack` — добавлены `ActionIcon`, `Tooltip`, `CopyButton`, `Stack`.
- Добавить новый импорт: `import { IconCopy, IconCheck } from "@tabler/icons-react";`.
- Расширить импорт типов: `import type { SessionResult, Usage } from "../../types/claude";` — добавлен `Usage`.

**(2) Интерфейс StatusBarProps (строки 5-8):**
- Добавить 3 новых поля после `sessionResult`: `model: string | null;`, `sessionId: string | null;`, `usage: Usage | null;`. Все три — required (без `?`), nullable через `| null` (D-05-12).
- Существующие поля `isRunning` и `sessionResult` НЕ менять.

**(3) Сигнатура функции (строка 10):**
- Деструктуризация props расширяется до `{ isRunning, sessionResult, model, sessionId, usage }`.

**(4) JSX рендер — вставить 3 новых поля МЕЖДУ закрывающим `</Badge>` (строка 41) и началом блока `{sessionResult && (` (строка 42):**

Порядок и стилистика — match существующему `<Text size="xs" c="dimmed">{t("chat.cost")}: ...</Text>` (строка 45):

(a) **Поле model** — одна строка: `<Text size="xs" c="dimmed">{t("chat.model")}: {model ?? "—"}</Text>` (D-05-12 em-dash).

(b) **Поле session_id** — Group/inline с двумя элементами:
   - `<Text size="xs" c="dimmed">{t("chat.session")}: {sessionId ? `${sessionId.slice(0, 8)}...` : "—"}</Text>`
   - При `sessionId !== null` — рядом рендерится Mantine `<CopyButton value={sessionId} timeout={1500}>` с render-prop children, отдающим `({ copied, copy })` в `<Tooltip label={copied ? t("chat.copied") : t("chat.copySessionId")} withArrow position="top">` обёртку, внутри которой `<ActionIcon variant="subtle" size="xs" color={copied ? "teal" : "gray"} onClick={copy} aria-label={t("chat.copySessionId")}>` с children `copied ? <IconCheck size={12} stroke={1.5} /> : <IconCopy size={12} stroke={1.5} />` (D-05-02, PATTERNS.md Pattern D).
   - При `sessionId === null` CopyButton НЕ рендерится (только em-dash в Text).
   - Группировать Text и CopyButton можно через inline-JSX (React.Fragment или вложенный `<Group gap={4}>`), главное чтобы они визуально стояли рядом и оба внутри outer `<Group gap="md">`. Рекомендация: использовать React Fragment без дополнительной Group-обёртки — структура остаётся flat per D-05-01.

(c) **Поле ΣTokens** — `<Tooltip ...>` обёртывающий `<Text size="xs" c="dimmed">`:
   - При `usage === null` — рендерим только `<Text size="xs" c="dimmed">{t("chat.tokens")}: —</Text>` БЕЗ Tooltip-обёртки (breakdown показывать нечего).
   - При `usage !== null` — обернуть в `<Tooltip withArrow position="top" label={...}>` где label это `<Stack gap={2}>` содержащий 4 `<Text size="xs">` строки: `{t("chat.tokensTooltip.input")}: {usage.input_tokens.toLocaleString()}`, `{t("chat.tokensTooltip.output")}: {usage.output_tokens.toLocaleString()}`, `{t("chat.tokensTooltip.cacheCreation")}: {usage.cache_creation_input_tokens.toLocaleString()}`, `{t("chat.tokensTooltip.cacheRead")}: {usage.cache_read_input_tokens.toLocaleString()}` (D-05-03 раздельные значения в Tooltip).
   - Внутри Tooltip — `<Text size="xs" c="dimmed" style={{ cursor: "default" }}>{t("chat.tokens")}: {sum.toLocaleString()}</Text>` где `sum = usage.input_tokens + usage.output_tokens + usage.cache_creation_input_tokens + usage.cache_read_input_tokens` (D-05-03 объединение в displayed sum).

**(5) Существующий блок `{sessionResult && (...)}` (строки 42-54)** — оставить БЕЗ ИЗМЕНЕНИЙ. Поля cost / duration / turns должны продолжать работать ровно как сейчас. Они рендерятся ПОСЛЕ новых трёх полей.

**(6) Стилистика и обёрточный `<Group>` (строки 14-21)** — НЕ менять `px`, `py`, `gap="md"`, `style` (border, fontSize, backgroundColor). Существующий outer Group остаётся, новые поля встраиваются как дополнительные children.

**Что НЕ менять/добавлять:**
- НЕ менять Badge (строки 22-41) — статус-бейдж остаётся первым элементом.
- НЕ добавлять useState — новые поля — purely presentational пропсы.
- НЕ добавлять useEffect.
- НЕ менять fontSize, background, border outer Group.
- НЕ оборачивать в новый Stack или Box (D-05-01 — flat single-row).
- НЕ копировать absolute positioning и opacity-hover-эффект из MessageItem (это специфика overlay в карточке assistant message — в StatusBar Кнопка inline в Group).
- НЕ менять prop-сигнатуру `<StatusBar>` так чтобы существующие тесты сломались на компиляции — добавление required props сломает StatusBar.test.tsx, но это исправляется в Task 2 этого же плана.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Команда `npm run typecheck` exit 0 (типы корректны).
    - Source assertion: `grep -cE "^  model:\s*string\s*\|\s*null;" src/components/chat/StatusBar.tsx` ≥ 1 (поле model в interface).
    - Source assertion: `grep -cE "^  sessionId:\s*string\s*\|\s*null;" src/components/chat/StatusBar.tsx` ≥ 1 (поле sessionId).
    - Source assertion: `grep -cE "^  usage:\s*Usage\s*\|\s*null;" src/components/chat/StatusBar.tsx` ≥ 1 (поле usage).
    - Source assertion (imports): `grep -nE "import type \{[^}]*Usage[^}]*\}" src/components/chat/StatusBar.tsx | wc -l` ≥ 1.
    - Source assertion (imports): `grep -nE "CopyButton" src/components/chat/StatusBar.tsx | wc -l` ≥ 2 (импорт + использование).
    - Source assertion (imports): `grep -nE "IconCopy|IconCheck" src/components/chat/StatusBar.tsx | wc -l` ≥ 2 (обе иконки используются).
    - Source assertion: `grep -cE "t\(\"chat\.model\"\)" src/components/chat/StatusBar.tsx` ≥ 1 (i18n key model используется).
    - Source assertion: `grep -cE "t\(\"chat\.session\"\)" src/components/chat/StatusBar.tsx` ≥ 1.
    - Source assertion: `grep -cE "t\(\"chat\.tokens\"\)" src/components/chat/StatusBar.tsx` ≥ 1.
    - Source assertion: `grep -cE "t\(\"chat\.copySessionId\"\)" src/components/chat/StatusBar.tsx` ≥ 2 (Tooltip label + aria-label, оба обращения).
    - Source assertion: `grep -cE "t\(\"chat\.copied\"\)" src/components/chat/StatusBar.tsx` ≥ 1.
    - Source assertion: `grep -cE "tokensTooltip\.(input|output|cacheCreation|cacheRead)" src/components/chat/StatusBar.tsx` ≥ 4 (все 4 nested keys в Tooltip-breakdown).
    - Source assertion: `grep -cE "sessionId\.slice\(0,\s*8\)" src/components/chat/StatusBar.tsx` ≥ 1 (префикс 8 символов per D-05-02).
    - Source assertion: `grep -cE "toLocaleString\(\)" src/components/chat/StatusBar.tsx` ≥ 5 (sum + 4 breakdown values).
    - Source assertion: `grep -cE "usage\.input_tokens \+ usage\.output_tokens \+ usage\.cache_creation_input_tokens \+ usage\.cache_read_input_tokens" src/components/chat/StatusBar.tsx` ≥ 1 (формула суммы для display per D-05-03).
    - Source assertion: `grep -cE "\"—\"" src/components/chat/StatusBar.tsx` ≥ 3 (em-dash placeholders для model, session, tokens per D-05-12).
    - Source assertion (regression): `grep -cE "sessionResult\.cost" src/components/chat/StatusBar.tsx` ≥ 1, `grep -cE "sessionResult\.durationMs" src/components/chat/StatusBar.tsx` ≥ 1, `grep -cE "sessionResult\.numTurns" src/components/chat/StatusBar.tsx` ≥ 1 — существующие поля cost/duration/turns не вырезаны.
    - Source assertion (D-05-01 flat layout): `grep -cE "<Stack" src/components/chat/StatusBar.tsx` ≤ 1 (если есть Stack — он только внутри Tooltip.label, не обёртывает root Group). Альтернативная проверка: первый JSX-элемент после `return (` всё ещё `<Group` (не `<Stack`).
    - Backend safety (выполнять через Bash tool): `git diff --name-only src-tauri crates packages | wc -l` возвращает 0.
  </acceptance_criteria>
  <done>
    `src/components/chat/StatusBar.tsx` расширен: 3 новых required props (model, sessionId, usage), 3 новых поля рендера между Badge и существующим sessionResult-блоком, CopyButton render-prop pattern, Tooltip-breakdown для tokens. Существующие поля cost/duration/turns не задеты. `npm run typecheck` exit 0. Тесты пока упадут (renderBar не передаёт новые required props) — фиксится в Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить StatusBar.test.tsx — расширить renderBar helper под новые props и добавить тесты на model/session/tokens поведение</name>
  <read_first>
    - src/__tests__/StatusBar.test.tsx (полный файл, 60 строк — будем переписывать renderBar и добавлять test cases)
    - src/components/chat/StatusBar.tsx (после правок Task 1 — точные имена test-cases должны соответствовать актуальному рендеру)
    - src/__tests__/setup.ts (моки: t возвращает key as-is, mantine/notifications замокан)
    - .planning/phases/05-chat-visibility-controls/05-CONTEXT.md (Claude's Discretion раздел Тесты — 4 обязательных кейса; specifics truths #1, #2, #3, #5, #8, #9)
    - .planning/phases/05-chat-visibility-controls/05-PATTERNS.md (раздел StatusBar.test.tsx — точные test case описания)
  </read_first>
  <files>src/__tests__/StatusBar.test.tsx</files>
  <action>
В `src/__tests__/StatusBar.test.tsx` выполнить следующие изменения:

**(1) Расширить импорты типов:**
- Импортировать `Usage` тип: `import type { SessionResult, Usage } from "../types/claude";`.

**(2) Переписать `renderBar` helper (строки 7-13)** на options-object форму с null-defaults для всех props (включая существующие, чтобы все вызовы стали единообразными):

```tsx
function renderBar(opts: {
  isRunning?: boolean;
  sessionResult?: SessionResult | null;
  model?: string | null;
  sessionId?: string | null;
  usage?: Usage | null;
} = {}) {
  return render(
    <MantineProvider>
      <StatusBar
        isRunning={opts.isRunning ?? false}
        sessionResult={opts.sessionResult ?? null}
        model={opts.model ?? null}
        sessionId={opts.sessionId ?? null}
        usage={opts.usage ?? null}
      />
    </MantineProvider>,
  );
}
```

**(3) Обновить все 8 существующих вызовов `renderBar(...)` (строки 17, 22, 27, 33, 38, 43, 49, 55)** под новый options-object API:
- `renderBar(true)` → `renderBar({ isRunning: true })`
- `renderBar(false)` → `renderBar({ isRunning: false })` (или просто `renderBar()`)
- `renderBar(false, { cost: 0.1234, permissionDenials: [] })` → `renderBar({ sessionResult: { cost: 0.1234, permissionDenials: [] } })`
- … аналогично для остальных
- Поведенческие проверки в этих 8 тестах НЕ менять — они продолжают валидировать ровно ту же логику (status badge, cost/duration/turns rendering).

**(4) Добавить новый describe-блок ИЛИ дописать в конец существующего `describe("StatusBar", ...)` (рекомендуется второе для единства) следующие новые тесты:**

**Тест A (specifics truth #1):** `it("shows model when model prop is set", ...)`
- `renderBar({ model: "claude-sonnet-4-6" })`
- assert: `expect(screen.getByText(/claude-sonnet-4-6/)).toBeInTheDocument()`
- assert: `expect(screen.getByText(/chat\.model/)).toBeInTheDocument()`

**Тест B (D-05-12 em-dash для null model):** `it("shows em-dash for model when null", ...)`
- `renderBar({ model: null })`
- assert: `expect(screen.getByText(/chat\.model.*—/)).toBeInTheDocument()` — em-dash в строке model. ВАЖНО: т.к. em-dash присутствует также в session и tokens полях (3 раза), искать строго через `t("chat.model"): —` паттерн — например `screen.getByText(content => /chat\.model.*—/.test(content))`.

**Тест C (specifics truth #1 для session_id):** `it("shows session_id prefix when sessionId set", ...)`
- `renderBar({ sessionId: "abc12345-def6-7890-abcd-ef1234567890" })`
- assert: `expect(screen.getByText(/abc12345\.\.\./)).toBeInTheDocument()` — отображается prefix 8 символов + троеточие.
- assert: `expect(screen.queryByText(/def6-7890-abcd-ef1234567890/)).not.toBeInTheDocument()` — полный UUID НЕ в DOM-тексте.

**Тест D (D-05-02 CopyButton presence):** `it("renders copy button when sessionId is set", ...)`
- `renderBar({ sessionId: "test-uuid-12345678" })`
- assert: `expect(screen.getByRole("button", { name: "chat.copySessionId" })).toBeInTheDocument()` (aria-label search).

**Тест E (D-05-02 CopyButton absence):** `it("does not render copy button when sessionId is null", ...)`
- `renderBar({ sessionId: null })`
- assert: `expect(screen.queryByRole("button", { name: "chat.copySessionId" })).not.toBeInTheDocument()`.

**Тест F (specifics truth #5 + D-05-03):** `it("shows sum of usage tokens when usage is set", ...)`
- `const usage: Usage = { input_tokens: 100, output_tokens: 50, cache_creation_input_tokens: 0, cache_read_input_tokens: 200 }`
- `renderBar({ usage })`
- assert: `expect(screen.getByText(/chat\.tokens/)).toBeInTheDocument()`
- assert: `expect(screen.getByText(/350/)).toBeInTheDocument()` — сумма всех 4 полей.
- (Опционально) `expect(screen.queryByText(/100$/)).not.toBeInTheDocument()` — отдельных значений в основном Text быть не должно (только в Tooltip, который не рендерится без hover).

**Тест G (D-05-12 em-dash для null usage):** `it("shows em-dash for tokens when usage is null", ...)`
- `renderBar({ usage: null })`
- assert: `expect(screen.getByText(content => /chat\.tokens.*—/.test(content))).toBeInTheDocument()`.

**Что НЕ делать:**
- НЕ тестировать через `userEvent.hover` содержимое Tooltip-breakdown — Mantine Tooltip лениво монтируется, для unit-теста это overkill (Tooltip — purely presentational).
- НЕ тестировать реальный clipboard.writeText — `navigator.clipboard` недоступен в jsdom; тест Theme D ограничивается проверкой что кнопка с правильным aria-label рендерится.
- НЕ удалять и НЕ переписывать существующие 8 тестов (только их вызовы renderBar — конвертированы под options-object).
- НЕ создавать новый файл — расширяем существующий `StatusBar.test.tsx`.
- НЕ создавать собственный helper для копирования — Mantine CopyButton сам управляет clipboard, мы только проверяем DOM-структуру.
  </action>
  <verify>
    <automated>npm run test -- StatusBar</automated>
  </verify>
  <acceptance_criteria>
    - Команда `npm run test -- StatusBar` exit 0 — все тесты (старые конвертированные + новые) проходят.
    - Source assertion: `grep -cE "function renderBar\(opts:\s*\{" src/__tests__/StatusBar.test.tsx` ≥ 1 (helper переписан на options-object).
    - Source assertion: `grep -cE "import type \{[^}]*Usage[^}]*\}" src/__tests__/StatusBar.test.tsx` ≥ 1 (Usage тип импортирован).
    - Source assertion: `grep -cE "model:" src/__tests__/StatusBar.test.tsx` ≥ 3 (как минимум 1 в типах opts + 2 в тестах model rendering).
    - Source assertion: `grep -cE "sessionId:" src/__tests__/StatusBar.test.tsx` ≥ 3 (тип + 2 теста session/CopyButton).
    - Source assertion: `grep -cE "usage:" src/__tests__/StatusBar.test.tsx` ≥ 2.
    - Source assertion: `grep -cE "copySessionId" src/__tests__/StatusBar.test.tsx` ≥ 2 (тесты для CopyButton presence/absence через aria-label).
    - Source assertion: `grep -cE "abc12345|test-uuid-12345678" src/__tests__/StatusBar.test.tsx` ≥ 1 (тестовый UUID для sessionId).
    - Source assertion: `grep -cE "slice|prefix|8" src/__tests__/StatusBar.test.tsx | head -1` ≥ 1 ИЛИ assertion через regex /abc12345\.\.\./ — префикс 8 символов покрыт тестом.
    - Source assertion: `grep -cE "claude-sonnet-4-6" src/__tests__/StatusBar.test.tsx` ≥ 1 (model rendering test).
    - Source assertion: `grep -cE "input_tokens:\s*100" src/__tests__/StatusBar.test.tsx` ≥ 1 (usage sum test fixture).
    - Source assertion: `grep -cE "350" src/__tests__/StatusBar.test.tsx` ≥ 1 (ожидаемая сумма в тесте F).
    - Source assertion: `grep -cE "queryByRole\(\"button\".*copySessionId" src/__tests__/StatusBar.test.tsx` ≥ 1 (CopyButton absence test).
    - Source assertion: `grep -cE "getByRole\(\"button\".*copySessionId" src/__tests__/StatusBar.test.tsx` ≥ 1 (CopyButton presence test).
    - Source assertion (regression): `grep -cE "chat\.cost" src/__tests__/StatusBar.test.tsx` ≥ 1, `grep -cE "chat\.duration" src/__tests__/StatusBar.test.tsx` ≥ 1, `grep -cE "chat\.turns" src/__tests__/StatusBar.test.tsx` ≥ 1 — существующие 3 теста на cost/duration/turns сохранены.
    - Source assertion (regression): `grep -cE "^  it\(" src/__tests__/StatusBar.test.tsx` ≥ 13 (было 8 существующих + минимум 5 новых тестов).
    - Behavior: общее число вызовов `it(...)` соответствует прогону через `npm run test -- StatusBar --reporter=verbose 2>&1 | grep -cE "✓"` ≥ 13.
  </acceptance_criteria>
  <done>
    `src/__tests__/StatusBar.test.tsx` расширен: renderBar helper переписан на options-object с 5 опциональными полями, все 8 существующих тестов сконвертированы и проходят, добавлено минимум 5 новых тестов (model, em-dash model, session prefix + CopyButton presence/absence, tokens sum + em-dash). `npm run test -- StatusBar` exit 0.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` exit 0 — типы корректны (новые props совместимы, Usage импортирован).
- `npm run test -- StatusBar` exit 0 — старые и новые тесты проходят.
- `git diff --name-only` показывает ровно: `src/components/chat/StatusBar.tsx`, `src/__tests__/StatusBar.test.tsx`.
- Backend (`src-tauri/`, `crates/`) и вендорированные пакеты НЕ задеты — Compatibility-инвариант v1.0.
- D-05-01 (flat layout) сохранён — нет нового Stack-обёртки на root уровне.
- D-05-02 (session prefix + CopyButton + полный UUID копируется) реализовано.
- D-05-03 (cache_creation + cache_read объединены в Σ display, раздельно в Tooltip) реализовано.
- D-05-12 (em-dash для null model/session/usage) реализовано в 3 местах.
- Phase 5 Success Criterion #1, #2, #3 закрыты на уровне рендера (corresponding state — план 05-03).
</verification>

<success_criteria>
- `StatusBarProps` расширен 3 required nullable полями.
- В render появились 3 новых поля в порядке model → session+CopyButton → ΣTokens+Tooltip между Badge и существующим sessionResult-блоком.
- Существующие cost/duration/turns поля не задеты.
- Mantine `CopyButton` (новый pattern для проекта) корректно копирует полный sessionId.
- Tooltip ΣTokens содержит breakdown по 4 полям Usage с раздельными toLocaleString-значениями.
- Em-dash placeholder при null для всех 3 новых полей.
- `npm run typecheck` и `npm run test -- StatusBar` exit 0.
- Тесты: 8 существующих + ≥5 новых, все проходят.
</success_criteria>

<output>
Создать `.planning/phases/05-chat-visibility-controls/05-02-SUMMARY.md` со следующими секциями:
- Изменённые блоки в `src/components/chat/StatusBar.tsx` (импорты, props, JSX)
- Список новых тестов в `src/__tests__/StatusBar.test.tsx` с покрытием truths из CONTEXT.md specifics
- Подтверждение, что D-05-01 (flat layout), D-05-02 (CopyButton полный UUID), D-05-03 (Σ display + breakdown), D-05-12 (em-dash) реализованы
- Подтверждение, что Badge / cost / duration / turns поведение не задето
- Подтверждение, что Phase 5 Success Criteria #1, #2, #3 закрыты на уровне рендера (Phase 5 SC #4, #5 — в плане 05-03)
- Маппинг покрытых REQ-IDs: VIS-01 (model rendering), VIS-02 (session + CopyButton), VIS-03 (tokens sum + breakdown)
- Результат `npm run typecheck` и `npm run test -- StatusBar`
</output>
