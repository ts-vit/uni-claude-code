---
phase: 03-build-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [BUILD-04]

must_haves:
  truths:
    - "Файл `README.md` существует в корне репозитория и пишется на русском языке (D-R1)"
    - "README содержит ровно 4 секции по D-R2: краткое описание проекта (1-2 строки), Prerequisites, Commands, ничего лишнего"
    - "Prerequisites перечисляет Node.js + npm, Rust toolchain (stable, edition 2021) через rustup, Claude CLI на PATH, Tauri platform prerequisites со ссылкой на `https://v2.tauri.app/start/prerequisites/` (D-R2)"
    - "Commands перечисляет одной строкой каждую из 7 команд: `npm ci`, `npm run dev`, `npm run build`, `npm run typecheck`, `npm run test`, `npm run test:rust`, `npm run test:all` (D-R2)"
    - "README НЕ упоминает приватный реестр `npm.ts-vit.com` и приватный git-источник `github.com/ts-vit/ai-chat`; НЕ содержит follow-up инструкций «настроить .npmrc» или «получить доступ к ai-chat» (D-R3)"
    - "README НЕ описывает `.planning/`, `.claude/`, `.agents/` — это внутренние инструменты, не часть build-инструкций (D-R4)"
    - "Quickstart-only: длина файла между 35 и 80 строк включительно (D-R1)"
    - "Команды README консистентны с `scripts` в `package.json` (BUILD-04)"
  artifacts:
    - path: "README.md"
      provides: "Top-level README с quickstart-инструкцией сборки из чистого клона"
      contains: "UNI Claude Code"
  key_links:
    - from: "README.md (секция Commands)"
      to: "package.json `scripts`"
      via: "Одноимённые npm-команды"
      pattern: "npm (ci|run (dev|build|typecheck|test|test:rust|test:all))"
    - from: "README.md (секция Prerequisites — Tauri)"
      to: "https://v2.tauri.app/start/prerequisites/"
      via: "Markdown-ссылка"
      pattern: "v2\\.tauri\\.app/start/prerequisites"
---

<objective>
Создать top-level `README.md` (на русском, quickstart-only ~40-60 строк), который описывает, как собрать `uni-claude-code` из чистого клона репозитория. README **не упоминает** приватный реестр `npm.ts-vit.com` и приватный git-источник `github.com/ts-vit/ai-chat` — после Phase 1/2 вендоринга они не нужны. README **не описывает** `.planning/` / `.claude/` / `.agents/` — это внутренние инструменты разработки, а не часть build-инструкции.

Покрывает требование **BUILD-04** (REQUIREMENTS.md): README обновлён — инструкция «как собрать из чистого клона», без упоминания приватного реестра или git-источника ai-chat.

Purpose: Зафиксировать публичный фасад репозитория. После вендоринга это первый файл, который видит человек, открывая репо; он должен дать понятный набор prerequisites и команд, и ни одного намёка на приватные сервисы.

Output: Файл `README.md` в корне репозитория. Длина 35-80 строк (quickstart-only). На русском.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-build-docs/03-CONTEXT.md

# Источники истины для prerequisites и commands
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать README.md в корне репозитория</name>
  <files>README.md</files>
  <read_first>
    - `package.json` — проверить, что секция `scripts` содержит все 7 команд из D-R2 (`dev`, `build`, `typecheck`, `test`, `test:watch`, `test:rust`, `test:all`); если каких-то нет — STOP и сообщить
    - `CLAUDE.md` строки 9-21 — секция «Common Commands» — источник списка команд, проверить совпадение со scripts в package.json
    - `.planning/codebase/STACK.md` строки 5-22, 99-110 — Languages / Runtime / Platform Requirements — источник для секции Prerequisites
    - `.planning/codebase/TESTING.md` строки 22-27 — расшифровка команд `npm run test`, `npm run test:rust`, `npm run test:all`
    - `.planning/phases/03-build-docs/03-CONTEXT.md` решения D-R1, D-R2, D-R3, D-R4 (строки 27-37) — задают язык, обязательное содержимое, запрещённые упоминания, исключения
  </read_first>
  <action>
    Создать новый файл `README.md` в корне репозитория (`D:\work-ai\uni-claude-code\README.md`). Структура файла строго по D-R2:

    1) **H1 заголовок:** `# UNI Claude Code`
    2) **Параграф-описание (1-2 строки):** «Tauri 2 + React 19 + Rust IDE-ассистент на базе Claude Code CLI. Многосессионный чат, встроенный терминал, SSH-туннелирование, управление MCP-серверами и пайплайн задач.» Источник формулировки — `CLAUDE.md:7` + `PROJECT.md`.
    3) **H2 секция `## Prerequisites`** — маркированный список из 4 пунктов:
       - Node.js + npm (свежая LTS — фиксированной версии нет, `.nvmrc` в репозитории отсутствует)
       - Rust toolchain (stable, edition 2021) — установка через https://rustup.rs
       - Claude CLI на PATH — runtime-зависимость для чат-сессий (упоминание «Claude CLI by Anthropic», без жёсткой ссылки на установку)
       - Tauri platform prerequisites (WebView2 на Windows / webkit2gtk на Linux / Xcode CLT на macOS) — со ссылкой `https://v2.tauri.app/start/prerequisites/` (D-R2: не дублируем список, отсылаем к официальной документации Tauri 2)
    4) **H2 секция `## Команды сборки и проверки`** (или эквивалентный русский заголовок) — каждая команда отдельной строкой в виде ```` ```bash` блока ```` с inline-комментарием:
       - `npm ci` — установить зависимости (workspace-симлинки для `@uni-fw/*`, никакого `.npmrc` не нужно)
       - `npm run dev` — Tauri dev (Vite frontend + Rust backend)
       - `npm run build` — production-сборка (Tauri bundle)
       - `npm run typecheck` — `tsc --noEmit`
       - `npm run test` — vitest run (frontend tests)
       - `npm run test:rust` — `cargo test --workspace` (backend tests)
       - `npm run test:all` — typecheck + test + test:rust последовательно

    Запреты (нарушение = провал acceptance):
    - НЕ упоминать `npm.ts-vit.com`, `ts-vit/ai-chat`, `private registry`, `.npmrc`, «приватный реестр», «git dependency», «получить доступ» (D-R3)
    - НЕ описывать `.planning/`, `.claude/`, `.agents/`, `GSD`, `slash-команды` (D-R4)
    - НЕ добавлять секции Overview / Architecture / Troubleshooting / Contributing / License — README остаётся quickstart-only (D-R1)
    - НЕ переводить технические идентификаторы (имена команд, пути файлов, URL) — они остаются verbatim
    - НЕ упоминать `cross-env` или cross-platform поведение dev-скрипта — D-S1 фиксирует `set TAURI_CONFIG=…` как Windows-only осознанное решение

    Целевая длина файла — 35-80 строк включительно (D-R1 «quickstart-only ~40-60 строк», верхняя граница 80 даёт запас под маркированные списки и code-блоки).
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; awk 'END{print NR}' README.md | awk '$1&gt;=35 &amp;&amp; $1&lt;=80 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Файл `README.md` существует в корне репозитория: `test -f README.md` exit 0
    - Длина файла 35-80 строк включительно: `awk 'END{print NR}' README.md` → значение в диапазоне [35, 80]
    - H1 присутствует и содержит `UNI Claude Code`: `grep -q '^# UNI Claude Code' README.md` exit 0
    - Все 7 команд упоминаются: `grep -cE 'npm (ci|run (dev|build|typecheck|test|test:rust|test:all))' README.md` ≥ 7
    - 0 совпадений с приватным реестром: `grep -cE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|приватн(ый|ого) реестр|\.npmrc' README.md` == 0
    - 0 упоминаний внутренних инструментов: `grep -cE '\.planning/|\.claude/|\.agents/|GSD' README.md` == 0
    - Tauri prerequisites ссылка присутствует: `grep -q 'v2\.tauri\.app/start/prerequisites' README.md` exit 0
    - rustup ссылка присутствует: `grep -q 'rustup\.rs' README.md` exit 0
    - 0 секций Overview/Architecture/Troubleshooting/Contributing/License: `grep -cE '^## (Overview|Architecture|Troubleshooting|Contributing|License)' README.md` == 0
  </acceptance_criteria>
  <done>
    README.md создан в корне репозитория, проходит все acceptance criteria, содержит prerequisites + 7 команд + ссылку на Tauri 2 docs, не содержит ни одного упоминания приватных сервисов или внутренних инструментов разработки.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| репо ↔ человек, читающий README | README — публичный фасад репозитория после Phase 1/2 вендоринга |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README.md | mitigate | Запрет на упоминание `npm.ts-vit.com` / `ts-vit/ai-chat` — приватные URL не должны утечь в публичный README. Проверка через grep-инвариант в acceptance_criteria (0 совпадений). |
| T-03-02 | Tampering (по аналогии — desync README/реальности) | README.md vs package.json | mitigate | Acceptance включает проверку «все 7 команд из package.json присутствуют» через `grep -cE` ≥ 7. Если команда исчезнет/переименуется в `package.json`, README рассинхронизируется и будет пойман BUILD-05 grep-инвариантами. |

</threat_model>

<verification>
- Все acceptance_criteria из Task 1 выполнены (см. выше — 9 пунктов).
- Файл `README.md` коммитится в git и появляется в `git ls-files` после плана.
- Финальный grep-sweep (выполнить вручную после плана для уверенности):
  - `grep -inE 'npm\.ts-vit\.com|ts-vit/ai-chat|private registry|\.npmrc' README.md` → 0 матчей
  - `grep -cE '^## ' README.md` → 2 (только Prerequisites и Команды; H1 не считается)
</verification>

<success_criteria>
README.md создан, проходит все 9 acceptance criteria Task 1, готов как фасадный документ для чистого клона репозитория. После этого плана любой человек, открывающий репо, видит инструкцию «`npm ci` + `npm run dev`» без необходимости настраивать приватные сервисы.
</success_criteria>

<output>
Создать `.planning/phases/03-build-docs/03-01-SUMMARY.md` по шаблону из `$HOME/.claude/get-shit-done/templates/summary.md` после выполнения плана.
</output>
