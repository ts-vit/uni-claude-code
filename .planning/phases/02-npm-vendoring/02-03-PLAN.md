---
phase: 02-npm-vendoring
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - package-lock.json
  - .planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md
autonomous: true
requirements: [NPM-07, NPM-08, NPM-09, NPM-10]

must_haves:
  truths:
    - "`package-lock.json` регенерирован — нет ни одной подстроки `npm.ts-vit.com` (NPM-08 → DoD)"
    - "Записи для `@uni-fw/ui`, `@uni-fw/ssh-ui`, `@uni-fw/terminal-ui` в `package-lock.json` указывают на workspace-symlink (формат npm 7+: `\"link\": true` либо `\"resolved\": \"file:packages/uni-fw-*\"`), НЕ на `\"resolved\": \"https://npm.ts-vit.com/...\"`"
    - "Команда `npm ci` завершается с exit code 0 (NPM-08)"
    - "Команда `npm run typecheck` завершается с exit code 0 — типы из `packages/uni-fw-*/src/` резолвятся через workspace symlink + `moduleResolution: \"bundler\"` (NPM-09)"
    - "Команда `npm run test` (vitest run) завершается с exit code 0 — корневые тесты в `src/__tests__/` зелёные, моки `vi.mock(\"@uni-fw/...\")` продолжают работать (NPM-10)"
    - "Импорты `@uni-fw/ui`, `@uni-fw/ssh-ui`, `@uni-fw/terminal-ui` в 9 файлах `src/` (verified via grep: `src/main.tsx`, `src/App.tsx`, `src/components/{ClaudeMdEditor,chat/MessageItem,DualPanelLayout,HistoryPage,McpServersPage,ProjectSidebar,SettingsPage,SshStatusIndicator}.tsx` + 5 файлов `src/__tests__/`) НЕ изменены (NPM-07, D-12 compatibility-constraint)"
    - "CSS-импорт `@uni-fw/ui/src/styles/markdown.css` из `src/main.tsx:7` продолжает работать через workspace symlink + поле `exports.\"./src/styles/*\"` (D-03)"
    - "Файл `src/__tests__/setup.ts` НЕ модифицирован — моки `vi.mock(\"@uni-fw/...\")` работают независимо от того, откуда резолвится пакет (CONTEXT.md «Reusable Assets»)"
    - "Тесты внутри `packages/uni-fw-*/src/__tests__/` НЕ запускаются корневым `npm run test` (D-05/D-06: они «спящие», скопированы для архива)"
  artifacts:
    - path: "package-lock.json"
      provides: "Регенерированный lockfile без npm.ts-vit.com записей; @uni-fw/* как workspace symlinks"
    - path: ".planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md"
      provides: "Лог: какие тесты были тронуты (если применялся it.skip), либо строка `Все тесты прошли без правок.` (по аналогии с 01-03-TEST-NOTES.md)"
  key_links:
    - from: "package-lock.json"
      to: "вендорированные packages/uni-fw-* (workspace symlinks)"
      via: "Отсутствие `\"resolved\": \"https://npm.ts-vit.com/...\"` для @uni-fw/* пакетов; вместо этого workspace-link записи"
      pattern: "\"link\":|file:packages/uni-fw-"
    - from: "node_modules/@uni-fw/ui (после npm ci)"
      to: "packages/uni-fw-ui (исходный каталог)"
      via: "npm workspace symlink — Vite + tsc резолвят TS-источники напрямую"
      pattern: "@uni-fw/ui"
---

<objective>
Финальный шаг фазы npm Vendoring: регенерировать `package-lock.json` (чтобы он перестал ссылаться на `npm.ts-vit.com` и начал ссылаться на workspace symlinks), выполнить `npm ci` для установки чистого lockfile-аккуратного состояния, и доказать DoD фазы через `npm run typecheck` + `npm run test`.

При необходимости пометить нестабильные тесты в `src/__tests__/` атрибутом `it.skip` с TODO-комментарием — но это маловероятно, потому что моки `@uni-fw/*` уже централизованы в `src/__tests__/setup.ts` и не зависят от происхождения пакета (см. CONTEXT.md «Reusable Assets» и 02-PATTERNS.md «Test-mocks остаются нетронутыми»).

Покрывает требования NPM-07 (импорты в `src/` работают без правок), NPM-08 (`npm ci` без сети к `npm.ts-vit.com`), NPM-09 (`npm run typecheck` зелёный), NPM-10 (`npm run test` зелёный).

Purpose: Установить Definition of Done всей фазы — frontend собирается, типы резолвятся, тесты зелёные без сетевого доступа к `npm.ts-vit.com`. Зафиксировать в `02-03-TEST-NOTES.md` точный список изменённых тестов (если такие будут), чтобы трассировать TODO в milestone v2 (см. MAINT-02 в REQUIREMENTS.md). Прямой аналог Plan 01-03.

Output: Регенерированный `package-lock.json` без `npm.ts-vit.com` записей; зелёные `npm ci` + `npm run typecheck` + `npm run test`; `02-03-TEST-NOTES.md` (если применялся `it.skip`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-npm-vendoring/02-CONTEXT.md
@.planning/phases/02-npm-vendoring/02-PATTERNS.md

# Phase 1 прецедент — паттерн «регенерировать lockfile + полная верификация»
@.planning/phases/01-rust-vendoring/01-03-regenerate-lock-and-verify-PLAN.md

# Должны быть готовы (созданы в Plan 02-01) и переписаны (Plan 02-02)
@package.json
@packages/uni-fw-ui/package.json
@packages/uni-fw-ssh-ui/package.json
@packages/uni-fw-terminal-ui/package.json

# Setup-файл с моками — НЕ должен меняться (CONTEXT.md «Reusable Assets»)
@src/__tests__/setup.ts

# PROJECT.md → Constraints: «Тесты внутри вендорированных пакетов копируются "как есть";
# если они зависят от инфраструктуры ai-chat и падают по unrelated причинам — помечаются
# `it.skip` (Vitest) с комментарием TODO». Аналог Phase 1 #[ignore]-policy для frontend.
# Применимо ТОЛЬКО к корневым тестам src/__tests__/, поскольку packages/uni-fw-*/src/__tests__/
# вообще не запускаются (D-05/D-06).

<interfaces>
<!-- Корневые тесты, которые запустит `npm run test` (vitest run в cwd).
     Если кто-то падёт после vendoring — кандидат на it.skip + TODO. Но этот сценарий маловероятен
     благодаря `vi.mock("@uni-fw/...")` в setup.ts — моки заменяют пакеты на in-memory заглушки.
     Файлы в `src/__tests__/`: -->

src/__tests__/
  - App.test.tsx          (использует useSettings из @uni-fw/ui — мокирован)
  - DualPanelLayout.test.tsx (использует @uni-fw/ui + @uni-fw/terminal-ui — оба мокированы)
  - McpServersPage.test.tsx (свой vi.mock на @uni-fw/ui)
  - MessageItem.test.tsx  (импортирует * as UniUi из @uni-fw/ui)
  - SettingsPage.test.tsx (vi.mock на @uni-fw/ssh-ui)
  - SshStatusIndicator.test.tsx (useSettings из @uni-fw/ui — мокирован)

ВАЖНО: setup.ts уже содержит vi.mock для всех трёх пакетов. Если корневой test падает,
причина с большой вероятностью НЕ в vendoring (моки изолируют), а в other-issue —
не маскировать it.skip-ом, а сообщить и разобраться.

Тесты внутри `packages/uni-fw-*/src/__tests__/` НЕ запускаются корневым vitest:
- vitest.config.ts корня указывает setupFiles только на src/__tests__/setup.ts
- Корневой vitest по дефолту ищет тесты в `src/`, не в `packages/`
- Дополнительная защита: можно явно проверить через verify-команду, что vitest список тестов
  не содержит `packages/` префикса (см. acceptance criteria Task 2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить устаревший package-lock.json и регенерировать через npm install</name>
  <files>package-lock.json</files>
  <read_first>
    - D:\work-ai\uni-claude-code\package-lock.json (текущий — содержит записи `"resolved": "https://npm.ts-vit.com/@uni-fw/..."` для трёх пакетов; проверка перед удалением)
    - D:\work-ai\uni-claude-code\package.json (после Plan 02-02 — workspaces объявлен, @uni-fw/* как workspace:*, @xterm/* в dependencies)
    - D:\work-ai\uni-claude-code\packages\uni-fw-ui\package.json (после Plan 02-02 — source-direct entry)
    - D:\work-ai\uni-claude-code\packages\uni-fw-ssh-ui\package.json
    - D:\work-ai\uni-claude-code\packages\uni-fw-terminal-ui\package.json
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-PATTERNS.md (раздел «`package-lock.json` (lockfile-regen)» — точный sequence из Phase 1)
    - D:\work-ai\uni-claude-code\.planning\phases\01-rust-vendoring\01-03-SUMMARY.md (Decisions Made — почему выбран полный rebuild lockfile через rm + generate)
  </read_first>
  <action>
    Принудительно регенерировать `package-lock.json` так, чтобы старые `https://npm.ts-vit.com/@uni-fw/...` записи исчезли и появились новые workspace-link записи (npm 7+ формат — поле `"link": true` либо `"resolved": "file:packages/uni-fw-*"` для пакетов из workspaces).

    Способ (по аналогии с Plan 01-03 / Cargo.lock — rm + generate, без incremental update):

    1. Удалить корневой `package-lock.json` файл (`Remove-Item package-lock.json -Force` или `rm package-lock.json`).
    2. (Защитный шаг) Удалить `node_modules/` целиком, чтобы исключить residual `@uni-fw/*` из старой registry-версии: `Remove-Item -Recurse -Force node_modules` (если он существует). Это аналог `cargo clean` из Plan 01-03 «Если по какой-то причине регенерация не убирает старые ссылки (transitive cache), выполнить `cargo clean` затем повторить».
    3. Выполнить `npm install` (без флагов) — npm 7+ при отсутствии lockfile создаёт новый, подхватывает поле `workspaces` из `package.json`, симлинкует `packages/uni-fw-*` в `node_modules/@uni-fw/*`. Команда МОЖЕТ обращаться к публичному реестру `registry.npmjs.org` для разрешения transitive deps (`react-markdown`, `@xterm/*`, и др.) — это допустимо согласно PROJECT.md Constraints: «Сетевой доступ к публичным реестрам npm/crates.io остаётся допустимым».
    4. После генерации проверить, что в новом `package-lock.json` нет подстроки `npm.ts-vit.com` ни в одной строке.
    5. Также проверить наличие workspace-symlink записей: для каждого из трёх пакетов в `package-lock.json` должна быть запись с одним из признаков `"link": true`, `"resolved": "file:packages/uni-fw-..."` или `"version": "0.1.x"` без `resolved`-URL. Точный формат зависит от версии npm — критерий: НЕТ `resolved`-URL на `npm.ts-vit.com`.

    НЕ выполнять `npm ci`/`npm run typecheck`/`npm run test` на этом таске — они в Task 2. Цель этого таска — только lockfile.

    **Package Legitimacy Gate (npm install gate):** При первом `npm install` после смены registry-структуры — проверить вывод `npm install` на предмет неожиданных пакетов. Если в выводе появляются ASSUMED/SUS-кандидаты (новые транзитивные deps, не упомянутые в RESEARCH.md), остановиться. На этой фазе не ожидается новых пакетов вне списка уже зарегистрированных в существующем `package-lock.json` (плюс три явных `@xterm/*` peer-deps из Plan 02-02). Если diff `npm ls --depth=0` ДО и ПОСЛЕ показывает только ожидаемые изменения (`@uni-fw/* — резолвятся через workspace`, `@xterm/* — добавлены явно), gate проходит автоматически. Если появляются прочие пакеты — поставить плану на паузу, человеческий ревью.

    Если `npm install` падает (например, peer-deps конфликт, неразрешимый граф) — НЕ маскировать `--legacy-peer-deps` или `--force` без явного решения; остановиться, сообщить и разобраться. Соответствие peer-version обязательно.

    Создать атомарный git commit:
    `chore(02-03): regenerate package-lock.json without npm.ts-vit.com`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path package-lock.json -Pattern 'npm\\.ts-vit\\.com' | Measure-Object).Count" — должно вернуть 0; powershell -NoProfile -Command "Test-Path package-lock.json" — должно вернуть True; node -e "const lock=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); for(const k of ['@uni-fw/ui','@uni-fw/ssh-ui','@uni-fw/terminal-ui']){const p=lock.packages&&(lock.packages['node_modules/'+k]||lock.packages['packages/'+k.replace('@uni-fw/','uni-fw-')]); if(!p){console.error('missing entry for '+k); process.exit(2);} if(p.resolved&&p.resolved.includes('npm.ts-vit.com')){console.error('still pointing to private registry: '+k); process.exit(3);}} console.log('ok')" — должно вывести `ok` и exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Файл `package-lock.json` существует в корне репозитория
    - `(Select-String -Path package-lock.json -Pattern 'npm.ts-vit.com').Count` возвращает 0 (zero-tolerance, аналог Plan 01-03 для Cargo.lock)
    - В `package-lock.json` присутствуют записи `node_modules/@uni-fw/ui`, `node_modules/@uni-fw/ssh-ui`, `node_modules/@uni-fw/terminal-ui` либо записи под ключом `packages/uni-fw-ui`, `packages/uni-fw-ssh-ui`, `packages/uni-fw-terminal-ui` (точный ключ зависит от формата `lockfileVersion`)
    - Для каждого из трёх `@uni-fw/*` пакетов в `package-lock.json` НЕТ поля `"resolved": "https://npm.ts-vit.com/..."` (это симптом не-workspace-link)
    - В `package-lock.json` поле `"lockfileVersion"` присутствует и имеет значение >= 2 (npm 7+ формат, обязательно для workspaces)
    - Каталог `node_modules/@uni-fw/ui` существует после `npm install` и содержит symlink/junction на `packages/uni-fw-ui` (Windows: junction; Linux: symlink): `node -e "const fs=require('fs'); const s=fs.lstatSync('node_modules/@uni-fw/ui'); console.log(s.isSymbolicLink()||s.isDirectory()?'ok':'fail')"` выводит `ok`
    - `npm pkg get workspaces` возвращает `["packages/*"]` (sanity-check, что workspaces поле всё ещё валидно)
    - Корневой `package.json` НЕ изменён в этом таске: `git diff --quiet HEAD -- package.json` exits 0
    - Создан git-коммит с заголовком `chore(02-03): regenerate package-lock.json without npm.ts-vit.com`
  </acceptance_criteria>
  <done>`package-lock.json` регенерирован, ни одной ссылки на `npm.ts-vit.com` не осталось, три `@uni-fw/*` пакета представлены workspace-link записями, `node_modules/@uni-fw/*` symlink-и на `packages/uni-fw-*` созданы.</done>
</task>

<task type="auto">
  <name>Task 2: Запустить npm ci + npm run typecheck + npm run test, при необходимости пометить нестабильные корневые тесты it.skip</name>
  <files>.planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md, src/__tests__/*.test.tsx (если потребуются it.skip-правки — что маловероятно)</files>
  <read_first>
    - D:\work-ai\uni-claude-code\package-lock.json (после Task 1 — должен быть чистый)
    - D:\work-ai\uni-claude-code\src\__tests__\setup.ts (моки на @uni-fw/* — НЕ должны меняться)
    - D:\work-ai\uni-claude-code\src\__tests__\App.test.tsx
    - D:\work-ai\uni-claude-code\src\__tests__\DualPanelLayout.test.tsx
    - D:\work-ai\uni-claude-code\src\__tests__\McpServersPage.test.tsx
    - D:\work-ai\uni-claude-code\src\__tests__\MessageItem.test.tsx
    - D:\work-ai\uni-claude-code\src\__tests__\SettingsPage.test.tsx
    - D:\work-ai\uni-claude-code\src\__tests__\SshStatusIndicator.test.tsx
    - D:\work-ai\uni-claude-code\.planning\phases\01-rust-vendoring\01-03-TEST-NOTES.md (формат TEST-NOTES — markdown-таблица или строка «Все тесты прошли без правок.»)
    - D:\work-ai\uni-claude-code\.planning\phases\02-npm-vendoring\02-PATTERNS.md (раздел «Test verification gate» и «TODO-trail / TEST-NOTES артефакт»)
  </read_first>
  <action>
    Шаг A — clean install (sanity check):
    1. Выполнить `npm ci` из корня репозитория. ОЖИДАНИЕ: exit code 0. `npm ci` строже `npm install` — он требует, чтобы lockfile был полностью консистентен с `package.json`; если он падает — это сигнал, что Task 1 не дорегенерировал lockfile. ПРИМЕЧАНИЕ: `npm ci` удалит и пере-создаст `node_modules/`; это нормальное поведение, не блокер.
    2. Если `npm ci` падает с ошибкой `EUSAGE` про несоответствие lockfile vs package.json, остановиться и сообщить — это регрессия Task 1. НЕ запускать повторно `npm install` для «починки» (это маскировка проблемы).

    Шаг B — typecheck (NPM-09):
    3. Выполнить `npm run typecheck` (=> `tsc --noEmit`). ОЖИДАНИЕ: exit code 0.
    4. Если падает с `Cannot find module '@uni-fw/...'` или `has no exported member ...` — это сигнал, что source-direct rewrite в Plan 02-02 либо не сделан, либо `moduleResolution: "bundler"` не подхватывает `./src/index.ts`. Не маскировать через `// @ts-ignore`. Сообщить и проверить:
       - `node_modules/@uni-fw/ui` указывает на `packages/uni-fw-ui` (Task 1 уже это проверил)
       - `packages/uni-fw-ui/package.json` имеет `main: "./src/index.ts"` (Plan 02-02 Task 1)
       - `tsconfig.json` корня содержит `"moduleResolution": "bundler"` (текущее состояние, см. STACK.md)
       Если все три условия выполнены, но typecheck падает — это нетривиальная Vite/tsc интеграционная проблема, поставить плану на паузу.

    Шаг C — vitest (NPM-10):
    5. Выполнить `npm run test` (=> `vitest run`). ОЖИДАНИЕ: exit code 0.
    6. Также убедиться, что vitest НЕ собрал тесты из `packages/uni-fw-*/src/__tests__/` (D-05/D-06): в выводе `npm run test` префикс `packages/` НЕ должен встречаться в списке тест-файлов. Защитная проверка: `npm run test 2>&1 | Select-String -Pattern '^\s*packages/uni-fw-' | Measure-Object | Select -ExpandProperty Count` должна вернуть 0.
    7. Если упали тесты в `src/__tests__/`:
       - Сначала разобраться, по какой причине: vitest показывает stack trace и assertion. Если причина — реальная регрессия (например, mock в `setup.ts` устарел из-за изменения публичного API пакета — что не должно случиться по D-12, но проверить надо), это блокер: остановиться и сообщить.
       - ТОЛЬКО если причина unrelated к vendoring (например, flaky test, временный сбой jsdom) и обвинить в этом vendoring невозможно — для упавшего теста добавить `.skip` к нужному `it`/`describe` и комментарий-строку выше:
         `// TODO: re-enable after vendoring — fails due to <короткая причина из вывода vitest>`
       - Повторить `npm run test` пока exit code не станет 0.
       - Допустимо помечать `.skip` ТОЛЬКО тесты в `src/__tests__/`. Тесты в `packages/uni-fw-*/src/__tests__/` НЕ запускаются и НЕ должны быть тронуты (D-05/D-06).
       - НЕЛЬЗЯ менять `src/__tests__/setup.ts` (CONTEXT.md «Reusable Assets»).

    Шаг D — фиксация TODO trail (по аналогии с 01-03):
    8. Создать файл `.planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md` со списком всех изменённых тестов:
       - Если правок не было — единственная строка `Все тесты прошли без правок.` (формат точно как в `01-03-TEST-NOTES.md`).
       - Если правки были — markdown-таблица с колонками «Файл», «Тест», «Причина» (из вывода vitest). Это станет источником для MAINT-02 (re-enable in v2).
       Также зафиксировать в TEST-NOTES: «Тесты внутри `packages/uni-fw-*/src/__tests__/` НЕ запускаются (CONTEXT.md D-05/D-06 — спящие, для архива)» — отдельной строкой/секцией для трассировки.

    Создать атомарный git commit:
    `chore(02-03): verify npm ci + typecheck + vitest after vendoring`
    с включёнными файлами: `02-03-TEST-NOTES.md` обязательно; любые `it.skip` правки в `src/__tests__/` — если применялись.
  </action>
  <verify>
    <automated>npm ci 1>NUL 2>NUL — exit code 0; npm run typecheck 1>NUL 2>NUL — exit code 0; npm run test 1>NUL 2>NUL — exit code 0; powershell -NoProfile -Command "Test-Path .planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md" — должно вернуть True</automated>
  </verify>
  <acceptance_criteria>
    - `npm ci` exits 0 (lockfile полностью консистентен с package.json + workspaces)
    - `npm run typecheck` exits 0 (типы из вендорированных пакетов резолвятся через source-direct entry)
    - `npm run test` exits 0 (корневые vitest-тесты в `src/__tests__/` зелёные)
    - Вывод `npm run test` НЕ содержит ни одного тест-файла с префиксом `packages/uni-fw-` (D-05/D-06: тесты вендорированных пакетов не запускаются)
    - Если в каком-либо файле `src/__tests__/*.test.tsx` добавлен `.skip`, то ровно над ним присутствует комментарий с подстрокой `TODO: re-enable after vendoring`
    - Файл `src/__tests__/setup.ts` НЕ изменён: `git diff --quiet HEAD~3 -- src/__tests__/setup.ts` exits 0 (HEAD~3 = до начала Phase 2 = до 02-01 коммита)
    - Файл `.planning/phases/02-npm-vendoring/02-03-TEST-NOTES.md` существует и содержит либо строку `Все тесты прошли без правок.`, либо markdown-таблицу с колонками «Файл», «Тест», «Причина», плюс отдельную секцию про `packages/uni-fw-*/src/__tests__/` со ссылкой на D-05/D-06
    - Любой файл под `src/`, кроме перечисленных тест-файлов, НЕ изменён в этом таске: `git diff HEAD -- src/ ':(exclude)src/__tests__/*.test.tsx'` пуст (compatibility-constraint D-12 / NPM-07)
    - Импорты `@uni-fw/ui`, `@uni-fw/ssh-ui`, `@uni-fw/terminal-ui` в `src/main.tsx`, `src/App.tsx`, `src/components/*.tsx` НЕ изменены
    - Создан git-коммит с заголовком `chore(02-03): verify npm ci + typecheck + vitest after vendoring`
  </acceptance_criteria>
  <done>`npm ci` + `npm run typecheck` + `npm run test` все три зелёные. Источники в `src/` не тронуты. TEST-NOTES.md фиксирует точный список изменённых тестов (или их отсутствие). Конец фазы — DoD выполнен.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Конфиг репо → npm CLI | После Plan 02-02 единственные удалённые источники — публичный `registry.npmjs.org` (для transitive deps) + workspace-symlinks (для @uni-fw/*). Доверие = доверие публичному npm registry плюс integrity-check через lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Регенерация package-lock.json подтянет updated transitive deps | mitigate | Lockfile регенерируется без `npm update` — npm install использует minimum-satisfying версии для каждой dep range. Acceptance criterion (npm ci exits 0) гарантирует, что новый lockfile полностью консистентен с package.json. Если transitive dep версии существенно разойдутся, `npm run typecheck` либо `npm run test` упадут — это будет remediated на Шаге B/C (или планировщику будет явный сигнал об отклонении). |
| T-02-07 | Tampering | npm install / npm ci может выполнить package install scripts | accept | Это стандартное поведение npm для всех пакетов в `dependencies`. Список зависимостей фиксированный (то же, что было до vendoring + три явных @xterm/*); никаких новых неизвестных пакетов не добавляется (Package Legitimacy Gate в Task 1). |
| T-02-SC | Tampering | Package Legitimacy Gate для npm installs | mitigate | Action в Task 1 явно требует diff-сравнения `npm ls --depth=0` ДО (нельзя, lockfile удалён) и инспекции вывода `npm install`. На этом плане НЕТ новых пакетов вне ожидаемых: 3 `@xterm/*` (явно из Plan 02-02 + peerDeps снапшота, известные публичные пакеты), 3 transitive deps `@uni-fw/ui` (`react-markdown`, `rehype-highlight`, `remark-gfm` — известные публичные пакеты, использовались и до vendoring транзитивно). Если в выводе появляются неожиданные новые пакеты, Action требует поставить плану на паузу для человеческого ревью. |
</threat_model>

<verification>
End-to-end проверка успеха фазы 2:

1. `(Select-String -Path package.json,package-lock.json,.npmrc -Pattern 'npm\.ts-vit\.com' -ErrorAction SilentlyContinue | Measure-Object).Count` — 0 совпадений (`.npmrc` отсутствует, в остальных файлах нет упоминаний)
2. `npm ci` — exit 0 без сетевого доступа к `npm.ts-vit.com`
3. `npm run typecheck` — exit 0
4. `npm run test` — exit 0
5. Никаких изменений в `src/` (кроме возможных `it.skip` в `src/__tests__/*.test.tsx` — задокументированы в TEST-NOTES.md). Compatibility-constraint D-12 / NPM-07 выполнен.
6. `node_modules/@uni-fw/{ui,ssh-ui,terminal-ui}` существуют как symlink/junction на `packages/uni-fw-*`
7. (Опционально, для подтверждения offline-сборки) Если все transitive deps уже в локальном npm cache, проверка: `rm -rf node_modules && npm ci --offline` — exit 0

Полное end-to-end на чистом клоне без сети — это задача Phase 3 (BUILD-05). Здесь мы доказываем npm-часть.
</verification>

<success_criteria>
Покрытие требований:
- NPM-07: Импорты `@uni-fw/*` в `src/` работают без правок (verified: `git diff HEAD~3 -- src/main.tsx src/App.tsx src/components/` пуст для не-тестовых файлов) ✓
- NPM-08: `npm ci` exits 0 без сети к npm.ts-vit.com (verified: lockfile чистый + npm ci зелёный) ✓
- NPM-09: `npm run typecheck` exits 0 (типы вендорированных пакетов резолвятся через source-direct + workspace symlink) ✓
- NPM-10: `npm run test` exits 0 (vitest зелёный) ✓

Фаза 2 завершена: ВСЕ Success Criteria из ROADMAP.md → Phase 2 → Success Criteria выполнены.
</success_criteria>

<output>
Создать `.planning/phases/02-npm-vendoring/02-03-SUMMARY.md` по завершении.
</output>
