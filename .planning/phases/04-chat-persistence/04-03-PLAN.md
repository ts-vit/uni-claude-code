---
phase: 04-chat-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/__tests__/App.test.tsx
autonomous: false
requirements:
  - PERSIST-01
  - PERSIST-02

must_haves:
  truths:
    - "Тест 'does not render inactive project layout' в src/__tests__/App.test.tsx инвертирован в keep-mounted-семантику: после переключения с проекта p1 на p2 ОБА dual-panel-p1 И dual-panel-p2 присутствуют в DOM."
    - "Добавлен новый тест keep-mounted для view-switch: при переключении в view (например settings) и обратно в main, DualPanelLayout активного проекта остаётся в DOM на протяжении всего перехода (visible когда view===main, скрыт через display:none когда view≠main)."
    - "Мок DualPanelLayout в App.test.tsx упрощён до набора props { projectId } — без initialState и onStateChange (TypeScript-strict mode noUnusedParameters иначе сломает typecheck после плана 04-02)."
    - "Все 4 phase Success Criteria из ROADMAP.md формально верифицированы (3 — автоматически через тесты + ручной чек-лист в SUMMARY; 4-й — ручное подтверждение, что 'Session already running' ошибок не возникает при возврате)."
    - "npm run typecheck, npm run test, npm run test:rust возвращают exit code 0."
  artifacts:
    - path: "src/__tests__/App.test.tsx"
      provides: "Обновлённый App.test.tsx с keep-mounted семантикой и упрощённым DualPanelLayout моком"
      contains_grep: "dual-panel-p1.*dual-panel-p2|dual-panel-p2.*dual-panel-p1"
      forbids_grep: "initialState|onStateChange"
  key_links:
    - from: "src/__tests__/App.test.tsx"
      to: "src/App.tsx (после плана 04-01)"
      via: "тест проверяет, что переключение проекта НЕ размонтирует DualPanelLayout предыдущего проекта"
      pattern: "expect.*dual-panel-p1.*toBeInTheDocument"
    - from: "src/__tests__/App.test.tsx"
      to: "src/App.tsx"
      via: "тест проверяет, что переключение view !== main НЕ размонтирует DualPanelLayout (DOM-узел остаётся в дереве)"
      pattern: "view.*settings.*toBeInTheDocument"
---

<objective>
Закрыть Phase 4 верификацией:

(1) Привести `src/__tests__/App.test.tsx` в соответствие с новой keep-mounted-семантикой:
  - Упростить мок `DualPanelLayout` (убрать `initialState` / `onStateChange` props — их больше нет в публичном API после плана 04-02; иначе TypeScript-strict mode выдаст `noUnusedParameters` / неизвестный prop).
  - Инвертировать существующий тест `does not render inactive project layout` в keep-mounted-семантику: после клика по open-project-2, ОБА `dual-panel-p1` И `dual-panel-p2` должны быть в DOM (этот тест ранее проверял противоположное; теперь он проверяет PERSIST-02).
  - Добавить новый тест keep-mounted для view-switch: переключиться на view (например `settings` через клик иконки настроек или явный setView через mock — см. action), убедиться, что `dual-panel-p1` всё ещё в DOM. Этот тест покрывает PERSIST-01.

(2) Выполнить полный test pass: `npm run typecheck`, `npm run test`, `npm run test:rust` — все три зелёные. `test:rust` запускается как sanity-check, что backend не задет (D-04-06).

(3) Ручная верификация всех 6 truths из CONTEXT.md specifics §3 в режиме приложения (`npm run dev`) с занесением результатов в `04-03-SUMMARY.md`. Чек-лист обязателен — это формальное закрытие фазы.

Purpose: Финальная верификация Phase 4 на уровне автоматических тестов и ручного UAT. После этого плана PERSIST-01 и PERSIST-02 могут быть отмечены как Complete в REQUIREMENTS.md.

Output: Обновлённый `src/__tests__/App.test.tsx`. Зелёный `npm run test:all`. SUMMARY-документ с ручным чек-листом всех 6 truths и всех 4 Success Criteria.

Этот план содержит `checkpoint:human-verify` (ручная UAT-проверка), поэтому `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chat-persistence/04-CONTEXT.md
@.planning/phases/04-chat-persistence/04-01-SUMMARY.md
@.planning/phases/04-chat-persistence/04-02-SUMMARY.md
@.planning/ROADMAP.md
@src/App.tsx
@src/components/DualPanelLayout.tsx
@src/__tests__/App.test.tsx
@src/__tests__/setup.ts

<interfaces>
<!-- Существующий мок DualPanelLayout (src/__tests__/App.test.tsx:24-37) — ДО: -->

vi.mock("../components/DualPanelLayout", () => ({
  DualPanelLayout: ({
    projectId,
    onStateChange,
    initialState,
  }: {
    projectId: string;
    onStateChange?: (state: { projectId: string }) => void;
    initialState?: { projectId: string };
  }) => {
    onStateChange?.(initialState ?? { projectId });
    return <div>{`dual-panel-${projectId}`}</div>;
  },
}));

<!-- ПОСЛЕ (целевое состояние): -->

vi.mock("../components/DualPanelLayout", () => ({
  DualPanelLayout: ({ projectId }: { projectId: string }) => (
    <div>{`dual-panel-${projectId}`}</div>
  ),
}));

<!-- Существующий тест (src/__tests__/App.test.tsx:72-88) — ДО: -->
<!-- `expect(screen.queryByText("dual-panel-p1")).not.toBeInTheDocument();` -->

<!-- ПОСЛЕ (инвертировано): -->
<!-- `expect(screen.getByText("dual-panel-p1")).toBeInTheDocument();` -->
<!-- `expect(screen.getByText("dual-panel-p2")).toBeInTheDocument();` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить App.test.tsx — упростить мок DualPanelLayout + инвертировать тест на keep-mounted + добавить тест view-switch</name>
  <read_first>
    - src/__tests__/App.test.tsx (полный файл — правим мок и сам тест)
    - src/App.tsx (для понимания, какие data-роли header-кнопок и aria-label для view-switch — например IconSettings → tooltip 'common.settings'; в моке настроек не используется, можно вызывать setView через клик по соответствующей ActionIcon)
    - src/__tests__/setup.ts (мок i18next — `t(key) => key`, поэтому aria-label кнопки настроек = 'common.settings' либо tooltip)
    - .planning/phases/04-chat-persistence/04-01-SUMMARY.md
    - .planning/phases/04-chat-persistence/04-02-SUMMARY.md
  </read_first>
  <files>src/__tests__/App.test.tsx</files>
  <action>
В `src/__tests__/App.test.tsx` выполнить три связанные правки:

(1) Упростить мок `DualPanelLayout` (текущие строки 24-37). Новый мок принимает только `projectId` props и рендерит `<div>dual-panel-${projectId}</div>`. Никаких `initialState`, `onStateChange`, никаких side-effect вызовов. Это требуется потому что плана 04-02 удалил эти props из публичного API; TypeScript-strict mode выдаст ошибку `noUnusedParameters` или `Property 'initialState' does not exist on type 'DualPanelLayoutProps'` при попытке передать их в production-импорте, а в моке — `noUnusedParameters` сломает typecheck. Точная замена приведена в `<interfaces>` секции `<context>` выше.

(2) Инвертировать существующий тест `it("does not render inactive project layout", ...)` (строки 72-88). Переименовать в `it("keeps previously active project layout mounted when switching projects", ...)`. Тело теста после переключения с p1 на p2:
  - сохраняется: клик «open-project-1», ожидание `dual-panel-p1` в DOM, клик «open-project-2», ожидание `dual-panel-p2` в DOM.
  - заменяется финальная проверка: вместо `expect(screen.queryByText("dual-panel-p1")).not.toBeInTheDocument();` теперь `expect(screen.getByText("dual-panel-p1")).toBeInTheDocument();` И `expect(screen.getByText("dual-panel-p2")).toBeInTheDocument();` — оба DualPanelLayout остаются смонтированными (PERSIST-02).

(3) Добавить новый тест `it("keeps active project layout mounted when switching views", ...)` (Vitest, в том же describe-блоке). Алгоритм:
  - Рендерим App, кликаем `open-project-1`, ждём `dual-panel-p1` в DOM (теперь visible через display:flex).
  - Кликаем по кнопке в header, переключающей view на `settings`. Для теста удобнее всего найти кнопку через её Tooltip-label или иконку. В существующем коде есть `<Tooltip label={t("common.settings")}>` с `ActionIcon` внутри. Используем `screen.getByRole("button", { name: "common.settings" })` (моки i18next возвращают ключ как строку). Если этот селектор не работает — можно использовать `document.querySelector(".tabler-icon-settings")?.closest("button")` (pattern уже применяется в `DualPanelLayout.test.tsx`).
  - После клика по settings-кнопке ожидаем `screen.getByText("settings")` в DOM (мок SettingsPage из строки 39 рендерит `<div>settings</div>`).
  - КРИТИЧЕСКАЯ проверка: `expect(screen.getByText("dual-panel-p1")).toBeInTheDocument();` — то есть DualPanelLayout НЕ перемонтировался при переключении в settings-view (PERSIST-01).
  - Кликаем по settings-кнопке ещё раз (вернёт view → main).
  - Финальная проверка: `expect(screen.getByText("dual-panel-p1")).toBeInTheDocument();` И `expect(screen.queryByText("settings")).not.toBeInTheDocument();` (в реальном DOM мок SettingsPage всё ещё может присутствовать через display:none — поэтому используем `queryByText` через атрибут, см. ниже). Если рантайм-проверка через `queryByText` показывает, что SettingsPage всё ещё в DOM из-за keep-mounted (а это допустимое поведение — план 04-01 ставит компоненты sibling-блоком), оставляем только assertion на `dual-panel-p1`.

ВАЖНО:
- Все существующие моки `SettingsPage`, `PipelinePage`, `HistoryPage`, `FileTreePanel`, `ClaudeMdEditor`, `DiffViewer`, `WelcomeScreen` (строки 39-53) сохраняются. Мок i18next из `src/__tests__/setup.ts` гарантирует, что `t("common.settings")` возвращает строку `"common.settings"` — это используется как accessible name кнопки.
- НЕ менять моки `ProjectSidebar`, `SshStatusIndicator`, `CreateProjectModal` — они работают как раньше.
- НЕ трогать `src/App.tsx`, `src/components/DualPanelLayout.tsx` — они уже в финальном состоянии после планов 04-01 и 04-02.
- Если accessible-name селектор (`getByRole("button", { name: "common.settings" })`) ломается из-за того, что Mantine ActionIcon не пробрасывает Tooltip как aria-label — fallback использовать `document.querySelector(".tabler-icon-settings")?.closest("button")` и кликать через `fireEvent.click(btn!)`. Обоснование fallback документировать комментом в тесте.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- App</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exit 0.
    - `npm run test -- App` exit 0 — все тесты в `App.test.tsx` зелёные (инвертированный + новый view-switch тест).
    - Source assertion: `grep -nE "initialState|onStateChange" src/__tests__/App.test.tsx | grep -v '^//' | wc -l` возвращает 0 (мок очищен).
    - Source assertion: `grep -nE "keeps previously active project layout mounted|keeps active project layout mounted when switching views" src/__tests__/App.test.tsx | wc -l` = 2 (оба теста с обновлёнными именами присутствуют).
    - Source assertion: `grep -c "getByText(\"dual-panel-p1\")" src/__tests__/App.test.tsx` ≥ 2 (используется в обоих тестах: project-switch и view-switch).
    - Source assertion: `grep -c "getByText(\"dual-panel-p2\")" src/__tests__/App.test.tsx` ≥ 1 (PERSIST-02 — оба проекта mounted одновременно).
    - Backend safety: `git diff --name-only` для этого таска показывает только `src/__tests__/App.test.tsx`.
  </acceptance_criteria>
  <done>
    `src/__tests__/App.test.tsx` обновлён: мок DualPanelLayout упрощён, существующий тест инвертирован в keep-mounted-семантику, добавлен новый тест view-switch keep-mounted. `npm run typecheck` и `npm run test -- App` зелёные.
  </done>
</task>

<task type="auto">
  <name>Task 2: Полный test pass — typecheck + JS-тесты + Rust-тесты</name>
  <read_first>
    - package.json (для подтверждения, что `npm run test:all` = `typecheck && test && test:rust`)
  </read_first>
  <files>(не модифицируются — это verification-таск)</files>
  <action>
Запустить полный тестовый прогон проекта:
  - `npm run typecheck` — должен пройти exit 0.
  - `npm run test` — все Vitest-suite зелёные (включая ChatPanel, DualPanelLayout, App, остальные 16+ test-файлов).
  - `npm run test:rust` — `cargo test --workspace` зелёные. Это sanity-check: D-04-06 гарантирует, что backend не задет; если хоть один Rust-тест упал — это ошибка вообще не из этой фазы (либо регрессия другой работы — эскалируем пользователю).

Если какой-то JS-тест падает по причине, не связанной с Phase 4 (например, regression от другой работы) — задокументировать в SUMMARY с пометкой «не относится к Phase 4». Изменения в production-код в этом таске НЕ вносить — только верификация.

Если все три команды зелёные — переходим к таску 3 (human verify).
  </action>
  <verify>
    <automated>npm run typecheck && npm run test && npm run test:rust</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exit 0 — нет TypeScript-ошибок ни в одном файле.
    - `npm run test` exit 0 — все Vitest test-suite зелёные. Ожидаемо все 19 test-файлов работают.
    - `npm run test:rust` exit 0 — все cargo tests зелёные (sanity backend D-04-06).
    - Git diff после этого таска не показывает изменений в production-коде (только новые planning/SUMMARY-файлы фазы).
  </acceptance_criteria>
  <done>
    Все три тестовых прогона (typecheck, test, test:rust) зелёные. Любые unrelated падения задокументированы в SUMMARY с пометкой «не Phase 4». Production-код не модифицируется в этом таске.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручная UAT-верификация всех 6 truths из CONTEXT.md specifics §3 + всех 4 Success Criteria фазы из ROADMAP.md</name>
  <what-built>
    После планов 04-01 и 04-02 `DualPanelLayout` для каждого `openedProject` остаётся всегда смонтированным в DOM. Видимость управляется CSS `display: none / flex`. React-state в `ChatPanel` (messages, sessionResult, refs, стриминг-буферы) и в `DualPanelLayout` (layoutMode, activePanel, splitPosition, discussTabs, discussActiveTab) физически перестаёт теряться при переключении view ↔ view и project ↔ project в пределах `ui.maxOpenProjects`. Backend Claude-runners НЕ задеты — claude-event listener в ChatPanel живёт постоянно и не теряет события.

    Все autoматические тесты (taskи 1-2) зелёные. Теперь нужна ручная проверка на запущенном приложении, что эти 4 Success Criteria выполняются end-to-end. Все 6 truths из CONTEXT.md specifics §3 — детализированные критерии той же верификации.
  </what-built>
  <how-to-verify>
    Запустить dev-режим: `npm run dev` (стартует Vite на порту 1420 + Tauri backend). После загрузки приложения убедиться, что `claude` CLI установлен и доступен — иначе ChatPanel.handleSend бросит ошибку при попытке `claude_start`. Если `claude` недоступен — пометить SC#3 и truth #3 как «не проверено, требуется claude CLI», остальные пункты по-прежнему проверяемы.

    **Чек-лист (последовательно отметить каждый пункт в SUMMARY):**

    Setup:
    1. Открыть приложение, создать или выбрать 2-3 тестовых проекта (например `D:\test-a`, `D:\test-b`, `D:\test-c`).
    2. Убедиться, что в settings → maxOpenProjects ≥ 3 (или просто оставить default 3).
    3. Выбрать проект A → открывается `main` view с DualPanelLayout.

    Truth #1 (PERSIST-01, view ↔ view) — Success Criterion #1:
    4. В проекте A отправить тестовое сообщение (например «hello») в Architect-таб. Дождаться, пока появится `system-info` (Model / Session) и assistant-ответ хотя бы частично.
    5. Кликнуть на иконку settings в header → view меняется на settings. Затем кликнуть settings снова → возврат в main.
    6. Убедиться: массив сообщений во вкладке Architect содержит те же элементы (как минимум `user: hello`, `system-info: Model/Session`, `assistant-text: ...`) что и до переключения. session_id в StatusBar/system-info не пропадает.
    7. Повторить пункт 5 для каждого view: `files`, `diff`, `history`, `claude-md`, `pipeline`. После каждого возврата в `main` — переписка та же.

    Truth #2 (PERSIST-02, project ↔ project) — Success Criterion #2:
    8. Открыть проект B через сайдбар (становится активным).
    9. В проекте B отправить тестовое сообщение.
    10. Вернуться в проект A через сайдбар.
    11. Убедиться: переписка проекта A показывается ровно в том виде, в каком она была до переключения. Перейти в проект B → переписка проекта B полностью на месте.
    12. Повторить с проектом C (если открыт). Циклически: A → B → C → A.

    Truth #3 (PERSIST + Success Criterion #3, события приходят при невидимой панели):
    13. В проекте A отправить запрос, который занимает ≥ 5 секунд (например «напиши длинное сочинение про X»).
    14. Не дожидаясь ответа, переключиться на settings (или на проект B).
    15. Подождать достаточно, чтобы Claude точно успел отстримить хотя бы часть текста.
    16. Вернуться в проект A / view main.
    17. Убедиться: assistant-text-сообщение в DOM содержит больше текста, чем было видно в момент ухода — то есть события `claude-event` приходили в фоне и накапливались. На момент возврата стриминг либо продолжается, либо уже finalised (result-event пришёл).

    Truth #4 (Success Criterion #4, panelId и runners стабильны):
    18. В проекте A с активным running-табом (после пункта 13, пока isRunning=true) переключиться на settings и обратно на main. Попробовать отправить второе сообщение в тот же таб (PromptInput может быть disabled пока isRunning=true — это OK; нажать Stop, дождаться idle, и тогда отправить).
    19. Убедиться: НЕ появляется уведомление «Session already running» (panelId стабильный, backend runner не пересоздаётся). Если такое уведомление появилось — это блокирующий regression фазы.

    Truth #5 (panelId стабилен):
    20. Открыть React DevTools (если доступно через built-in Tauri WebView — иначе вручную смотреть в логах). Запомнить инстанс ChatPanel активной вкладки до переключения, после переключения — тот же инстанс (или хотя бы тот же panelId="discuss-1"). Если DevTools недоступен — проверить через console.log temporarily, либо принять на основании truth #1 (если messages сохранились — значит ChatPanel инстанс тот же).

    Truth #6 (handleCloseTab не сломан):
    21. В проекте A добавить второй таб (кнопка `+` в SessionTabs).
    22. Отправить сообщение в новом табе, дождаться ответа.
    23. Закрыть второй таб (кнопка `×`). Убедиться: claude_stop вызван без ошибок (нет красных уведомлений), оставшийся таб корректно становится активным.

    Final verification:
    24. typecheck/test/test:rust из таска 2 — все зелёные (подтверждено автоматикой).
    25. Не появилось «Session already running» (truth #4 / SC#4) — подтверждено в пункте 19.
    26. Все 4 Success Criteria фазы из `ROADMAP.md` (lines 33-36) подтверждены.

    Если какой-то пункт чек-листа не проходит — это блокирующая регрессия Phase 4, фаза не завершена.
  </how-to-verify>
  <resume-signal>Type "approved" если все 26 пунктов чек-листа отмечены зелёными, либо опишите проблему / нерабочий пункт. При "approved" исполнитель создаёт 04-03-SUMMARY.md с заполненным чек-листом и закрывает фазу.</resume-signal>
</task>

</tasks>

<verification>
- Автоматическая: `npm run typecheck`, `npm run test`, `npm run test:rust` — все три exit 0.
- Source-level: `src/__tests__/App.test.tsx` содержит инвертированный тест keep-mounted и новый view-switch тест; мок DualPanelLayout без initialState/onStateChange.
- Ручная UAT: 26-пунктовый чек-лист всех 6 truths + 4 SC проходит (Task 3).
- Backend isolation: `git diff --name-only HEAD~3 HEAD -- src-tauri crates` пусто (фаза не задела backend — D-04-06).
- Vendored packages isolation: `git diff --name-only HEAD~3 HEAD -- packages crates/uni-*` пусто (compatibility-инвариант с milestone v1.0).
</verification>

<success_criteria>
Все 4 Success Criteria Phase 4 формально верифицированы:
1. View ↔ view: переписка во вкладках Architect/Developer сохраняется при main → settings/files/diff/history/claude-md/pipeline → main (truths #1, ручной чек-лист пункты 4-7).
2. Project ↔ project: переписка во вкладках каждого openedProject сохраняется при переключениях A → B → C → A (truths #2, пункты 8-12).
3. Активная сессия в фоне: при isRunning === true события claude-event продолжают приходить, при возврате видна догнавшаяся переписка (truths #3, пункты 13-17).
4. panelId стабилен: нет «Session already running» при возврате на ранее активную вкладку (truths #4, пункты 18-19).

PERSIST-01 и PERSIST-02 могут быть отмечены как Complete в `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
Создать `.planning/phases/04-chat-persistence/04-03-SUMMARY.md` с секциями:
- Изменения в `src/__tests__/App.test.tsx` (точные правки мока и тестов)
- Результаты `npm run typecheck`, `npm run test`, `npm run test:rust`
- Заполненный 26-пунктовый ручной чек-лист (truths #1-6 + SC #1-4) с галочками либо отметками «не проверено / regression / OK»
- Подтверждение, что `src-tauri/**`, `crates/**`, `packages/**` не задеты этой фазой (git diff)
- Финальная отметка: фаза 4 завершена, PERSIST-01 и PERSIST-02 готовы к переключению в REQUIREMENTS.md Complete (выполняется orchestrator-командой `/gsd:complete-phase 4` либо вручную)
</output>
</content>
</invoke>